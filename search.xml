<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>关于Blob、FileReader、FormData的那些事</title>
    <url>/2017/07/05/about-blob-filereader-formdata/</url>
    <content><![CDATA[<h1 id="Blob对象是什么"><a href="#Blob对象是什么" class="headerlink" title="Blob对象是什么"></a>Blob对象是什么</h1><p>从<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Blob" target="_blank" rel="external">MDN</a>上有这样一段描述：</p>
<blockquote>
<p>一个 Blob对象表示一个不可变的, 原始数据的<strong>类似文件对象</strong>。Blob表示的数据不一定是一个JavaScript原生格式。 File 接口基于Blob，继承 blob功能并将其扩展为支持用户系统上的文件。</p>
</blockquote>
<p>引用中的关键词是保存着原始数据的类似文件的对象，所谓类似文件的对象可以理解为这种对象本身不是文件，但可以从原始数据解析出文件数据。</p>
<p>既然要解析，就需要知道Blob对象的类型，所以创建Blob的时候第二个参数就是配置项，我们可以配置所有<a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Basics_of_HTTP/MIME_types" target="_blank" rel="external">MIME类型</a>，比如将配置项的type制定为’text/xml’，’text/plain’，’text/css’等，Blob构造函数语法如下（参考MDN）：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> aBlob = <span class="keyword">new</span> Blob( array, options );</div></pre></td></tr></table></figure></p>
<ul>
<li>array 是一个由ArrayBuffer, ArrayBufferView, Blob, DOMString 等对象构成的 Array ，或者其他类似对象的混合体，它将会被放进 Blob.</li>
<li>options 是一个可选的Blob熟悉字典，它可能会指定如下两种属性：<ul>
<li>type，默认值为 “”，它代表了将会被放入到blob中的数组内容的MIME类型。</li>
<li>endings，默认值为”transparent”，它代表包含行结束符\n的字符串如何被输出。 它是以下两个值中的一个： “native”，代表行结束符会被更改为适合宿主操作系统文件系统的惯例，或者 “transparent”, 代表会保持blob中保存的结束符不变 </li>
</ul>
</li>
</ul>
<p>Blob创建出来，那JS种如何读取或者说从原始数据解析出文件数据呢？从Blob中读取内容的唯一方法是使用<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FileReader" target="_blank" rel="external">FileReader</a>。</p>
<h1 id="FileReader对象"><a href="#FileReader对象" class="headerlink" title="FileReader对象"></a>FileReader对象</h1><p>故名思议，FileReader对象用于读取文件数据，所谓的文件数据其实就是Blob对象和File对象（因为File对象继承于Blob对象）。<br>由于文件的读取时间基于文件的大小，所以FileReader读取的过程为设计为异步，如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> fileReader = <span class="keyword">new</span> FileReader();</div><div class="line">FileReader.addEventListener(<span class="string">'onload'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</div><div class="line">	<span class="comment">// 读取的数据保存在fileReader实例的result属性</span></div><div class="line">	<span class="keyword">var</span> data = e.target.result</div><div class="line">	<span class="comment">// Todo...</span></div><div class="line">&#125;)</div><div class="line">fileReader.readAsDataURL(file)</div></pre></td></tr></table></figure></p>
<p>这个过程跟预加载图片的过程相同，<strong>生成实例-&gt;监听-&gt;开始加载</strong>，上面的例子以读取文件为例，使用readAsDataURL的方法，FileRader还有三种读取为其他数据类型的方法：</p>
<table>
<thead>
<tr>
<th>方法名</th>
<th>参数</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>readAsBinaryString</td>
<td>file</td>
<td>将文件读取为二进制编码</td>
</tr>
<tr>
<td>readAsBinaryArray</td>
<td>file</td>
<td>将文件读取为二进制数组</td>
</tr>
<tr>
<td>readAsText</td>
<td>file[, encoding]</td>
<td>按照格式将文件读取为文本，encode默认为UTF-8</td>
</tr>
<tr>
<td>readAsDataURL</td>
<td>file</td>
<td>将文件读取为DataUrl</td>
</tr>
</tbody>
</table>
<p>另外还有一个abort方法用于阻止文件读取</p>
<p>我们知道Image对象读取图像的事件有onload、onerror、onabort，而FileReader除了这三个事件，还新增了三个对过程的监听事件，onloadstart、onprogress、onloadend，但实际上新增的事件使用的并不多，主要用于大文件读取时进度条实现的需求上。    </p>
<p>在onprogress的事件处理器中，有一个ProgressEvent对象，这个事件对象实际上继承了Event对象，提供了三个只读属性：</p>
<ul>
<li>lengthComputable</li>
<li>loaded  （已读取的字节数）  </li>
<li><ul>
<li>total   （总字节数）  </li>
</ul>
</li>
</ul>
<p>其中事件的lengthComputable属性代表文件总大小是否可知。如果 lengthComputable 属性的值是 false，那么意味着总字节数是未知并且 total 的值为零。</p>
<p>看到这些是否感觉似曾相识，“蓦然回首，那人却在灯火阑珊处”。没错，使用ajax上传文件的时候也有同样的事件钩子，<a href="https://en.wikipedia.org/wiki/XMLHttpRequest" target="_blank" rel="external">XMLHttpRequest Level 2</a>中，xhr的progress事件用于异步请求进度的监听，上传的进度事件绑定在xhr的upload属性中，在异步上传文件时我们可以这样监听进度：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</div><div class="line">xhr.upload.onprogress = <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (e.lengthComputable) &#123;</div><div class="line">        <span class="keyword">var</span> percentComplete = (e.loaded / e.total) * <span class="number">100</span>;</div><div class="line">        $progress.css(<span class="string">'width'</span>, percentComplete + <span class="string">'%'</span>);</div><div class="line">    &#125;</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p>同样地，我们只需要为onprogress事件添加处理器就获取文件读取的进度。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">progressHandler</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> percentLoaded = <span class="built_in">Math</span>.round((e.loaded / e.total) * <span class="number">100</span>);</div><div class="line">    $progress.css(<span class="string">'width'</span>, percentLoaded + <span class="string">'%'</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>FileReader和文件密不可分，既然说到文件上传，就再说一下多文件上传、拖拽上传</p>
<h1 id="FormData与文件上传"><a href="#FormData与文件上传" class="headerlink" title="FormData与文件上传"></a>FormData与文件上传</h1><h2 id="多文件上传"><a href="#多文件上传" class="headerlink" title="多文件上传"></a>多文件上传</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> fileInput = <span class="built_in">document</span>.getElementById(<span class="string">"myFile"</span>);</div><div class="line"><span class="keyword">var</span> files = fileInput.files;</div><div class="line"><span class="keyword">var</span> formData = <span class="keyword">new</span> FormData();</div><div class="line"></div><div class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; files.length; i++) &#123;</div><div class="line">    <span class="keyword">var</span> file = files[i];</div><div class="line">    formData.append(<span class="string">'files[]'</span>, file, file.name);</div><div class="line">    xhr.open(<span class="string">"POST"</span>, <span class="string">"/upload.php"</span>);</div><div class="line"></div><div class="line">	xhr.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	    <span class="keyword">if</span>(<span class="keyword">this</span>.status === <span class="number">200</span>)&#123;</div><div class="line">	        <span class="comment">//对请求成功的处理</span></div><div class="line">	    &#125;</div><div class="line">	&#125;</div><div class="line">	<span class="comment">// 如果要监听进度</span></div><div class="line">	xhr.upload.onprogress = progressHandler</div><div class="line">	</div><div class="line">	xhr.send(formData);</div><div class="line">	xhr = <span class="literal">null</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这是将所有的文件发送一次异步请求，而我们监听的进度也是所有文件上传的总进度，如果我们需要单独监听单个文件的上传进度，只需改成递归的方式依次发送请求。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> fileInput = <span class="built_in">document</span>.getElementById(<span class="string">"myFile"</span>);</div><div class="line"><span class="keyword">var</span> files = fileInput.files;</div><div class="line"><span class="keyword">var</span> i = <span class="number">0</span>;</div><div class="line"></div><div class="line"><span class="keyword">var</span> uploadFile = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">var</span> formData = <span class="keyword">new</span> FormData();</div><div class="line">	formData.append(files[i]);</div><div class="line"></div><div class="line">	xhr.open(<span class="string">"POST"</span>, <span class="string">"/upload.php"</span>);</div><div class="line"></div><div class="line">	xhr.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	    <span class="keyword">if</span>(<span class="keyword">this</span>.status === <span class="number">200</span>)&#123;</div><div class="line">			<span class="comment">//进行下一次请求</span></div><div class="line">	        i++;</div><div class="line">	        <span class="keyword">if</span>(i != files.length)&#123;</div><div class="line">			    uploadFile()</div><div class="line">			&#125;  </div><div class="line">	    &#125;</div><div class="line">	&#125;</div><div class="line">	<span class="comment">// 如果要监听进度</span></div><div class="line">	xhr.upload.onprogress = progressHandler</div><div class="line">	</div><div class="line">	xhr.send(formData);</div><div class="line">	xhr = <span class="literal">null</span>;</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="拖拽上传"><a href="#拖拽上传" class="headerlink" title="拖拽上传"></a>拖拽上传</h2><p>拖拽上传只需了解HTML5基本的拖拽事件即可，参考MDN的<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/HTML_Drag_and_Drop_API" target="_blank" rel="external">HTML 拖放 API</a>。</p>
<ul>
<li>drag : 元素被拖拽时由<strong>拖拽元素</strong>频繁触发的事件</li>
<li>dragstart : 拖拽时开始时由<strong>拖拽元素</strong>触发的事件</li>
<li>dragend : 拖拽结束时触发由<strong>拖拽元素</strong>的事件</li>
<li>dragover : 当拖拽元素进入放置区域时由<strong>放置元素</strong>频繁触发的事件(每隔几百毫秒就会触发一次)</li>
<li>dragenter : 当拖拽元素进入放置区域时由<strong>放置元素</strong>触发的事件</li>
<li>dragleave : 当拖拽元素离开放置区域时由<strong>放置元素</strong>触发的事件</li>
<li>drop : 当拖拽元素在放置区域放置时由<strong>放置元素</strong>触发的事件</li>
</ul>
<p><strong>持续触发的事件</strong>：drag和dragover<br><strong>发生在拖拽元素的事件</strong>：drag、dragstart、dragend<br><strong>发生在放置元素的事件</strong>：dragover 、dragenter 、dragleave、drop<br><strong>事件触发次序：</strong>dragstart -&gt; drag -&gt; dragenter -&gt; dragover -&gt; dragleave -&gt; drop -&gt; dragend</p>
<p>当我们拖放文件到浏览器中时，浏览器默认的行为是浏览器将当前页面重定向到被拖拽元素所指向的资源上，因此需要阻止dragenter、dragover、drop的默认行为，这样才能使drop事件被触发。（最好同时阻止冒泡）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> dropArea;</div><div class="line"></div><div class="line">dropArea = <span class="built_in">document</span>.getElementById(<span class="string">"dropArea"</span>);</div><div class="line">dropArea.addEventListener(<span class="string">"dragenter"</span>, handleDragenter, <span class="literal">false</span>);</div><div class="line">dropArea.addEventListener(<span class="string">"dragover"</span>, handleDragover, <span class="literal">false</span>);</div><div class="line">dropArea.addEventListener(<span class="string">"drop"</span>, handleDrop, <span class="literal">false</span>);</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleDragenter</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">	<span class="comment">/*若要兼容ie</span></div><div class="line">	window.event? window.event.cancelBubble = true : e.stopPropagation();</div><div class="line">	window.event? window.event.returnValue = false : e.preventDefault();*/</div><div class="line">    e.stopPropagation();</div><div class="line">    e.preventDefault();</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleDragover</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">    e.stopPropagation();</div><div class="line">    e.preventDefault();</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleDrop</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">    e.stopPropagation();</div><div class="line">    e.preventDefault();</div><div class="line"></div><div class="line">    <span class="keyword">var</span> dt = e.dataTransfer;</div><div class="line">    <span class="keyword">var</span> files = dt.files;</div><div class="line"></div><div class="line">    <span class="comment">// FileReader将图片读取为dataUrl并立刻展示..省略</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>值得注意的是：触发dragstart事件后，其他元素的mousemove、mouseover、mouseenter、mouseleaver、mouseout事件均不会被触发。</p>
<p>上面拖拽回调中的事件对象，继承自 MouseEvent 对象，它的<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/DataTransfer" target="_blank" rel="external">dataTransfer</a>属性保存着拖拽对象的相关信息。</p>
<p>DataTransfer对象有几个重要的属性，其中files属性保存文件的数据。<br>effectAllowed 和 dropEffect 最主要的作用是，用于配置拖拽操作过程中鼠标指针的类型以便提示用户后续可执行怎样的操作；其次的作用是，控制 drop 事件的触发与否。当显示禁止的指针样式时，将无法触发目标元素的 drop 事件。</p>
<p>注意：只能在dragstart中设置effectAllowed，只能在dragover中设置dropEffect </p>
<p>拖拽后通过FileReader读取立刻显示图片优化体验，因为用户可能需要确定是否更换图片，然后单击按钮才将图片，这样可以防止不必要的请求，接下来就是上文提到的FormData上传文件，这里不再赘述。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Blob</tag>
        <tag>FileReader</tag>
        <tag>FormData</tag>
      </tags>
  </entry>
  <entry>
    <title>ajax请求数据</title>
    <url>/2016/05/25/ajax-and-php/</url>
    <content><![CDATA[<h2 id="表单数据提交"><a href="#表单数据提交" class="headerlink" title="表单数据提交"></a>表单数据提交</h2><p>核心方法：serialize（）：可以将表单中的数据序列化成key=value&amp;key=value…的形式<br>核心代码：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="string">"#test"</span>).submit(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    $.ajax(&#123;</div><div class="line">        <span class="attr">type</span>: <span class="string">"POST"</span>,</div><div class="line">        <span class="attr">url</span>:<span class="string">"form.php"</span>,</div><div class="line">        <span class="attr">data</span>:$(<span class="string">'#test'</span>).serialize(),<span class="comment">// 你的formid</span></div><div class="line">        success: <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">            $(<span class="string">"#res"</span>).html(data);</div><div class="line">        &#125;，</div><div class="line">        error: <span class="function"><span class="keyword">function</span>(<span class="params">request</span>) </span>&#123;</div><div class="line">            alert(<span class="string">"Connection error"</span>);</div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">        <span class="keyword">return</span> <span class="literal">false</span>;                   <span class="comment">// 阻止表单默认提交事件，防止页面跳转</span></div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h2 id="PHP获取数据的方式"><a href="#PHP获取数据的方式" class="headerlink" title="PHP获取数据的方式"></a>PHP获取数据的方式</h2><p>ajax中data可以提交的数据方式：<br><strong>普通键值对的方式</strong>：PHP后台可以通过_POST[‘键名’]或 _GET[‘键名’]获取数据<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">  $.ajax(&#123;</div><div class="line"><span class="attr">url</span>: <span class="string">'test2.php'</span>,</div><div class="line"><span class="attr">type</span>: <span class="string">'POST'</span>,</div><div class="line"><span class="attr">data</span>: &#123;<span class="string">"姓名"</span>: <span class="string">'张三'</span>,<span class="string">"年龄"</span>:<span class="number">14</span>&#125;,       <span class="comment">//等价于"姓名=张三&amp;年龄=14",属于传入基本键值对</span></div><div class="line">success: <span class="function"><span class="keyword">function</span>(<span class="params">data</span>)</span>&#123;</div><div class="line">	$(<span class="string">"#content"</span>).html(data);</div><div class="line">&#125;,</div><div class="line"><span class="attr">error</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">	alert(<span class="string">"connection fail"</span>);	</div><div class="line">&#125;</div><div class="line">  &#125;);</div></pre></td></tr></table></figure></p>
<p><strong>json字符串的方式</strong>（需要制定contentType）：后台通过file_get_contents(‘php://input’)获取数据<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.ajax(&#123;</div><div class="line">    <span class="attr">type</span>: <span class="string">"POST"</span>,</div><div class="line">    <span class="attr">url</span>:<span class="string">"test1.php"</span>,	</div><div class="line">    **contentType:<span class="string">"application/json;charset=utf-8"</span>**,</div><div class="line">    <span class="attr">data</span>: <span class="built_in">JSON</span>.stringify(json),</div><div class="line">    <span class="attr">error</span>: <span class="function"><span class="keyword">function</span>(<span class="params">request</span>) </span>&#123;</div><div class="line">        alert(<span class="string">"Connection error"</span>);</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">success</span>: <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">    	$(<span class="string">"#container"</span>).html(data);</div><div class="line">    &#125;</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h2 id="PHP输出json字符串数据"><a href="#PHP输出json字符串数据" class="headerlink" title="PHP输出json字符串数据"></a>PHP输出json字符串数据</h2><p>在PHP中比较常用的是数组，而PHP中json不是标准数据格式，所以PHP提供了对json数据进行编码和解码的函数。</p>
<ul>
<li><strong>json_encode($arr)</strong>:将数组转化为json数据格式（json字符串）</li>
<li><strong>json_decode(json)</strong>:将json解析成PHP对象，通常会添加第二个参数true，此时会解析成PHP数组</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span></div><div class="line">   	header(<span class="string">"Content-type:text/html;charset=utf-8"</span>);</div><div class="line">   	$arr = <span class="keyword">array</span>(<span class="string">'姓名'</span> =&gt; <span class="string">'张三'</span>, <span class="string">'年龄'</span> =&gt; <span class="string">'14'</span>, <span class="string">'性别'</span> =&gt; <span class="string">'男'</span>);</div><div class="line">   	$res =json_encode($arr) ;            </div><div class="line">   	<span class="comment">//当使用php自带的json_encode对数据进行编码时，中文都会变成unicode，导致不可读。如：对字符串”厦门“进行json_encode后，输出的是"\u53a6\u95e8"。经测试前端通过JSON.parse()解析出json对象后乱码消失</span></div><div class="line">       <span class="keyword">echo</span>($res);</div><div class="line"><span class="meta">?&gt;</span></div></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>汉服秀</title>
    <url>/2016/03/15/annual-meeting/</url>
    <content><![CDATA[<p>有章服之美谓之华；有礼仪之大故称夏。</p>
<video id="video" style="max-width: 100%" controls preload="none" width="640" poster="http://qiniu.huzerui.com/image/2016-3-15-company-annual-meeting-poster.jpg"><br>      <source id="rmvb" src="http://oeu9ic4uq.bkt.clouddn.com/video/2016/3/15/annual-meeting.mp4" type="video/mp4"><br>      <p>Your user agent does not support the HTML5 Video element.</p><br>    </video>

]]></content>
      <tags>
        <tag>年会</tag>
      </tags>
  </entry>
  <entry>
    <title>《百鸟朝凤》观感</title>
    <url>/2016/05/18/bainiaochaofeng/</url>
    <content><![CDATA[<p><strong>有的电影值得一个人细细品味，就这样，在电影院座位静静等候开场，悄无声息。</strong></p>
<p><strong>唢呐</strong>是中国民族吹管乐器的一种，也是中国各地广泛流传的民间乐器。由波斯人发明，唢呐的音色明亮，音量大，管身木制，呈圆椎形，上端装有带哨子的铜管，下端套着一个铜制的喇叭口（称作碗），所以<strong>俗称喇叭</strong>。在台湾民间称为鼓吹，广东地区亦将之称为“八音”。</p>
<p>唢呐也许陌生，但喇叭想必耳熟能详，《百鸟朝凤》这电影正和“喇叭”相关。</p>
<p><strong>A：兄弟说的头头是道，接受一下采访？</strong><br>B：某问题。</p>
<p>A：一句话概括一下电影讲了些什么？<br>B：一个唢呐班子的昌盛、传承、发展、没落为故事主线，展示了中华民族民间乐器唢呐为西方音乐文化所同化的现象，象征着像唢呐一样的某些传统民族文化正在消失。</p>
<p><strong>A：想到什么？</strong><br>B：一个拉着唢呐的老朽沦落到街边卖艺维生的场景，在都市的大街道常常有一些上了年纪的艺人用长笛、二胡等不流行的民间乐器上演绎着同样的片段；又如西方的节日受年轻人的欢迎程度远远高于传统文化节日，以致于节日的气氛、形式多样化、格调上西方节日的气焰往往凌驾于传统节日之上；还有中华戏曲文化，我亲身的例子是偶尔父母在听戏剧，一副悄然自得、沉静其中的感觉，而身旁的我却完全不懂这部戏曲在表达什么，我缺乏对戏曲文化的审美感，抑或说对这种传统文化我一无所知。</p>
<p><strong>A：印象最深的片段？</strong><br>B：电影中焦师傅即使咳着血也要带领自己的弟子将曲子演完，因为他知道在时代变迁的十字路口，寻找到一个肯将唢呐吹到骨子里的接班人是来之不易的，焦老头子要传给他弟子的其实不是唢呐，而是自己是一生的事业、使命、热血。</p>
<p><strong>A：潮汕不是信仰佛教吗？讲讲你的经历</strong><br>B：如果这种民间乐器没有这样寻找接班人，或许早已在当今销声匿迹了，文化传承很重要。潮汕人大都信仰佛教，与其说信仰，不如说佛教给与我们一种寄托亦或者希望。逢年过节或恰初一十五等等，烧香拜佛、请愿保佑不在话下，求平安、包富贵如是而已。然而这些习俗却无法传承到下一代的我们，因为一旦受过高等教育就会定义为“迷信”从而敬而远之了。</p>
<p><strong>A：还有其他补充？</strong><br>B：随时代的发展我想有很多这样的传统文化、习俗在渐渐没落，正如电影中焦师傅那样，从前吹唢呐众人哭喊朝拜，仿佛看脚了唢呐唤起的长辈仙逝的灵魂，但到了游天明那一代唢呐连找活干都变得困难了，更别说当做一门生存的匠活了。</p>
<p>这种没落的变化在天明父亲对他吹唢呐的态度变化中显而易见，从天明小时候为他求师四处寻关系到当他看到天明后来对着唢呐发呆时的一句话“还不丢掉这些破玩意”的转变，不得不说时代发展捧起一些东西的同时也会摔破一些东西，以致于这些即将被摔破但是对于民族而言是不可缺少的东西，最终要通过贴上“非物质文化遗产”来拯救了。</p>
<p><strong>电影往往是社会的真实写照。</strong></p>
<p>传承民族文化是必须的，但这种传承不能只通过班子传班子或者师傅传弟子，因为这样的文化不具有普及性，而是局限在某个区域的传承。把传统文化普及宣扬下去，让更多的青年了解，让更多的人懂得欣赏它的魅力，才是这种文化经久不衰的奥秘。</p>
]]></content>
      <categories>
        <category>电影</category>
      </categories>
      <tags>
        <tag>电影</tag>
      </tags>
  </entry>
  <entry>
    <title>前端都了解的配色策略</title>
    <url>/2017/10/15/color-formula/</url>
    <content><![CDATA[<p>在色彩设计应用中，我们对颜色不同程度的理解，影响到设计页面的表现。可是这跟前端有什么联系么？不是设计师的事情么？</p>
<p>可试想一下如果某些情况下我们需要脱离脱离设计师，比如我们需要做些赏心悦目的示例分享，除了将交互、逻辑做好，没有优雅的配色和设计，可能你的分享会显得苍白无力，下面直入正题。</p>
<h1 id="颜色理论（Color-theory）"><a href="#颜色理论（Color-theory）" class="headerlink" title="颜色理论（Color theory）"></a>颜色理论（Color theory）</h1><p>三原色：指色彩中不能再分解的三种基本颜色，我们通常说的三原色，即<strong>红、黄、蓝</strong>。三原色可以混合出所有的颜色，同时相加为黑色，黑白灰属于无色系。色彩中颜料调配三原色混合色为黑色。</p>
<p><strong>红+黄=橙、黄+蓝=绿、蓝+红=紫</strong></p>
<p>色环（Color  Wheel）：又称色轮、色圈，是<strong>将可见光区域的颜色以圆环来表示</strong>，为色彩学的一个工具，一个基本色环通常包括12种不同的颜色。<br><img src="http://qiniu.huzerui.com/image/2017-10-15-color-ring.gif" alt="色环形成"><br><img src="http://qiniu.huzerui.com/image/2017-10-15-color-ring.jpg" alt="色坏"></p>
<h1 id="配色策略"><a href="#配色策略" class="headerlink" title="配色策略"></a>配色策略</h1><p>在理解的色环之后，在前人的总结和指引下，就衍生出了一些配色策略。注意每一种配色策略都可以因饱和度、明度的变化产生数不清的配色方案。比如下面的相似色两种配色方案，<strong>基本颜色就是四种（主色、辅色、点睛、背景）</strong>。</p>
<p>可以发现它们对应颜色的色相变化值都不超过30，在提升或降低饱和度之后形成的颜色变化从而形成不同配色方案。</p>
<p>配色策略提供的一种指引，具体的颜色值确定靠的更多是灵感和视觉审美。</p>
<h2 id="单色（Monochromatic）"><a href="#单色（Monochromatic）" class="headerlink" title="单色（Monochromatic）"></a>单色（Monochromatic）</h2><p><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-monochromatic.jpg" alt="Monochromatic"></p>
<h2 id="相似色（Analogous）"><a href="#相似色（Analogous）" class="headerlink" title="相似色（Analogous）"></a>相似色（Analogous）</h2><p><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-analogous.jpg" alt="Analogous"><br><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-analogous-1.jpg" alt="Analogous"></p>
<h2 id="互补色（Complementary）"><a href="#互补色（Complementary）" class="headerlink" title="互补色（Complementary）"></a>互补色（Complementary）</h2><p>如：红绿对比、蓝橙、黄紫对比<br><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-complementary.jpg" alt="Complementary"></p>
<h2 id="分割互补色（Split-complementary）"><a href="#分割互补色（Split-complementary）" class="headerlink" title="分割互补色（Split complementary）"></a>分割互补色（Split complementary）</h2><p><img src="http://qiniu.huzerui.com/image/2017-10-15-formular-split-complementary.jpg" alt="Split complementary"></p>
<h2 id="三角对立（Triadic-）"><a href="#三角对立（Triadic-）" class="headerlink" title="三角对立（Triadic ）"></a>三角对立（Triadic ）</h2><p><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-triadic.jpg" alt="Triadic "></p>
<h2 id="矩形（Tetradic）"><a href="#矩形（Tetradic）" class="headerlink" title="矩形（Tetradic）"></a>矩形（Tetradic）</h2><p><img src="http://qiniu.huzerui.com/image/2017-10-15-formula-tetradic.jpg" alt="Tetradic"></p>
<h1 id="关于主色调、辅色调、点睛色、背景色"><a href="#关于主色调、辅色调、点睛色、背景色" class="headerlink" title="关于主色调、辅色调、点睛色、背景色"></a>关于主色调、辅色调、点睛色、背景色</h1><p> 主色调<br>页面色彩的主要色调、总趋势，其他配色不能超过该主要色调的视觉面积。（背景白色不一定根据视觉面积决定，可以根据页面的感觉需要。）</p>
<p>辅色调<br>仅次与主色调的视觉面积的辅助色，是烘托主色调、支持主色调、起到融合主色调效果的辅助色调。 </p>
<p>点睛色<br>在小范围内点上强烈的颜色来突出主题效果，使页面更加鲜明生动。</p>
<p>背景色<br>衬托环抱整体的色调，协调、支配整体的作用。</p>
<h1 id="配色工具"><a href="#配色工具" class="headerlink" title="配色工具"></a>配色工具</h1><p>Adobe出品的一个在线配色工具，上边列出了海量的配色方案供我们使用，如果你不满足，也可以试下上传图片建立自己的配色方案，只需上传一张图片，网站便可自动提取图片颜色生成配色方案。</p>
<p>网页版地址：<a href="https://color.adobe.com/zh/create/color-wheel/" target="_blank" rel="external">https://color.adobe.com/zh/create/color-wheel/</a><br>除了创建自己的配色方案，可以参考他人的配色方案并且提供下载编辑的功能，十分好用。<br><img src="http://qiniu.huzerui.com/image/2017-10-15-color-explore.jpg" alt=" explore"></p>
<p>Kuler也有PS插件，使用方式参考这篇文章<a href="http://www.uisdc.com/adobe-kuler-vc" target="_blank" rel="external">教你使用最受欢迎的配色小工具Kuler</a>。</p>
<p>其他配色工具：<br><a href="http://www.eigenlogik.com/spectrum/mac" target="_blank" rel="external">Spectrum</a>：Mac + iOS 配色工具<br><a href="http://paletton.com/" target="_blank" rel="external">Paletton</a>：The Color Scheme Designer<br><a href="http://www.peise.net/tools/web/" target="_blank" rel="external">Color Scheme Designer</a>：高级在线配色工具</p>
<p>更多的配色工具参考：<a href="https://www.zhihu.com/question/23220933" target="_blank" rel="external">推荐配色工具</a>、<a href="http://www.uisdc.com/best-22-color-website" target="_blank" rel="external">国外最好的22个配色网站</a></p>
<h1 id="网页设计配色文章"><a href="#网页设计配色文章" class="headerlink" title="网页设计配色文章"></a>网页设计配色文章</h1><p>一、<a href="http://www.uisdc.com/6-rules-webdesign-color" target="_blank" rel="external">秒变配色高手！怎么都不会错的6条网页设计配色原则</a><br>二、<a href="http://tool.c7sky.com/webcolor/" target="_blank" rel="external">网页设计常用色彩搭配表</a><br>三、<a href="http://www.uisdc.com/color-psychology-web-design" target="_blank" rel="external">如何巧用色彩打造动人心弦的网页设计</a></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>设计</category>
      </categories>
      <tags>
        <tag>设计</tag>
        <tag>配色</tag>
      </tags>
  </entry>
  <entry>
    <title>css模块化</title>
    <url>/2016/06/06/css-module/</url>
    <content><![CDATA[<h1 id="模块化定义"><a href="#模块化定义" class="headerlink" title="模块化定义"></a>模块化定义</h1><ul>
<li>一系列相关联的结构组成的整体</li>
<li>带有一定语义，而非表现</li>
</ul>
<h1 id="怎么做"><a href="#怎么做" class="headerlink" title="怎么做"></a>怎么做</h1><ol>
<li>为模块分类命名（如m-,md-）</li>
<li>以一个主选择器开头（模块根节点）</li>
<li>使用以主选择器开头的后代选择器（模块子节点）</li>
</ol>
<h2 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h2><p> Html代码<br> <figure class="highlight html"><table><tr><td class="code"><pre><div class="line"> <span class="comment">&lt;!--Nav Module--&gt;</span></div><div class="line">	<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-nav"</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">ul</span>&gt;</span></div><div class="line">			<span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">"z-crt"</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>index<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;<span class="name">li</span>&gt;</span></div><div class="line">			<span class="tag">&lt;<span class="name">li</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>link1<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span>...</div><div class="line">		<span class="tag">&lt;/<span class="name">ul</span>&gt;</span></div><div class="line">	<span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="comment">&lt;!--End Nav Module--&gt;</span></div></pre></td></tr></table></figure></p>
<p> Css代码：<br> <figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.m-nav</span>&#123;&#125;   <span class="comment">/*模块容器*/</span></div><div class="line"><span class="selector-class">.m-nav</span> <span class="selector-tag">li</span>,<span class="selector-class">.m-nav</span> <span class="selector-tag">a</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-nav</span> <span class="selector-tag">li</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-nav</span> <span class="selector-tag">a</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-nav</span> <span class="selector-class">.z-crt</span> <span class="selector-tag">a</span>&#123;&#125;</div></pre></td></tr></table></figure></p>
<h1 id="模块扩展"><a href="#模块扩展" class="headerlink" title="模块扩展"></a>模块扩展</h1><p>原模块：Html<br> <figure class="highlight html"><table><tr><td class="code"><pre><div class="line"> <span class="comment">&lt;!--Nav Module--&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-nav"</span>&gt;</span></div><div class="line">	<span class="tag">&lt;<span class="name">ul</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">"z-crt"</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>index<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;<span class="name">li</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">li</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>link1<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span>...</div><div class="line">	<span class="tag">&lt;/<span class="name">ul</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="comment">&lt;!--End Nav Module--&gt;</span></div></pre></td></tr></table></figure></p>
<p>基于原模块的扩展模块Html：新增一个class类，名为m-nav-n，n为正整数依次递增<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"> <span class="comment">&lt;!--Nav-1 Module--&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-nav m-nav-1"</span>&gt;</span></div><div class="line">	<span class="tag">&lt;<span class="name">ul</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">"z-crt"</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>index<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;<span class="name">li</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">li</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"#"</span>&gt;</span>link1<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span>...</div><div class="line">	<span class="tag">&lt;/<span class="name">ul</span>&gt;</span></div><div class="line">	<span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">"btn"</span>&gt;</span>Login<span class="tag">&lt;/<span class="name">a</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="comment">&lt;!--End Nav-1 Module--&gt;</span></div></pre></td></tr></table></figure></p>
<p> 对模块和拓展模块设置css<br> <figure class="highlight"><table><tr><td class="code"><pre><div class="line"> /*导航模块*/</div><div class="line">.m-nav&#123;&#125;   /*模块容器*/</div><div class="line">.m-nav li,.m-nav a&#123;&#125;</div><div class="line">.m-nav li&#123;&#125;</div><div class="line">.m-nav a&#123;&#125;</div><div class="line">.m-nav .z-crt a&#123;&#125;    /*交互状态变化*/</div><div class="line">/*导航模块扩展*/</div><div class="line">.m-nav-1&#123;&#125;</div><div class="line">.m-nav-1 a&#123;border-radius:5px;   //设置扩展模块导航按钮有5px圆角&#125;</div><div class="line">.m-nav-1 btn&#123;&#125;</div></pre></td></tr></table></figure></p>
<h1 id="为什么要模块化"><a href="#为什么要模块化" class="headerlink" title="为什么要模块化"></a>为什么要模块化</h1><ul>
<li>利于多人协同开发</li>
<li>便于扩展和重用</li>
<li>可读性、可维护性好</li>
</ul>
<p>如：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">&lt;div class=&quot;m-slides&quot;&gt;</div><div class="line">	&lt;ol&gt;</div><div class="line">		&lt;li class=&quot;slide z-crt&quot;&gt;&lt;img src=&quot;&quot; alt=&quot;&quot;&gt;&lt;/li&gt;</div><div class="line">		&lt;li class=&quot;slide&quot;&gt;&lt;img src=&quot;&quot; alt=&quot;&quot;&gt;&lt;/li&gt;</div><div class="line">		&lt;li class=&quot;slide&quot;&gt;&lt;img src=&quot;&quot; alt=&quot;&quot;&gt;&lt;/li&gt;</div><div class="line">		&lt;li class=&quot;slide&quot;&gt;&lt;img src=&quot;&quot; alt=&quot;&quot;&gt;&lt;/li&gt;</div><div class="line">		&lt;span class=&quot;ctrl&quot;&gt;&lt;i class=&quot;z-crt&quot;&gt;&lt;/i&gt;&lt;i&gt;&lt;/i&gt;&lt;i&gt;&lt;/i&gt;&lt;i&gt;&lt;/i&gt;&lt;/span&gt;</div><div class="line">	&lt;/ol&gt;</div><div class="line">&lt;/div&gt;</div></pre></td></tr></table></figure></p>
<p>使用了外层div类名为m-slides时css结构清晰<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/*模块化样式*/</span></div><div class="line"><span class="selector-class">.m-slides</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-tag">slide</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-tag">slide</span> <span class="selector-tag">img</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-tag">li</span><span class="selector-class">.z-crt</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-class">.ctrl</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-class">.ctrl</span> <span class="selector-tag">i</span>&#123;&#125;</div><div class="line"><span class="selector-class">.m-slides</span> <span class="selector-class">.ctrl</span> <span class="selector-tag">i</span><span class="selector-class">.z-crt</span>&#123;&#125;</div></pre></td></tr></table></figure></p>
<p>去掉外层div时css<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/*未模块化样式*/</span></div><div class="line"><span class="selector-class">.slides</span>&#123;&#125;</div><div class="line"><span class="selector-class">.slide</span>&#123;&#125;</div><div class="line"><span class="selector-class">.slide</span> <span class="selector-tag">img</span>&#123;&#125;</div><div class="line"><span class="selector-tag">li</span><span class="selector-class">.z-crt</span>&#123;&#125;</div><div class="line"><span class="selector-class">.ctrl</span>&#123;&#125;</div><div class="line"><span class="selector-class">.ctrl</span> <span class="selector-tag">i</span>&#123;&#125;</div><div class="line"><span class="selector-tag">i</span><span class="selector-class">.z-crt</span>&#123;&#125;</div></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>css命名规范总结（分类命名）</title>
    <url>/2016/06/06/css-regulation-sum/</url>
    <content><![CDATA[<h2 id="使用类选择器，放弃ID选择器"><a href="#使用类选择器，放弃ID选择器" class="headerlink" title="使用类选择器，放弃ID选择器"></a>使用类选择器，放弃ID选择器</h2><p>ID在一个页面中的唯一性导致了如果以ID为选择器来写CSS，就无法重用。</p>
<h2 id="在样式命名中使用符号“-”"><a href="#在样式命名中使用符号“-”" class="headerlink" title="在样式命名中使用符号“-”"></a>在样式命名中使用符号“-”</h2><p>“-”在规范中并不表示连字符，而有两个含义：分类前缀分隔符、扩展分隔符</p>
<h2 id="分类的命名方法：使用单个字母-”-“为前缀"><a href="#分类的命名方法：使用单个字母-”-“为前缀" class="headerlink" title="分类的命名方法：使用单个字母+”-“为前缀"></a>分类的命名方法：使用单个字母+”-“为前缀</h2><p><strong>布局（grid）（.g-）；模块（module）（.m-）；元件（unit）（.u-）；功能（function）（.f-）；皮肤（skin）（.s-）；状态（.z-）</strong>。</p>
<ul>
<li>布局（grid）（.g-）：将页面分割为几个大块，通常有头部、主体、主栏、侧栏、尾部等。</li>
<li>模块（module）（.m-）：通常是一个语义化的可以重复使用的较大的整体。比如导航、登录、注册、各种列表、评论、搜索等。</li>
<li>元件（unit）（.u-）：通常是一个不可再分的较为小巧的个体，通常被重复用于各种模块中。比如按钮、输入框、loading、图标等。</li>
<li>功能（function）（.f-）：为方便一些常用样式的使用，我们将这些使用率较高的样式剥离出来，按需使用，通常这些选择器具有固定样式表现，比如清除浮动等。</li>
<li>皮肤（skin）（.s-）：如果你需要把皮肤型的样式抽离出来，通常为文字色、背景色（图）、边框色等，非换肤型网站通常只提取文字色！（非换肤型网站不可滥用此类）</li>
<li>状态（.z-）：为状态类样式加入前缀，统一标识，方便识别，她只能组合使用或作为后代出现</li>
</ul>
<p>示例：<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"g-header"</span>&gt;</span>...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"g-section"</span>&gt;</span></div><div class="line">	<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-article"</span>&gt;</span></div><div class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"header"</span>&gt;</span>...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">	<span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"g-footer"</span>&gt;</span>...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"></div><div class="line">/*布局样式*/</div><div class="line">.g-header&#123;color:black&#125;</div><div class="line">/*文章样式*/</div><div class="line">.m-article&#123;color:blue&#125;</div><div class="line">.m-article .header&#123;font-size:12px;&#125;</div></pre></td></tr></table></figure></p>
<p>注：</p>
<ul>
<li>在样式中的选择器总是要以上面前五类开头，然后在里面使用后代选择器。</li>
<li>如果这五类不能满足需求，可以另外定义一个或多个大类，但必须符合单个字母+”-“为前缀的命名规则，即 .x- 的格式。</li>
<li>特殊：.j-将被专用于JS获取节点，请勿使用.j-定义样式。</li>
</ul>
<h2 id="后代选择器命名"><a href="#后代选择器命名" class="headerlink" title="后代选择器命名"></a>后代选择器命名</h2><ul>
<li>约定不以单个字母+ “-“ 为前缀且长度大于等于2的类选择器为后代选择器，如：.item为m-list模块里的每一个项，.text为m-list模块里的文本部分：.m-list .item{}.m-list .text{}。</li>
<li>一个语义化的标签也可以是后代选择器，比如：.m-list li{}。</li>
<li>不允许单个字母的类选择器出现，原因详见下面的“模块和元件的后代选择器的扩展类”。</li>
<li>通过使用后代选择器的方法，你不需要考虑他的命名是否已被使用，因为他只在当前模块或元件中生效，同样的样式名可以在不同的模块或元件中重复使用，互不干扰；在多人协作或者分模块协作的时候效果尤为明显！</li>
</ul>
<p>后代选择器不需要完整表现结构树层级，尽量能短则短。</p>
<p>注：后代选择器不要在页面布局中使用，因为污染的可能性较大；</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* 这里的.itm和.cnt只在.m-list中有效 */</span></div><div class="line"><span class="selector-class">.m-list</span>&#123;<span class="attribute">margin</span>:<span class="number">0</span>;<span class="attribute">padding</span>:<span class="number">0</span>;&#125;</div><div class="line"><span class="selector-class">.m-list</span> <span class="selector-class">.itm</span>&#123;<span class="attribute">margin</span>:<span class="number">1px</span>;<span class="attribute">padding</span>:<span class="number">1px</span>;&#125;</div><div class="line"><span class="selector-class">.m-list</span> <span class="selector-class">.cnt</span>&#123;<span class="attribute">margin-left</span>:<span class="number">100px</span>;&#125;</div><div class="line"><span class="comment">/* 这里的.cnt和.num只在.m-page中有效 */</span></div><div class="line"><span class="selector-class">.m-page</span>&#123;<span class="attribute">height</span>:<span class="number">20px</span>;&#125;</div><div class="line"><span class="selector-class">.m-page</span> <span class="selector-class">.cnt</span>&#123;<span class="attribute">text-align</span>:center;&#125;</div><div class="line"><span class="selector-class">.m-page</span> <span class="selector-class">.num</span>&#123;<span class="attribute">border</span>:<span class="number">1px</span> solid <span class="number">#ddd</span>;&#125;</div></pre></td></tr></table></figure>
<h2 id="命名应简约而不失语义"><a href="#命名应简约而不失语义" class="headerlink" title="命名应简约而不失语义"></a>命名应简约而不失语义</h2><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* 反对：表现化的或没有语义的命名 */</span></div><div class="line"><span class="selector-class">.m-abc</span> <span class="selector-class">.green2</span>&#123;&#125;</div><div class="line"><span class="selector-class">.g-left2</span>&#123;&#125;</div><div class="line"><span class="comment">/* 推荐：使用有语义的简短的命名 */</span></div><div class="line"><span class="selector-class">.m-list</span> <span class="selector-class">.wrap2</span>&#123;&#125;</div><div class="line"><span class="selector-class">.g-side2</span>&#123;&#125;</div></pre></td></tr></table></figure>
<h2 id="相同语义的不同类命名"><a href="#相同语义的不同类命名" class="headerlink" title="相同语义的不同类命名"></a>相同语义的不同类命名</h2><p>直接加数字或字母区分即可<br>如：<strong>.m-list、.m-list2、.m-list3等，都是列表模块，但是是完全不一样的模块</strong>。</p>
<p>其他举例：.f-fw0、.f-fw1、.s-fc0、.s-fc1、.m-logo2、.m-logo3、u-btn、u-btn2等等。</p>
<h2 id="模块和元件的扩展类的命名方法"><a href="#模块和元件的扩展类的命名方法" class="headerlink" title="模块和元件的扩展类的命名方法"></a>模块和元件的扩展类的命名方法</h2><p>当A、B、C、…它们类型相同且外形相似区别不大，那么就以它们中出现率最高的做成基类，其他做成基类的扩展。</p>
<p>方法：<strong>+ “-” +数字或字母（如：.m-list的扩展类为.m-list-1、.m-list-2等）</strong>。</p>
<p>补充：基类自身可以独立使用（如：class=”m-list”即可），<strong>扩展类必须基于基类使用（如：class=”m-list m-list-2”）</strong>。</p>
<p><strong>如果你的扩展类是表示不同状态，那么你可以这样命名：</strong>u-btn-dis，u-btn-hov，m-box-sel，m-box-hov等等，然后像这样使用：class=”u-btn u-btn-dis”。</p>
<p>如果你的网站可以不兼容IE6等浏览器，那么你标识状态的方法也可以采取独立状态分类（.z-）方法：.u-btn.z-dis，.m-box.z-sel，然后像这样使用：class=”u-btn z-dis”。</p>
<h2 id="模块和元件的后代选择器的扩展类"><a href="#模块和元件的后代选择器的扩展类" class="headerlink" title="模块和元件的后代选择器的扩展类"></a>模块和元件的后代选择器的扩展类</h2><p>有时候模块内会有些类似的东西，如果你没有把它们做成元件和扩展，那么也可以使用后代选择器和扩展。</p>
<p>后代选择器：.m-login .btn{}。</p>
<p> <strong>后代选择器扩展：.m-login .btn-1{}，.m-login .btn-dis{} </strong>。</p>
<p>同样也可以采取独立状态分类（.z-）方法：.m-login .btn.z-dis{}，然后像这样使用：class=”btn z-dis”。</p>
<p>注：</p>
<ul>
<li>此方法用于类选择器，直接使用标签做为选择器的则不需要使用此命名方法。</li>
<li>为防止后代选择器的扩展类和大类命名规范冲突，后代选择器不允许使用单个字母。</li>
</ul>
<p>比如： <strong>.m-list .a{}是不允许的，因为当这个.a需要扩展的时候就会变成.a-bb，这样就和大类的命名规范冲突 </strong>。</p>
<h2 id="分组选择器有时可以代替扩展方法"><a href="#分组选择器有时可以代替扩展方法" class="headerlink" title="分组选择器有时可以代替扩展方法"></a>分组选择器有时可以代替扩展方法</h2><p>有时候虽然两个同类型的模块很相似，但是 <strong>你希望他们之间不要有依赖关系 </strong>，也就是说你不希望使用扩展的方法，那么你可以 <strong>通过合并选择器来设置共性的样式 </strong>。</p>
<p>使用本方法的前提是：相同类型、功能和外观都相似，写在同一片代码区域方便维护。<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* 两个元件共性的样式 */</span></div><div class="line"><span class="selector-class">.u-tip1</span>,<span class="selector-class">.u-tip2</span>&#123;&#125;</div><div class="line"><span class="selector-class">.u-tip1</span> <span class="selector-class">.itm</span>,<span class="selector-class">.u-tip2</span> <span class="selector-class">.itm</span>&#123;&#125;</div><div class="line"><span class="comment">/* 在分别是两个元件各自的样式 */</span></div><div class="line"><span class="comment">/* tip1 */</span></div><div class="line"><span class="selector-class">.u-tip1</span>&#123;&#125;</div><div class="line"><span class="selector-class">.u-tip1</span> <span class="selector-class">.itm</span>&#123;&#125;</div><div class="line"><span class="comment">/* tip2 */</span></div><div class="line"><span class="selector-class">.u-tip2</span>&#123;&#125;</div><div class="line"><span class="selector-class">.u-tip2</span> <span class="selector-class">.itm</span>&#123;&#125;</div></pre></td></tr></table></figure></p>
<h2 id="防止污染和被污染"><a href="#防止污染和被污染" class="headerlink" title="防止污染和被污染"></a>防止污染和被污染</h2><p>当模块或元件之间互相嵌套，且使用了相同的标签选择器或其他后代选择器，那么里面的选择器就会被外面相同的选择器所影响。</p>
<p>所以， <strong>如果你的模块或元件可能嵌套或被嵌套于其他模块或元件，那么要慎用标签选择器 </strong>，必要时采用类选择器，并注意命名方式，可以采用 <strong>.m-layer .layerxxx </strong> 、  <strong>.m-list2 .list2xxx </strong>的形式来降低后代选择器的污染性。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>完美的犯罪，触目惊心的微笑</title>
    <url>/2016/03/13/film-review-detective-china-tang/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-3-13-film-tanrenjie-poster.jpg" alt="唐人街探案"><br>在当今百花齐放的电影片制作时代，有的人喜欢喜剧片，有的人喜欢科幻片，有的人喜欢充满童真的动画片，更有不少人喜欢恐怖片，可谓众口难调。但好电影能迎合绝大多数人的口味，今天看的正是这样一部去年末上映的电影，弥补了没去影院观看的遗憾。之说以写下这迟来的感受，因为我确实被震撼到了…</p>
<p>怀着看喜剧片的心情看《唐人街探案》，正如当初看《泰囧》《港囧》的感觉，本以为笑笑就过了，但渐渐发现有种看恐怖片的味道。不得不承认，将这一元素表现的淋漓尽致的是小女孩最后那一抹微笑。作为外行人来看，我觉得那是影帝级的水平，因为当背景音乐悬紧，她嘴角微扬，低沉地说出那一声“恶人是不是改这么笑”的时候，我浑身鸡皮疙瘩都起来了，似有一种闯进鬼的房间稍感不适连忙说出“对不起我走错房间”的感觉。如果你看过《跑男》，相信你对古城堡继承者那集会有印象，那时候邓超作为继承者即将可以通过镜子知道谁是外族人时，外族人Alla打开房门对邓超说了一句“Can I trust you？”后来邓超回忆，“那种眼神是我一生中都难忘了。没错《唐人街探案》小女孩的眼神和微笑正有那种至生难忘的感觉，幸好是白天看的（还是“吓尿了”），否则这一夜可谓彻夜难眠了。<br>s<br><img src="http://qiniu.huzerui.com/image/2016-3-13-film-tanrenjie-screenshot.jpg" alt="小女孩的微笑"></p>
<hr>
<p>喜剧片我向来不感冒，总觉得这样的片总是一笑而过没有深度；恐怖片是不敢看，可能因为我想象力太丰富会留下后遗症；侦探片偶尔看下，却不喜欢太过拘泥的情节。《唐人街探案》将三者中部分微妙元素结合的恰到好处，除了所有电影有的起承转合剧情，也不少那些温馨抑或引人深思的元素。随着真相被一层层剥离，人性也一步步立体，《唐人街探案》在喜剧在笑声的背后揭露的其实是关于善恶、关于真相的大命题。</p>
<p>“一阴一阳之谓道也，继之者善业，成之者性也。”—《周易·系辞》<br>道家认为事物都有阴阳两个方面、两种力量，相反相成，相互推移，不可偏废，构成事物的本性及其运动的法则。无论自然、人事，都表现此道。对应小女孩最后那句话，“个体生命不同，但世间善恶总量不变，每个人都扮演着各自的角色，有的是善有的是恶。”</p>
<p>在这个社会运转体系中，善和恶作为一种人的附属属性在发生不断地转换，但我认为一心从善的人应该占据着善的绝大部分比重，因为世界的美好正是由这些一心从善的人构筑的。当然再美好的世界都会有罪恶的人，有的遇到感化洗心革面，有的则一恶到底。尽管恶非本性，因为我认可“人之初，性本善”，罪恶的人总是某种极端环境下的产物，其生成转换的数量在于人的价值观正确与否和心理最后一道防线何时被打破。价值观积极向上的，心理强大的总能抵御或想法相处环境带来的负面因素。罪恶的人思想受环境慢慢影响心中慢慢发生质变走上犯罪道路，希望以罪恶的手段为自己谋求生存的空间和心灵的一片净土。所以，环境对人的影响太重要了，“勿以恶小而为之，勿以善小而不为”，做个积极向上的人，尽可能接受健康有益的信息和事物，于人于己都是莫大的善举，我想这就是电影能给我带来的正能量吧。</p>
<p>“排除所有的不可能，剩下的那个在不可思议，都是事实的真相”，我不希望社会中的“真相”会像电影中被埋没，只希望这样的“真相”越来越少。</p>
]]></content>
      <categories>
        <category>电影</category>
      </categories>
      <tags>
        <tag>电影</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS3变换及CSS3动画</title>
    <url>/2016/06/16/css3-transform-animation/</url>
    <content><![CDATA[<h1 id="CSS3变换动画"><a href="#CSS3变换动画" class="headerlink" title="CSS3变换动画"></a>CSS3变换动画</h1><h2 id="指定三维视角"><a href="#指定三维视角" class="headerlink" title="指定三维视角"></a>指定三维视角</h2><p>perspective 属性定义 3D 元素距视图的距离，以像素计。<br>当为元素定义 perspective 属性时，其子元素会获得透视效果，而不是元素本身。</p>
<h2 id="三维变形"><a href="#三维变形" class="headerlink" title="三维变形"></a>三维变形</h2><p>三维变形的变形方式：四种方法<br>旋转——缩放——平移——扭曲——指定变形基点</p>
<h2 id="旋转"><a href="#旋转" class="headerlink" title="旋转"></a>旋转</h2><p>transform:rotate(45deg);<br>该语句使div元素顺时针旋转45度。</p>
<h2 id="缩放"><a href="#缩放" class="headerlink" title="缩放"></a>缩放</h2><p>transform:scale(0.8,1);<br>使用缩放的方法实现文字或图像的缩放效果，在参数中指定缩放倍率。该语句使用scale方法使该元素在水平方向上缩小了20%，垂直方向上不缩放。</p>
<h2 id="平移"><a href="#平移" class="headerlink" title="平移"></a>平移</h2><p>translate(50px,50px);<br>使用translate方法来将文字或图像在水平方向和垂直方向上分别垂直移动50像素。</p>
<h2 id="扭曲"><a href="#扭曲" class="headerlink" title="扭曲"></a>扭曲</h2><p>transform:skew(30deg,0deg);<br>该实例通过skew方法把元素水平方向上倾斜30度，处置方向保持不变。</p>
<h2 id="指定变形基点"><a href="#指定变形基点" class="headerlink" title="指定变形基点"></a>指定变形基点</h2><p>在使用transform方法进行文字或图像的变形时，是以元素的中心点为基准点进行的。使用transform-orign属性，可以改变变形的基准点。<br>transorm-origin:left bottom;<br>left和bottom是基准点在元素水平方向和垂直方向上的位置。</p>
<h2 id="形成过渡动画"><a href="#形成过渡动画" class="headerlink" title="形成过渡动画"></a>形成过渡动画</h2><p>元素在定义了变化终点状态之后，在元素本身设置transition即可形成过渡动画。</p>
<h1 id="Animation动画"><a href="#Animation动画" class="headerlink" title="Animation动画"></a>Animation动画</h1><p>animation属性值：</p>
<table>
<thead>
<tr>
<th>属性</th>
<th style="text-align:center">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>@keyframes</td>
<td style="text-align:center">定义动画名称</td>
</tr>
<tr>
<td>animation</td>
<td style="text-align:center">所有动画属性的简写属性，除了animation-play-state属性。</td>
</tr>
<tr>
<td>animation-name</td>
<td style="text-align:center">规定 @keyframes 动画的名称。</td>
</tr>
<tr>
<td>animation-duration</td>
<td style="text-align:center">规定动画完成一个周期所花费的时间。默认是0s。</td>
</tr>
<tr>
<td>animation-timing-function</td>
<td style="text-align:center">规定动画的速度曲线。默认是 “ease”。</td>
</tr>
<tr>
<td>animation</td>
<td style="text-align:center">规定动画何时开始。默认是0s。</td>
</tr>
<tr>
<td>animation-iteration-count</td>
<td style="text-align:center">规定动画被播放的次数。默认是1。</td>
</tr>
<tr>
<td>animation-direction</td>
<td style="text-align:center">规定动画是否在下一周期逆向地播放。默认是 “normal” 。逆向alternate</td>
</tr>
<tr>
<td>animation-play-state</td>
<td style="text-align:center">规定动画是否正在运行或暂停。默认是 “running”，暂停时pause</td>
</tr>
<tr>
<td>animation-fill-mode</td>
<td style="text-align:center">规定对象动画时间之外的状态。forwards：设置对象状态为动画结束时的状态；backwards：设置对象状态为动画开始时的状态；both：设置对象状态为动画开始或结束的状态</td>
</tr>
</tbody>
</table>
<h1 id="两者区别"><a href="#两者区别" class="headerlink" title="两者区别"></a>两者区别</h1><p>1、变换动画不能自行触发，通过hover等动作或结合JS进行触发。anmiation可以自行运行。<br>2、变换动画可控性较弱，只能指定起始状态和结束状态，而animation可以定义多个关键帧。<br>3、变换动画在运行结束之后，需要回到初始状态<br>4、变换动画的作用在于平滑的改变CSS样式<br>5、animation多两个参数，循环和动画的方式。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>css3</tag>
      </tags>
  </entry>
  <entry>
    <title>游戏相伴的青春</title>
    <url>/2016/04/29/game-life/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-04-29-game-life-1.jpg" alt="游戏"><br>“游戏”，这个常被21世纪父母所厌恶至极的让儿女玩物丧志的名词，今天竟有点想为它沉冤昭雪的冲动。原因也很简单，除了它在我人生中抑或说读书生涯占据了巨大的比重让我不得不提下之外，更因为在互联网极度繁华的现在，它似乎有种走出阴霾渐渐被人们所理解的迹象，电竞游戏门类更产生了一个庞大的生态链，让人们觉得<strong>玩游戏也可以不是“颓废”、“懒”、“丧志”的代名词，它也可以成为赚钱、娱乐的一种手段</strong>。</p>
<p>当然，今天主题并非在游戏的发展抑或现在游戏产业的革命，只是自己热衷于记录跟自己人生息息相关的东西，所以，如果你是<strong>80后抑或90年初</strong>的孩纸，你也许能在文中找到与我类似的经历和产生共鸣，<strong>因为那是属于一个年代的乐趣</strong>。</p>
<p><img src="http://qiniu.huzerui.com/image/2016-04-29-game-life-2.jpg" alt="小霸王时代"></p>
<p>“魂斗罗”、“超级玛丽”、“雪人兄弟”、“坦克大战”等这些个在部分人眼里耳熟能详的名词，真的是属于小霸王时代的年轻人的乐趣。</p>
<p>在那个电脑十分不普及的年代，玩游戏机成了年轻人的fashion。我记得那时候我才6岁，家里买来一台小霸王，接触到第一款游戏好像就是魂斗罗，兄弟姐妹四人两两轮流着玩。</p>
<p>后来觉得一款游戏乏味了，就开始不断地买游戏带，让我印象最深的就是超级玛丽了。听着金币那个“滴滴”的声音和最终跳旗的优越感，满满的都是回忆。虽然现在出了电脑版的和手机版的，但在这个年代似乎感觉不到儿时的乐趣了。</p>
<p>但是，我的游戏生涯远远不止于此。我的家庭属于接触PC比较早的，在我大概8岁（00年）的时候，我爸购买了一台PC电脑，不知为何我妈似乎不排斥游戏，从外面购买了一些游戏光碟。</p>
<p>见到光碟的时候，我们兄弟姐妹几个都乐坏了，要知道在那个没有互联网的年代，玩PC游戏简直是儿童的无上乐趣，当然这跟兄弟或者小伙伴一起玩的乐趣是分不开的。</p>
<p>有个游戏印象很深，就是《大富翁4》，成为地产大亨是游戏的梦想，也是生活的梦想。嘎嘎，好像比较难实现，先挣他一个亿再说。</p>
<p><strong>所以直到现在，我的母亲还有一层只有我自己知道的身份——我的游戏启蒙导师</strong></p>
<p><img src="http://qiniu.huzerui.com/image/2016-04-29-game-life-3.jpg" alt="记忆中的游戏"></p>
<ul>
<li><em>泡泡堂：第一款让我小学5年级进入网吧的游戏*</em></li>
<li><strong>跑跑卡丁车：一款让我与初中好友乐在其中的游戏</strong></li>
<li>QQ幻想：第一款rpg游戏</li>
<li>CF：灵巧类游戏</li>
<li>Dota：考验智慧和反应的竞技类游戏</li>
</ul>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>毕业一年，如期而至</title>
    <url>/2016/09/08/graduate-for-one-year/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-09-08-graduration-poster.jpg" alt="poster"></p>
<iframe frameborder="0" style="max-width: 100%;max-height: 498px;" width="640" height="498" src="http://v.qq.com/iframe/player.html?vid=m03268wqppq&tiny=0&auto=0" allowfullscreen></iframe>

]]></content>
      <tags>
        <tag>大学</tag>
      </tags>
  </entry>
  <entry>
    <title>Sass使用指南</title>
    <url>/2016/03/05/grammar-of-sass/</url>
    <content><![CDATA[<p>随着对CSS的用法了解比较熟悉，觉得有必要学习一下CSS预编译的语言。有一种是Less，另一种是Sass。科普了一下，两种语言的主要不同是他们的实现方式，Less是基于javascript在客户端处理的，而Sass是基于Ruby在服务端处理的。但它们之间也有共同点</p>
<ul>
<li><p>混入(Mixins)——class中的class；</p>
</li>
<li><p>参数混入——可以传递参数的class，就像函数一样；</p>
</li>
<li><p>嵌套规则——Class中嵌套class，从而减少重复的代码；</p>
</li>
<li><p>运算——CSS中用上数学；</p>
</li>
<li><p>颜色功能——可以编辑颜色；</p>
</li>
<li><p>名字空间(namespace)——分组样式，从而可以被调用；</p>
</li>
<li><p>作用域——局部修改样式；</p>
</li>
<li><p>JavaScript 赋值——在CSS中使用JavaScript表达式赋值。</p>
</li>
</ul>
<p><strong><em>这篇博客主要记录一下Sass的用法</em></strong></p>
<p>Sass用法指南</p>
<h1 id="什么是Sass"><a href="#什么是Sass" class="headerlink" title="什么是Sass"></a>什么是Sass</h1><p>SASS是一种CSS的开发工具，提供了许多便利的写法，大大节省了设计者的时间，使得CSS的开发，变得简单和可维护。<br>本文总结了SASS的主要用法。我的目标是，有了这篇文章，日常的一般使用就不需要去看官方文档了。</p>
<h1 id="安装和使用"><a href="#安装和使用" class="headerlink" title="安装和使用"></a>安装和使用</h1><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>SASS是Ruby语言写的，但是两者的语法没有关系。不懂Ruby，照样使用。只是必须先安装Ruby，然后再安装SASS。<br>假定你已经安装好了Ruby，接着在命令行输入下面的命令：</p>
<figure class="highlight ruby"><table><tr><td class="code"><pre><div class="line">gem install sass</div></pre></td></tr></table></figure>
<p>然后，就可以使用了。</p>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>SASS文件就是普通的文本文件，里面可以直接使用CSS语法。文件后缀名是.scss，意思为Sassy CSS。<br>下面的命令，可以在屏幕上显示.scss文件转化的css代码。（假设文件名为test。）<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">sass test.scss</div></pre></td></tr></table></figure></p>
<p>如果要将显示结果保存成文件，后面再跟一个.css文件名。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">sass test.scss test.css</div></pre></td></tr></table></figure></p>
<p>SASS提供四个编译风格的选项：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">* nested：嵌套缩进的css代码，它是默认值。</div><div class="line">* expanded：没有缩进的、扩展的css代码。</div><div class="line">* compact：简洁格式的css代码。</div><div class="line">* compressed：压缩后的css代码。</div></pre></td></tr></table></figure></p>
<p>生产环境当中，一般使用最后一个选项。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">sass --style compressed test.sass test.css</div></pre></td></tr></table></figure></p>
<p>你也可以让SASS监听某个文件或目录，一旦源文件有变动，就自动生成编译后的版本。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">// watch a file</div><div class="line">sass --watch input.scss:output.css</div><div class="line">// watch a directory</div><div class="line">sass --watch app/sass:public/stylesheets</div></pre></td></tr></table></figure></p>
<p>SASS的官方网站，提供了一个在线转换器。你可以在那里，试运行下面的各种例子。</p>
<h1 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h1><h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><p>SASS允许使用变量，所有变量以$开头。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$blue : #1875e7;　</div><div class="line">div &#123;</div><div class="line">  color : $blue;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>如果变量需要镶嵌在字符串之中，就必须需要写在#{}之中。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$side : left;</div><div class="line">.rounded &#123;</div><div class="line">  border-#&#123;$side&#125;-radius: 5px;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="计算功能"><a href="#计算功能" class="headerlink" title="计算功能"></a>计算功能</h2><p>SASS允许在代码中使用算式：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">body &#123;</div><div class="line">  margin: (14px/2);</div><div class="line">  top: 50px + 100px;</div><div class="line">  right: $var * 10%;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="嵌套"><a href="#嵌套" class="headerlink" title="嵌套"></a>嵌套</h2><p>SASS允许选择器嵌套。比如，下面的CSS代码：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">div h1 &#123;</div><div class="line">  color : red;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>可以写成：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">div &#123;</div><div class="line">  hi &#123;</div><div class="line">　　   color:red;</div><div class="line">　　 &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>属性也可以嵌套，比如border-color属性，可以写成：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">p &#123;</div><div class="line">  border: &#123;</div><div class="line">　  color: red;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>注意，border后面必须加上冒号。<br>在嵌套的代码块内，可以使用&amp;引用父元素。比如a:hover伪类，可以写成：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">a &#123;</div><div class="line">  &amp;:hover &#123; color: #ffb3ff; &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h2><p>SASS共有两种注释风格。<br>标准的CSS注释 /<em> comment </em>/ ，会保留到编译后的文件。<br>单行注释 // comment，只保留在SASS源文件中，编译后被省略。<br>在/*后面加一个感叹号，表示这是”重要注释”。即使是压缩模式编译，也会保留这行注释，通常可以用于声明版权信息。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">/*! </div><div class="line">  重要注释！</div><div class="line">*/</div></pre></td></tr></table></figure></p>
<h1 id="代码的重用"><a href="#代码的重用" class="headerlink" title="代码的重用"></a>代码的重用</h1><h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><p>SASS允许一个选择器，继承另一个选择器。比如，现有class1：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">.class1 &#123;</div><div class="line">  border: 1px solid #ddd;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>class2要继承class1，就要使用@extend命令：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">class2 &#123;</div><div class="line">  @extend .class1;</div><div class="line">  font-size:120%;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="Mixin"><a href="#Mixin" class="headerlink" title="Mixin"></a>Mixin</h2><p>Mixin有点像C语言的宏（macro），是可以重用的代码块。<br>使用@mixin命令，定义一个代码块。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@mixin left &#123;</div><div class="line">  float: left;</div><div class="line">  margin-left: 10px;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用@include命令，调用这个mixin。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">div &#123;</div><div class="line">  @include left;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>mixin的强大之处，在于可以指定参数和缺省值。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@mixin left($value: 10px) &#123;</div><div class="line">  float: left;</div><div class="line">  margin-right: $value;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用的时候，根据需要加入参数：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">div &#123;</div><div class="line">  @include left(20px);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>下面是一个mixin的实例，用来生成浏览器前缀。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@mixin rounded($vert, $horz, $radius: 10px) &#123;</div><div class="line">  border-#&#123;$vert&#125;-#&#123;$horz&#125;-radius: $radius;</div><div class="line">  -moz-border-radius-#&#123;$vert&#125;#&#123;$horz&#125;: $radius;</div><div class="line">  -webkit-border-#&#123;$vert&#125;-#&#123;$horz&#125;-radius: $radius;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用的时候，可以像下面这样调用：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">#navbar li &#123; @include rounded(top, left); &#125;</div><div class="line">#footer &#123; @include rounded(top, left, 5px); &#125;</div></pre></td></tr></table></figure></p>
<h2 id="颜色函数"><a href="#颜色函数" class="headerlink" title="颜色函数"></a>颜色函数</h2><p>SASS提供了一些内置的颜色函数，以便生成系列颜色。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">lighten(#cc3, 10%) // #d6d65c</div><div class="line">darken(#cc3, 10%) // #a3a329</div><div class="line">grayscale(#cc3) // #808080</div><div class="line">complement(#cc3) // #33c</div></pre></td></tr></table></figure></p>
<h2 id="插入文件"><a href="#插入文件" class="headerlink" title="插入文件"></a>插入文件</h2><p>@import命令，用来插入外部文件。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@import &quot;path/filename.scss&quot;;</div></pre></td></tr></table></figure></p>
<p>如果插入的是.css文件，则等同于css的import命令。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@import &quot;foo.css&quot;;</div></pre></td></tr></table></figure></p>
<h1 id="高级用法"><a href="#高级用法" class="headerlink" title="高级用法"></a>高级用法</h1><h2 id="条件语句"><a href="#条件语句" class="headerlink" title="条件语句"></a>条件语句</h2><p>@if可以用来判断：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">p &#123;</div><div class="line">  @if 1 + 1 == 2 &#123; border: 1px solid; &#125;</div><div class="line">  @if 5 &lt; 3 &#123; border: 2px dotted; &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>配套的还有@else命令：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">=@if lightness($color) &gt; 30% &#123;</div><div class="line">  background-color: #000;</div><div class="line">=&#125; @else &#123;</div><div class="line">  background-color: #fff;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="循环语句"><a href="#循环语句" class="headerlink" title="循环语句"></a>循环语句</h2><p>SASS支持for循环：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@for $i from 1 to 10 &#123;</div><div class="line">  .border-#&#123;$i&#125; &#123;</div><div class="line">　  border: #&#123;$i&#125;px solid blue;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>也支持while循环：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$i: 6;</div><div class="line">@while $i &gt; 0 &#123;</div><div class="line">  .item-#&#123;$i&#125; &#123; width: 2em * $i; &#125;</div><div class="line">  $i: $i - 2;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>each命令，作用与for类似：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@each $member in a, b, c, d &#123;</div><div class="line">  .#&#123;$member&#125; &#123;</div><div class="line">　  background-image: url(&quot;/image/#&#123;$member&#125;.jpg&quot;);</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="自定义函数"><a href="#自定义函数" class="headerlink" title="自定义函数"></a>自定义函数</h2><p>SASS允许用户编写自己的函数。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@function double($n) &#123;</div><div class="line">  @return $n * 2;</div><div class="line">&#125;</div><div class="line">#sidebar &#123;</div><div class="line">  width: double(5px);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>css</tag>
        <tag>sass</tag>
      </tags>
  </entry>
  <entry>
    <title>Gulp前端自动化工具笔记</title>
    <url>/2016/11/07/gulp-learning-note/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-11-7-gulp-poster.jpg" alt="gulp"><br>某个深夜独自在房间深造，在不断键盘敲击的“Alt+Tab”和“F5”许久，我开始疲惫了，于是从冰箱抡起一杯可乐。畅饮之余，去Google寻求仙人指路，仙人找到了nodejs为我指点了迷津，并递给我一杯可乐和吸管，原来“她”就在这里，她的名字叫“Gulp”。</p>
<p>她虽然只是一杯插着吸管的饮料，可却能在烈日炎炎的夏天舒缓你焦灼的身心，在寒冷的冬天给你雪中送炭似的温暖，给你不一样的编码体验。</p>
<h1 id="拾起一杯Gulp"><a href="#拾起一杯Gulp" class="headerlink" title="拾起一杯Gulp"></a>拾起一杯Gulp</h1><p>gulpjs是一个前端构建工具，API简单，基于<strong>任务流</strong>，无需像Grunt复杂的配置。</p>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>在安装了node.js的基础上使用命令行或git即可安装。<br>全局安装：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$ npm install --global gulp</div></pre></td></tr></table></figure></p>
<p>作为项目的<strong>开发依赖安装（-dev）</strong>，如果作为<strong>项目依赖，则只需（–save）</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$ npm install --save-dev gulp</div></pre></td></tr></table></figure></p>
<h2 id="根目录创建gulpfile-js"><a href="#根目录创建gulpfile-js" class="headerlink" title="根目录创建gulpfile.js"></a>根目录创建gulpfile.js</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'default'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">  <span class="comment">// 将你的默认的任务代码放在这</span></div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h2 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h2><p>如果省略了task参数，则gulp === gulp default<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp [task]?    <span class="comment">//  []中存放参数，？参考js正则，代表可有可无</span></div></pre></td></tr></table></figure></p>
<h1 id="畅饮Gulp"><a href="#畅饮Gulp" class="headerlink" title="畅饮Gulp"></a>畅饮Gulp</h1><p>在了解的Gulp的一些插件和基本使用方法之后，我开始构建自己的gulpfile.js，这个流程可以实现如下功能。</p>
<ul>
<li>实时监听文件的变化，一旦保存浏览器自动刷新。</li>
<li>自动编译scss文件</li>
<li>合并样式文件、脚本文件</li>
<li>压缩样式文件、脚本文件、图片</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">/*============================引入插件===============================*/</span></div><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> plugins = <span class="built_in">require</span>(<span class="string">'gulp-load-plugins'</span>)(&#123;</div><div class="line">	<span class="attr">rename</span>: &#123;</div><div class="line">		<span class="string">'gulp-ruby-sass'</span>: <span class="string">'sass'</span>,</div><div class="line">		<span class="string">'gulp-clean-css'</span>: <span class="string">'cleancss'</span></div><div class="line">	&#125;</div><div class="line">&#125;);</div><div class="line"><span class="keyword">var</span> browserSync = <span class="built_in">require</span>(<span class="string">'browser-sync'</span>).create();</div><div class="line"><span class="keyword">var</span> reload = browserSync.reload;</div><div class="line"></div><div class="line"><span class="comment">/*=============================发布任务==============================*/</span></div><div class="line">gulp.task(<span class="string">'html'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> gulp.src(<span class="string">'src/*.html'</span>)</div><div class="line">		.pipe(gulp.dest(<span class="string">'dist/'</span>));</div><div class="line">&#125;);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'css'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="comment">// var cssFilter = plugins.filter(['src/**/*.css', '!src/css/vendor'], &#123;restore: true&#125;);</span></div><div class="line">	<span class="keyword">return</span> gulp.src(<span class="string">'src/css/**/*.css'</span>)</div><div class="line">		.pipe(plugins.cleancss())</div><div class="line">		.pipe(gulp.dest(<span class="string">'dist/css/'</span>));</div><div class="line">&#125;);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'js'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="comment">// var jsFilter = plugins.filter(['src/**/*.js', '!src/js/vendor'], &#123;restore: true&#125;);</span></div><div class="line">	<span class="keyword">return</span> gulp.src(<span class="string">'src/**/*.js'</span>)</div><div class="line">		.pipe(plugins.uglify())</div><div class="line">		.pipe(gulp.dest(<span class="string">'dist/'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'img'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	gulp.src(<span class="string">'src/img/*'</span>)</div><div class="line">		.pipe(plugins.imagemin())</div><div class="line">		.pipe(gulp.dest(<span class="string">'dist/img'</span>));</div><div class="line">&#125;);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'dist'</span>,[<span class="string">'html'</span>, <span class="string">'css'</span>, <span class="string">'js'</span> , <span class="string">'img'</span>], <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> gulp.src([<span class="string">'src/res'</span>, <span class="string">'src/mocks'</span>])</div><div class="line">		.pipe(gulp.dest(<span class="string">'dist/'</span>));</div><div class="line"></div><div class="line">&#125;);</div><div class="line"><span class="comment">/*=============================合并任务==============================*/</span></div><div class="line"></div><div class="line">gulp.task(<span class="string">'bundle'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">var</span> vendor = &#123;</div><div class="line">		<span class="attr">css</span>: [<span class="string">'src/css/vendor/jquery.fullpage.css'</span>],</div><div class="line">		<span class="attr">js</span>: [<span class="string">'src/js/vendor/jquery.js'</span>,<span class="string">'src/js/vendor/jquery.fullpage.js'</span>]</div><div class="line">	&#125;;</div><div class="line">	<span class="comment">// 合并库css</span></div><div class="line">	gulp.src(vendor.css)</div><div class="line">		.pipe(plugins.concat(<span class="string">'bundle.css'</span>))</div><div class="line">		.pipe(gulp.dest(<span class="string">'src/css/vendor/'</span>));</div><div class="line"></div><div class="line">	<span class="comment">// 合并库js</span></div><div class="line">	gulp.src(vendor.js)</div><div class="line">		.pipe(plugins.concat(<span class="string">'bundle.js'</span>))</div><div class="line">		.pipe(gulp.dest(<span class="string">'src/js/vendor/'</span>));</div><div class="line"></div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">/*=============================服务器任务==============================*/</span></div><div class="line"></div><div class="line"><span class="comment">// 静态服务器 + 监听 scss/html 文件</span></div><div class="line">gulp.task(<span class="string">'server'</span>, [<span class="string">'sass'</span>,<span class="string">'bundle'</span>], <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    browserSync.init(&#123;</div><div class="line">        <span class="attr">server</span>: <span class="string">'./src'</span></div><div class="line">    &#125;);</div><div class="line">    gulp.watch(<span class="string">'src/scss/**/*.scss'</span>, [<span class="string">'sass'</span>]);</div><div class="line">    gulp.watch(<span class="string">'src/**/*.html'</span>, [<span class="string">'html'</span>]).on(<span class="string">'change'</span>, reload);</div><div class="line">    gulp.watch(<span class="string">'src/js/**/*.js'</span>, [<span class="string">'js'</span>]).on(<span class="string">'change'</span>, reload);</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">/*=============================编译任务==============================*/</span></div><div class="line">gulp.task(<span class="string">'sass'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">	<span class="comment">// 编译库样式文件</span></div><div class="line">	plugins.sass(<span class="string">'src/scss/vendor/*.scss'</span>,&#123;<span class="attr">sourcemap</span>: <span class="literal">true</span>&#125;)</div><div class="line">    	.on(<span class="string">'error'</span>, plugins.sass.logError)</div><div class="line">    	.pipe(plugins.sourcemaps.write())</div><div class="line">        .pipe(gulp.dest(<span class="string">'src/css/vendor/'</span>))</div><div class="line">        .pipe(reload(&#123;<span class="attr">stream</span>: <span class="literal">true</span>&#125;));</div><div class="line">    <span class="comment">// 编译项目样式文件    </span></div><div class="line">    plugins.sass(<span class="string">'src/scss/main.scss'</span>,&#123;<span class="attr">sourcemap</span>: <span class="literal">true</span>&#125;)</div><div class="line">    	.on(<span class="string">'error'</span>, plugins.sass.logError)</div><div class="line">    	.pipe(plugins.sourcemaps.write())</div><div class="line">        .pipe(gulp.dest(<span class="string">'src/css/'</span>))</div><div class="line">        .pipe(reload(&#123;<span class="attr">stream</span>: <span class="literal">true</span>&#125;));</div><div class="line">    <span class="keyword">return</span> gulp;</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">/*=============================默认任务==============================*/</span></div><div class="line">gulp.task(<span class="string">'default'</span>, [<span class="string">'dist'</span>]);</div></pre></td></tr></table></figure>
<h2 id="插件介绍"><a href="#插件介绍" class="headerlink" title="插件介绍"></a>插件介绍</h2><p><strong>browserSync：</strong>浏览器实时相应文件更改并进行重新加载<br><strong>gulp-load-plugins：</strong>gulp插件加载工具，可对gulp插件进行重命名。当使用插件过多时可以有效管理和组织插件。<br><strong>gulp-ruby-sass：</strong> sass编译工具，同时生成sourcemap方便调试<br><strong>gulp-clean-css：</strong> 压缩css工具<br><strong>gulp-uglify：</strong> 压缩js工具<br><strong>gulp-concat:</strong>css、js合并工具<br><strong>gulp-imagemin：</strong> 图片压缩工具</p>
<h2 id="流程说明"><a href="#流程说明" class="headerlink" title="流程说明"></a>流程说明</h2><ul>
<li>在项目开发阶段，只存在src目录，所有开发均在src目录进行，直到项目开发完成，运行gulp命令则生成dist发布目录。</li>
<li>项目开发过程中执行gulp server命令，即可实施监听文件变动从而实现浏览器自动刷新。</li>
</ul>
<p><strong>gulp server运行时，一旦文件重新保存,则触发以下任务：</strong></p>
<ul>
<li>scss-vendor文件夹中的样式文件会被编译并打包成bundle.css并输出到css-vendor</li>
<li>js-vendor中的库文件会被打包成bundle.js并输出到js-vendor</li>
<li>scss目录下同级的scss文件将被合并到main.scss文件中并被编译成main.css打包到css目录下面。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">-root</div><div class="line">	-dist <span class="comment">//发布目录，执行gulp默认任务生成</span></div><div class="line">	-src</div><div class="line">		-css</div><div class="line">			vendor</div><div class="line">				...</div><div class="line">			main.css   </div><div class="line">		-img </div><div class="line">		-js </div><div class="line">			vendor</div><div class="line">			main.js</div><div class="line">		-scss </div><div class="line">			vendor</div><div class="line">				...</div><div class="line">			main.scss </div><div class="line">			normalize.scss</div><div class="line">		index.html</div><div class="line">		...</div></pre></td></tr></table></figure>
<h1 id="品味Gulp"><a href="#品味Gulp" class="headerlink" title="品味Gulp"></a>品味Gulp</h1><h2 id="Gulp-API"><a href="#Gulp-API" class="headerlink" title="Gulp-API"></a>Gulp-API</h2><p>以下为API介绍，[]中的参数为可选，options参数不太常用，故不作详细介绍。</p>
<h3 id="gulp-task-name-deps-fn"><a href="#gulp-task-name-deps-fn" class="headerlink" title="gulp.task(name[, deps], fn)"></a>gulp.task(name[, deps], fn)</h3><p>定义一个使用 Orchestrator 实现的任务（task）。<br>name: 任务的名字，如果你需要在命令行中运行你的某些任务，那么，请不要在名字中使用空格。<br>deps: 一个包含任务列表的数组，这些任务会在你当前任务运行之前完成。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.task(<span class="string">'somename'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">  <span class="comment">// 做一些事</span></div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h3 id="gulp-src-globs-options"><a href="#gulp-src-globs-options" class="headerlink" title="gulp.src(globs[, options])"></a>gulp.src(globs[, options])</h3><p>输出（Emits）符合所提供的匹配模式（glob）或者匹配模式的数组（array of globs）的文件。 将返回一个 Vinyl files 的 stream 它可以被 piped 到别的插件中。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src(<span class="string">'client/templates/*.jade'</span>)</div><div class="line">  .pipe(jade())</div><div class="line">  .pipe(minify())</div><div class="line">  .pipe(gulp.dest(<span class="string">'build/minified_templates'</span>));</div></pre></td></tr></table></figure></p>
<h3 id="gulp-dest-path-options"><a href="#gulp-dest-path-options" class="headerlink" title="gulp.dest(path[, options])"></a>gulp.dest(path[, options])</h3><p>能被 pipe 进来，并且将会写文件。并且重新输出（emits）所有数据，因此你可以将它 pipe 到多个文件夹。如果某文件夹不存在，将会自动创建它。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src(<span class="string">'./client/templates/*.jade'</span>)</div><div class="line">  .pipe(jade())</div><div class="line">  .pipe(gulp.dest(<span class="string">'./build/templates'</span>))</div><div class="line">  .pipe(minify())</div><div class="line">  .pipe(gulp.dest(<span class="string">'./build/minified_templates'</span>));</div></pre></td></tr></table></figure></p>
<h3 id="gulp-watch-glob-opts-cb"><a href="#gulp-watch-glob-opts-cb" class="headerlink" title="gulp.watch(glob[, opts, cb])"></a>gulp.watch(glob[, opts, cb])</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.watch(<span class="string">'js/**/*.js'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">event</span>) </span>&#123;</div><div class="line">  <span class="built_in">console</span>.log(<span class="string">'File '</span> + event.path + <span class="string">' was '</span> + event.type + <span class="string">', running tasks...'</span>);</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>怎么少了gulp.pipe()？<br>生活中我们每天都在搞事情，事情等同于任务，但这个任务中不同的环节是有依赖关系的，比如完成一幅画涂颜料需要在绘画完成后才能开始，于是gulp.pipe()相当于提供一个管道，让我们将这些环节连接起来，最终完成一个完整的任务。</p>
<h2 id="关于glob参数匹配规则"><a href="#关于glob参数匹配规则" class="headerlink" title="关于glob参数匹配规则"></a>关于glob参数匹配规则</h2><p>发起一项任务需要寻找文件对象，这就需要一些规则用于匹配文件。</p>
<h3 id="匹配规则"><a href="#匹配规则" class="headerlink" title="匹配规则"></a>匹配规则</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">*  匹配文件路径中的<span class="number">0</span>个或多个字符，但不会匹配路径分隔符，除非路径分隔符出现在末尾</div><div class="line">** 匹配路径中的<span class="number">0</span>个或多个目录及其子目录,需要单独出现，即它左右不能有其他东西了。如果出现在末尾，也能匹配文件。</div><div class="line">?  匹配文件路径中的一个字符(不会匹配路径分隔符)</div><div class="line">[...] 匹配方括号中出现的字符中的任意一个，当方括号中第一个字符为^或!时，则表示不匹配方括号中出现的其他字符中的任意一个，类似js正则表达式中的用法</div><div class="line">!(pattern|pattern|pattern) 匹配任何与括号中给定的任一模式都不匹配的</div><div class="line">?(pattern|pattern|pattern) 匹配括号中给定的任一模式<span class="number">0</span>次或<span class="number">1</span>次，类似于js正则中的(pattern|pattern|pattern)?</div><div class="line">+(pattern|pattern|pattern) 匹配括号中给定的任一模式至少<span class="number">1</span>次，类似于js正则中的(pattern|pattern|pattern)+</div><div class="line">*(pattern|pattern|pattern) 匹配括号中给定的任一模式<span class="number">0</span>次或多次，类似于js正则中的(pattern|pattern|pattern)*</div><div class="line">@(pattern|pattern|pattern) 匹配括号中给定的任一模式<span class="number">1</span>次，类似于js正则中的(pattern|pattern|pattern)</div></pre></td></tr></table></figure>
<p><strong>示例：</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">* 能匹配 a.js,x.y,abc,abc/,但不能匹配a/b.js</div><div class="line">*.* 能匹配 a.js,style.css,a.b,x.y</div><div class="line">*<span class="comment">/*/*.js 能匹配 a/b/c.js,x/y/z.js,不能匹配a/b.js,a/b/c/d.js</span></div><div class="line">** 能匹配 abc,a/b.js,a/b/c.js,x/y/z,x/y/z/a.b,能用来匹配所有的目录和文件</div><div class="line">**/*.js 能匹配 foo.js,a/foo.js,a/b/foo.js,a/b/c/foo.js</div><div class="line">a<span class="comment">/**/</span>z 能匹配 a/z,a/b/z,a/b/c/z,a/d/g/h/j/k/z</div><div class="line">a<span class="comment">/**b/z 能匹配 a/b/z,a/sb/z,但不能匹配a/x/sb/z,因为只有**单独出现才能匹配多级目录</span></div><div class="line">?.js 能匹配 a.js,b.js,c.js</div><div class="line">a?? 能匹配 a.b,abc,但不能匹配ab/,因为它不会匹配路径分隔符</div><div class="line">[xyz].js 只能匹配 x.js,y.js,z.js,不会匹配xy.js,xyz.js等,整个中括号只代表一个字符</div><div class="line">[^xyz].js 能匹配 a.js,b.js,c.js等,不能匹配x.js,y.js,z.js</div></pre></td></tr></table></figure></p>
<p>由上面可以看出gulp匹配的规则和正则大致相同。有一点要注意的是<strong>**匹配符号会深入所有的子目录里面</strong>，而出现路径分割符只会进入<strong>一级子目录</strong>中寻找匹配文件。</p>
<h3 id="匹配模式"><a href="#匹配模式" class="headerlink" title="匹配模式"></a>匹配模式</h3><p>匹配模式分为：<strong>单一匹配、多种匹配、展开匹配</strong></p>
<h4 id="单一匹配"><a href="#单一匹配" class="headerlink" title="单一匹配"></a>单一匹配</h4><p>精确匹配：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src(<span class="string">"./js/main.js"</span>)</div></pre></td></tr></table></figure></p>
<p>模糊匹配：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src(<span class="string">"**/*.js"</span>)</div></pre></td></tr></table></figure></p>
<h4 id="多种匹配"><a href="#多种匹配" class="headerlink" title="多种匹配"></a>多种匹配</h4><p>当有多种匹配模式(多个单一匹配)时可以使用数组<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src([<span class="string">'js/*.js'</span>,<span class="string">'css/*.css'</span>,<span class="string">'*.html'</span>])</div></pre></td></tr></table></figure></p>
<p>使用数组的方式还有一个好处就是可以很方便的使用排除模式，在数组中的单个匹配模式前加上!即是排除模式，它会在匹配的结果中排除这个匹配，要注意一点的是不能在数组中的第一个元素中使用排除模式<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.src([*.js,<span class="string">'!b*.js'</span>]) <span class="comment">//匹配所有js文件，但排除掉以b开头的js文件</span></div><div class="line">gulp.src([<span class="string">'!b*.js'</span>,*.js]) <span class="comment">//不会排除任何文件，因为排除模式不能出现在数组的第一个元素中</span></div></pre></td></tr></table></figure></p>
<h4 id="展开匹配"><a href="#展开匹配" class="headerlink" title="展开匹配"></a>展开匹配</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a&#123;b,c&#125;d 会展开为 abd,acd</div><div class="line">a&#123;b,&#125;c 会展开为 abc,ac</div><div class="line">a&#123;<span class="number">0.</span><span class="number">.3</span>&#125;d 会展开为 a0d,a1d,a2d,a3d</div><div class="line">a&#123;b,c&#123;d,e&#125;f&#125;g 会展开为 abg,acdfg,acefg</div><div class="line">a&#123;b,c&#125;d&#123;e,f&#125;g 会展开为 abdeg,acdeg,abdeg,abdfg</div></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>gulp</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML5新增input类型</title>
    <url>/2016/06/16/h5-tags/</url>
    <content><![CDATA[<h1 id="HTML5表单新增input类型"><a href="#HTML5表单新增input类型" class="headerlink" title="HTML5表单新增input类型"></a>HTML5表单新增input类型</h1><p>在HTML4等早期版本中，input元素已经有了一些type类型，type可取的值有下面这些：<br>text文本类型：<code>&lt;input type=&quot;text&quot;/&gt;</code><br>password密码类型：<code>&lt;input type=&quot;password&quot;/&gt;</code><br>radio 单选类型：<code>&lt;input type=&quot;radio&quot;/&gt;</code>选项<br>checkbox 多选类型：<code>&lt;input type=&quot;checkbox&quot;/&gt;</code>选项<br>file 文件类型：<code>&lt;input type=&quot;file&quot;/&gt;</code><br>submit 提交按钮：<code>&lt;input type=&quot;submit&quot;value=&quot;提交&quot;/&gt;</code><br>reset重置按钮：<code>&lt;input type=&quot;reset&quot;value=重置&quot;/&gt;</code><br>button定义按钮：<code>&lt;input type=&quot;button&quot;value=&quot;按钮1&quot;/&gt;</code><br>image 定义图片按钮：<code>&lt;input type=&quot;image&quot;src=&quot;..&quot;/&gt;</code><br>hidden定义隐藏域：<code>&lt;input type=&quot;hidden&quot;/&gt;</code></p>
<p><strong>在HTML5中，input元素新增了一些类型，有email邮件类型、number数字类型、range数字滑动条、url地址类型、date日期类型及search搜索类型。下面我们来逐个介绍一下它们。</strong></p>
<h2 id="email邮件类型"><a href="#email邮件类型" class="headerlink" title="email邮件类型"></a>email邮件类型</h2><p>跟文本框类似，但它只接受符合email邮件格式的字符串，当表单提交时会自动检测，若不合法，则会给出提示。<br>html代码如下：</p>
<pre><code>&lt;input type=&quot;email&quot; name=&quot;&quot; value=&quot;&quot;&gt;
</code></pre><h2 id="number数字类型"><a href="#number数字类型" class="headerlink" title="number数字类型"></a>number数字类型</h2><p>跟文本框类似，它只接受数字类型的值，表单提交时会自动检测，若不合法，则会给出提示。<br>html代码如下：</p>
<pre><code>&lt;input type=&quot;number&quot; name=&quot;&quot; value=&quot;&quot;&gt;
</code></pre><h2 id="range数字滑动条"><a href="#range数字滑动条" class="headerlink" title="range数字滑动条"></a>range数字滑动条</h2><p>它展现出来是一个可以拖拉的滑动条，包含一定的数字范围，如：</p>
<pre><code>&lt;input type=&quot;range&quot; min=&quot;1&quot; max=&quot;10&quot;&gt;
</code></pre><p>当拖动滑动块时，它的value值会在1到10之间变化。</p>
<h2 id="url地址类型"><a href="#url地址类型" class="headerlink" title="url地址类型"></a>url地址类型</h2><p>跟文本框类似，它只接受网址类型的值，表单提交时会自动检测，若不合法，则会给出提示。<br>html代码如下：</p>
<pre><code>&lt;input type=&quot;url&quot;&gt;
</code></pre><h2 id="date日期类型"><a href="#date日期类型" class="headerlink" title="date日期类型"></a>date日期类型</h2><p>当此类表单成为焦点时，会自动弹出日历或者是调节按钮，但其样式会由于浏览器内核的不同而不同，此类型有一系列的只可选：<br>date：选取日、月、年<br>month：选取月、年<br>week：选取周、年<br>time：选取时间（小时和分钟）<br>datetime：选取时间、日、月、年，为UTC时间<br>datetime-local：选取时间、日、月、年，为本地时间<br>html代码如下：</p>
<pre><code>&lt;input type=&quot;date&quot;&gt;
</code></pre><p>其它的只需更改type值就可以了，就不一一列举了。</p>
<h2 id="search搜索类型"><a href="#search搜索类型" class="headerlink" title="search搜索类型"></a>search搜索类型</h2><p>跟文本框类似，它可以接受任意的值作为关键字，我们可以通过value获取这些关键字。<br>html代码如下：</p>
<pre><code>&lt;input type=&quot;search&quot;&gt;
</code></pre>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>html5</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML5(Audio)移动音乐播放器</title>
    <url>/2016/10/30/how-to-make-a-html5-player/</url>
    <content><![CDATA[<p>“咳咳咳..在看正文之前，如果客官有兴趣可以轻点播放按钮聆听一下我的<strong>第一首吉他原创歌曲</strong>”。——作于大三下学期</p>
<p><audio style="width: 100%" src="http://qiniu.huzerui.com/audio/guita-september.mp3" controls></audio><br>“怎么感觉整个人都不好了…还有呜呜呜的声音”</p>
<p>“那时正值寒冬，于学校宿舍顶楼录制，周围一片漆黑，忽一阵冷风袭来，但对我而言那是美丽的协奏”</p>
<p>“什么鬼..！”</p>
<p>“好吧…那听听这首吧，也许能帮到客官您，这是我的<strong>第二首原创歌曲</strong>”。——作于大四上学期</p>
<p><audio style="width: 100%" src="http://qiniu.huzerui.com/audio/guita-nightsky.mp3" controls></audio><br>“怎么比第一首调子还哀伤”？Are you kiding me？</p>
<p><strong>“蓝瘦，香菇，在这里。”</strong></p>
<p>好了，客观见笑了，为了真正治愈你，让我们回归正题吧！</p>
<p>这段时间公司一直在做一个PC的教育类单页应用，庞大复杂，涉及非常多H5的知识，音频就是其中的一部分。闲暇时写了一个移动音乐播放器，除了将之作为练手项目，对于音乐的喜爱也是促使自己想写个播放器的原因。</p>
<p>在线地址：请猛击<a href="http://alex1504.oschina.io/demo/player/" target="_blank" rel="external">这里</a><br>源码：请猛击<a href="https://git.oschina.net/alex1504/demo/tree/osc-pages/player?dir=1&amp;filepath=player&amp;oid=4c10643c235593569490e3da0d2ff7f613ece98e&amp;sha=ec2e4efea87ee25ee0818481a8cf1f5893c5947c" target="_blank" rel="external">这里</a></p>
<p>注意：使用PC浏览最好打开<strong>移动设备模式</strong>，使用移动设备浏览需要关闭无痕浏览模式（否则无法使用本地存储，一般浏览器都是默认不开启），项目需要在<strong>本地服务器</strong>或<strong>线上服务器</strong>运行，以file:///形式的地址打开是无法进行ajax请求的，从而无法看到音乐数据。<br><img src="http://qiniu.huzerui.com/image/2016-10-30-how-to-make-a-mobile-html5-player.jpg" alt="UI图"></p>
<h1 id="项目实现的功能及所用知识"><a href="#项目实现的功能及所用知识" class="headerlink" title="项目实现的功能及所用知识"></a>项目实现的功能及所用知识</h1><ul>
<li>播放器的基础操作，上一首，下一首(顺序播放、随机播放、单曲循环)，播放暂停，滑动时间轴的歌词定位</li>
<li>初始handlebar模板渲染音乐列表数据，下拉滚动加载音乐列表数据。</li>
<li>歌曲列表可添加喜爱音乐，于下次刷新时更新喜爱音乐列表，基于HTML5本地存储。</li>
<li>布局采用rem布局，自适应移动端手机设备。</li>
<li>iconfont在线图标应用的使用</li>
</ul>
<h1 id="项目目录文件结构"><a href="#项目目录文件结构" class="headerlink" title="项目目录文件结构"></a>项目目录文件结构</h1><ul>
<li>css：存放样式文件</li>
<li>lib： 存放公共脚本库</li>
<li>js： 存放项目脚本文件</li>
<li>img： 存放图片</li>
<li>fonts： 项目字体文件</li>
<li>res： 项目音乐资源</li>
<li>ui：项目ui文件（psd）</li>
</ul>
<h1 id="项目js文件结构"><a href="#项目js文件结构" class="headerlink" title="项目js文件结构"></a>项目js文件结构</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// ============================配置变量================================</span></div><div class="line"><span class="keyword">var</span> rootPath = <span class="built_in">window</span>.location.href.replace(<span class="regexp">/\/\w+\.\w+/</span>, <span class="string">"/"</span>);</div><div class="line"><span class="keyword">var</span> Settings = &#123;</div><div class="line">    <span class="attr">playmode</span>: <span class="number">0</span>, <span class="comment">//0列表循环，1随机，2为单曲循环</span></div><div class="line">    volume: <span class="number">0.5</span>, <span class="comment">//音量</span></div><div class="line">    initNum: <span class="number">10</span>, <span class="comment">//列表初始化歌曲数</span></div><div class="line">    reqNum: <span class="number">10</span> <span class="comment">//后续请求歌曲数</span></div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="comment">// ============================工具函数================================</span></div><div class="line"><span class="keyword">var</span> Util = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> &#123;</div><div class="line"></div><div class="line">    &#125;</div><div class="line">&#125;)()</div><div class="line"><span class="comment">// ============================Dom选择器================================</span></div><div class="line"><span class="keyword">var</span> Dom = &#123;</div><div class="line">    </div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">// ============================全局变量================================</span></div><div class="line"><span class="keyword">var</span> winH = $(<span class="built_in">window</span>).height();</div><div class="line"></div><div class="line"><span class="keyword">var</span> songNum = <span class="number">0</span>; <span class="comment">//当前列表歌曲数目</span></div><div class="line"><span class="keyword">var</span> lrcHighIndex = <span class="number">0</span>; <span class="comment">// 歌词高亮索引</span></div><div class="line"><span class="keyword">var</span> lrcMoveIndex = <span class="number">0</span>; <span class="comment">// 歌词移动单位索引</span></div><div class="line"><span class="keyword">var</span> moveDis = <span class="number">0</span>; <span class="comment">// 单句歌词每次移动距离</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> duration = <span class="number">0</span>; <span class="comment">// 当前歌曲的时间</span></div><div class="line"><span class="keyword">var</span> index = <span class="number">0</span>; <span class="comment">//当前播放歌曲的索引</span></div><div class="line"><span class="keyword">var</span> songInfo = <span class="literal">null</span>; <span class="comment">// 当前歌曲信息</span></div><div class="line"><span class="keyword">var</span> songModelUI = <span class="literal">null</span>; <span class="comment">// 当前歌曲UI模型</span></div><div class="line"><span class="keyword">var</span> timeArr = []; <span class="comment">//当前歌曲时间数组</span></div><div class="line"><span class="keyword">var</span> formatTimeArr = []; <span class="comment">//当前歌曲时间数组(格式化为秒数)</span></div><div class="line"></div><div class="line"><span class="comment">// ============================入口函数================================</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">main</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    initUIFrame();</div><div class="line">    <span class="keyword">var</span> initModel = PlayerModel();</div><div class="line"></div><div class="line">    <span class="keyword">var</span> songListUI = ModelUIFrame(Dom.songListContainer);</div><div class="line">    <span class="keyword">var</span> lsongListUI = ModelUIFrame(Dom.lSongListContainer);</div><div class="line">    initModel.getSongList(<span class="string">"data/data.json"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">        <span class="comment">// 生成所有歌曲列表</span></div><div class="line">        songListUI.renderList(data, <span class="number">0</span>, <span class="literal">null</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            songListUI.updateList();</div><div class="line">        &#125;);</div><div class="line">        <span class="comment">// 生成喜爱歌曲列表</span></div><div class="line">        initModel.getLoveSongArr(<span class="function"><span class="keyword">function</span>(<span class="params">lSongArr</span>) </span>&#123;</div><div class="line">            lsongListUI.renderList(data, <span class="number">1</span>, lSongArr);</div><div class="line">        &#125;);</div><div class="line">        <span class="comment">// 添加动画</span></div><div class="line">        Util.addAnimationDelay(Dom.song);</div><div class="line">        <span class="comment">// 保存歌词数据</span></div><div class="line">        initModel.saveLyric(data);</div><div class="line"></div><div class="line">    &#125;);</div><div class="line">    EventHandler();</div><div class="line">&#125;</div><div class="line"><span class="comment">// ============================初始化UI函数================================</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">initUIFrame</span>(<span class="params"></span>) </span>&#123;</div><div class="line"></div><div class="line">&#125;</div><div class="line"><span class="comment">// ============================实现数据交互方法================================</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">PlayerModel</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    </div><div class="line">&#125;</div><div class="line"><span class="comment">// ============================模型动态UI模块================================</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">ModelUIFrame</span>(<span class="params">container</span>) </span>&#123;</div><div class="line">    </div><div class="line">&#125;</div><div class="line"><span class="comment">// ============================事件绑定模块================================</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">EventHandler</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    </div><div class="line">&#125;</div><div class="line"><span class="comment">// 调用入口函数</span></div><div class="line"> main();</div></pre></td></tr></table></figure>
<h1 id="功能点详解"><a href="#功能点详解" class="headerlink" title="功能点详解"></a>功能点详解</h1><h2 id="Handlebar-js初次渲染及滚动加载"><a href="#Handlebar-js初次渲染及滚动加载" class="headerlink" title="Handlebar.js初次渲染及滚动加载"></a>Handlebar.js初次渲染及滚动加载</h2><p>使用前端模板优点是把数据和结构分离出来，代码更清晰。但后来发现handlerbar.js似乎无法在js中示例模板对象，而html中的handlebar在初次进入页面便会被编译了，因此后续添加音乐还是采用传统的拼接字符串的方式，如果你有更优雅的动态加载方式，欢迎讨论交流。</p>
<p><strong>html:</strong> handlebars模板包含在script标签之中并且type类型为”text/x-handlebars-template”，在初始化页面的时候根据js获取数据植入后就渲染出相应的html。<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">id</span>=<span class="string">"sListTpl"</span> <span class="attr">type</span>=<span class="string">"text/x-handlebars-template"</span>&gt;</span><span class="xml"></span></div><div class="line">&#123;&#123;#each this&#125;&#125;</div><div class="line">&#123;&#123;#isInitData this @index&#125;&#125;</div><div class="line"><span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">"song btm-line"</span> <span class="attr">data-src</span>=<span class="string">&#123;&#123;songSrc&#125;&#125;</span> <span class="attr">data-index</span>=<span class="string">&#123;&#123;id&#125;&#125;</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"poster"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&#123;&#123;poster.thumbnail&#125;&#125;</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"songinfo"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">"lsongname"</span>&gt;</span>&#123;&#123;songName&#125;&#125;<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">sub</span> <span class="attr">class</span>=<span class="string">"lsinger"</span>&gt;</span>&#123;&#123;singer&#125;&#125;<span class="tag">&lt;/<span class="name">sub</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"loveflag"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">i</span> <span class="attr">class</span>=<span class="string">"icon icon-love &#123;&#123;#if loveFlag&#125;&#125;active&#123;&#123;/if&#125;&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">i</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">&#123;&#123;/isInitData&#125;&#125;</div><div class="line">&#123;&#123;/each&#125;&#125;</div><div class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line"><span class="comment">&lt;!-- END歌曲列表模块 --&gt;</span></div></pre></td></tr></table></figure></p>
<p><strong>js:</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">renderAllList</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> preTpl;</div><div class="line">    <span class="keyword">var</span> lsongArr = Util.getItem(<span class="string">'lsonglist'</span>) === <span class="literal">null</span> ? [] : <span class="built_in">JSON</span>.parse(Util.getItem(<span class="string">'lsonglist'</span>));</div><div class="line">    <span class="comment">// 生成列表</span></div><div class="line">    <span class="keyword">if</span> (!sListTpl) &#123;</div><div class="line">        <span class="comment">// 后续动态生成歌曲</span></div><div class="line">        <span class="keyword">var</span> tpl = <span class="string">""</span>;</div><div class="line">        <span class="keyword">var</span> songIndex = songNum;</div><div class="line">        $.each(data, <span class="function"><span class="keyword">function</span>(<span class="params">index, el</span>) </span>&#123;</div><div class="line">            <span class="keyword">if</span> (index &gt;= songIndex &amp;&amp; index &lt; songIndex + Settings.reqNum) &#123;</div><div class="line">                tpl += <span class="string">"&lt;li class='song btm-line' data-src='res/music/"</span> + songNum + <span class="string">".mp3' data-index='"</span> + songNum + <span class="string">"'&gt;&lt;div class='poster'&gt;&lt;img src='./img/poster/"</span> + songNum + <span class="string">"-thumbnail.jpg'&gt;&lt;/div&gt;&lt;div class='songinfo'&gt;&lt;h2 class='lsongname'&gt;"</span> + el.songName + <span class="string">"&lt;/h2&gt;&lt;sub class='lsinger'&gt;"</span> + el.singer + <span class="string">"&lt;/sub&gt;&lt;/div&gt;&lt;div class='loveflag'&gt;&lt;i class='icon icon-love '&gt;&lt;/i&gt;&lt;/div&gt;&lt;/li&gt;"</span>;</div><div class="line">                songNum++;</div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line">        $(container).append($(tpl));</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="comment">// 首次生成歌曲</span></div><div class="line">        preTpl = Handlebars.compile(sListTpl);</div><div class="line">        $(container).html(preTpl(data));</div><div class="line">    &#125;</div><div class="line">    <span class="comment">// 更新喜爱图标</span></div><div class="line">    <span class="keyword">if</span> (lsongArr.length !== <span class="number">0</span>) &#123;</div><div class="line">        $.each(lsongArr, <span class="function"><span class="keyword">function</span>(<span class="params">index, val</span>) </span>&#123;</div><div class="line">            Dom.songListContainer.find(<span class="string">".song"</span>).eq(val).find(<span class="string">".icon-love"</span>).addClass(<span class="string">'active'</span>);</div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="rem布局自适应方案"><a href="#rem布局自适应方案" class="headerlink" title="rem布局自适应方案"></a>rem布局自适应方案</h2><p>大体上指的是html根元素上定义一个字体大小，然后css样式定义时使用rem作为单位，包括margin、paddding、用于绝对定位的单位等等。然后js根据手机设备的屏幕大小，改变根字体的大小，这样整个页面也会跟着相应的缩小或放大。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 根据手机屏幕自适应字体大小（rem自适应方案）</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">rescale</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> docEle = <span class="built_in">document</span>.documentElement;</div><div class="line">    <span class="keyword">var</span> width = docEle.clientWidth || <span class="built_in">window</span>.innerWidth;</div><div class="line">    <span class="keyword">if</span> (width &gt; <span class="number">640</span> &amp;&amp; width !== <span class="number">768</span>) &#123;</div><div class="line">        docEle.style.fontSize = <span class="string">"100px"</span>;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (width == <span class="number">320</span>) &#123;</div><div class="line">        <span class="comment">//iphone5</span></div><div class="line">        docEle.style.fontSize = <span class="string">"49px"</span>;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (width == <span class="number">375</span>) &#123;</div><div class="line">        <span class="comment">//iphone6 </span></div><div class="line">        docEle.style.fontSize = <span class="string">"57px"</span>;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (width == <span class="number">412</span>) &#123;</div><div class="line">        <span class="comment">// Nexus 5X</span></div><div class="line">        docEle.style.fontSize = <span class="string">"57px"</span>;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (width == <span class="number">768</span>) &#123;</div><div class="line">        <span class="comment">//ipad </span></div><div class="line">        docEle.style.fontSize = <span class="string">"88px"</span>;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        docEle.style.fontSize = <span class="built_in">Math</span>.round(width / <span class="number">640</span> * <span class="number">100</span>) + <span class="string">"px"</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>更多详解，请看先前一篇文章《移动端自适应布局解决方案——rem》，您可以猛击<a href="http://huzerui.com/blog/2016/06/16/mobile-rem/" target="_blank" rel="external">这里</a>跳转。</p>
<h2 id="关于歌词的同步方案实现"><a href="#关于歌词的同步方案实现" class="headerlink" title="关于歌词的同步方案实现"></a>关于歌词的同步方案实现</h2><p>目前音乐播放器的歌词同步显示大概有两种，一种是精确到单个文字，一种是精确到单行歌词。本文实现的是第二种。</p>
<h2 id="整体实现思路"><a href="#整体实现思路" class="headerlink" title="整体实现思路"></a>整体实现思路</h2><p>页面初始化时，请求歌曲数据json（本地json文件模拟），其中歌名、歌手、图片等按需渲染到html中，将歌词存储到localStorage中。此时，F12打开chrome调试器，进入Application-LocalStorage可以看到：<br><img src="http://qiniu.huzerui.com/image/2016-10-30-lyric-localstorage.jpg" alt="歌词本地存储"></p>
<p>点击一首歌进入播放页面后，歌词就会从本地存储中读取，此时你会看到生成这样的歌词结构：<br><img src="http://qiniu.huzerui.com/image/2016-10-30-lyric-html.jpg" alt="歌词结构"><br>每一行歌词都将要将歌词时间绑定在data-point上，监听歌曲播放的timeupdate事件，当歌曲的时间(经过取整处理)与当前data-point值相等时，就为当前歌词高亮（相当于给p添加current类名），并且根据当前高亮歌词的index索引将整个歌词盒子向上移动<strong>p标签的高度+margin-top的高度</strong>。</p>
<h3 id="lrc歌词的结构"><a href="#lrc歌词的结构" class="headerlink" title="lrc歌词的结构"></a>lrc歌词的结构</h3><p>来自网易云音乐的歌词数据：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[<span class="number">00</span>:<span class="number">14.64</span>]如果不是那镜子\n[<span class="number">00</span>:<span class="number">16.73</span>]不像你不藏秘密\n[<span class="number">00</span>:<span class="number">21.26</span>]我还不肯相信\n[<span class="number">00</span>:<span class="number">23.02</span>]没有你我的笑更美丽\n[<span class="number">00</span>:<span class="number">28.99</span>]那天听你在电话里略带抱歉的关心\n</div><div class="line"></div><div class="line">[<span class="number">00</span>:<span class="number">16.959</span>]摘一颗苹果\n[<span class="number">00</span>:<span class="number">19.800</span>]等你从门前经过\n[<span class="number">00</span>:<span class="number">22.700</span>]送到你的手中帮你解渴\n[<span class="number">00</span>:<span class="number">25.570</span>]像夏天的可乐\n</div><div class="line"></div><div class="line">[<span class="number">00</span>:<span class="number">00.00</span>] 作曲 : 周杰伦\n[<span class="number">00</span>:<span class="number">01.00</span>] 作词 : 周杰伦\n[<span class="number">00</span>:<span class="number">05.620</span>]\n[<span class="number">00</span>:<span class="number">37.980</span>]亲吻你的手\n</div></pre></td></tr></table></figure></p>
<p>可以看到<strong>格式 = [时间点] + 要显示的文字 + \n</strong><br>这里有两个坑需要注意：</p>
<ul>
<li>有的歌词秒数是精确到<strong>小数点后两位</strong>，<strong>有的是三位</strong>。</li>
<li>有的歌词（周杰伦《算什么男人》）格式是 <strong>[时间点]+\n</strong></li>
</ul>
<h3 id="时间歌词创建映射"><a href="#时间歌词创建映射" class="headerlink" title="时间歌词创建映射"></a>时间歌词创建映射</h3><p>首先以\n将歌词字符串分割成以<strong>[时间点]文字</strong>的数组，但由于这样分割之后最后一个元素是空的，所以用<strong>tempArr.splice(-1, 1)</strong>删除最后一个元素。</p>
<p>接下来循环遍历这个临时数组，由于上面提到的秒数精确度的问题，所以判断一下index为9是否为数字，若为数字则将该位数字删除。(采用字符串截取方式，若你对js字符串方法不熟悉，可以猛击<a href="http://huzerui.com/blog/2016/06/14/js-string-method/" target="_blank" rel="external">这里</a>)</p>
<p>经过这样的处理之后，临时数组的元素格式不再有区别了，此时再进行字符串截取，将截取到的时间点放入timeArr，将截取的歌词放入lyricArr，并以返回保存着这两个变量的对象。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">createArrMap</span>(<span class="params">lyric</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> timeArr = [],</div><div class="line">        lyricArr = [];</div><div class="line">    <span class="keyword">var</span> tempArr = lyric.split(<span class="string">"\n"</span>);</div><div class="line">    tempArr.splice(<span class="number">-1</span>, <span class="number">1</span>);</div><div class="line">    <span class="keyword">var</span> tempStr = <span class="string">""</span>;</div><div class="line">    $(tempArr).each(<span class="function"><span class="keyword">function</span>(<span class="params">index</span>) </span>&#123;</div><div class="line">        tempStr = <span class="keyword">this</span>;</div><div class="line">        <span class="keyword">if</span> (tempStr.charAt(<span class="number">9</span>).match(<span class="regexp">/\d/</span>) !== <span class="literal">null</span>) &#123;</div><div class="line">            tempStr = tempStr.substring(<span class="number">0</span>, <span class="number">9</span>) + tempStr.substring(<span class="number">10</span>);</div><div class="line">        &#125;</div><div class="line">        timeArr.push(tempStr.substring(<span class="number">0</span>, <span class="number">10</span>));</div><div class="line">        lyricArr.push(tempStr.substring(<span class="number">10</span>));</div><div class="line">    &#125;);</div><div class="line">    <span class="keyword">return</span> &#123;</div><div class="line">        <span class="attr">timeArr</span>: timeArr,</div><div class="line">        <span class="attr">lyricArr</span>: lyricArr</div><div class="line">    &#125;;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="生成歌词"><a href="#生成歌词" class="headerlink" title="生成歌词"></a>生成歌词</h3><p>由于上面歌词格式造成时间点对应的歌词为空，此时如果渲染出一个</p><p>标签的高度将为0，这会影响歌词向上移动距离的不统一。因此下面作出个判断如果为空，则替换为“————–”。（为空的时候大多数是歌曲中间停顿或过渡的时候）<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">renderLyric</span>(<span class="params">songinfo</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> arrMap = Util.createArrMap(songinfo.lyric);</div><div class="line">    <span class="keyword">var</span> tpl = <span class="string">""</span>;</div><div class="line">    $.each(arrMap.lyricArr, <span class="function"><span class="keyword">function</span>(<span class="params">index, lyric</span>) </span>&#123;</div><div class="line">        <span class="keyword">var</span> lyricContent = lyric === <span class="string">""</span> ? <span class="string">"--------------"</span> : lyric;</div><div class="line">        tpl += <span class="string">"&lt;p class='' data-point='"</span> + arrMap.timeArr[index] + <span class="string">"'&gt;"</span> + lyricContent + <span class="string">"&lt;/p&gt;"</span>;</div><div class="line">    &#125;);</div><div class="line">    Dom.lrcwrap.html(tpl);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="歌词同步"><a href="#歌词同步" class="headerlink" title="歌词同步"></a>歌词同步</h3><p>歌词同步我写在了syncLyric方法中，监听audio元素的timeupdate事件调用。<br>这个方法接收两个参数，第一个是当前播放歌曲时间（秒），第二个是转化为秒数的时间点数组。<br>如果当前时间&gt;=时间点，那么高亮当前歌词（以lrcHighIndex）存储，并且lrcHighIndex自增1。<br>当歌词高亮索引lrcHighIndex&gt;=1即歌词高亮不为第一句时，计算索引并让歌词盒子向上移动。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">syncLyric</span>(<span class="params">curS, formatTimeArr</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (<span class="built_in">Math</span>.floor(curS) &gt;= formatTimeArr[lrcHighIndex]) &#123;</div><div class="line">        Dom.lrc.eq(lrcHighIndex).addClass(<span class="string">'current'</span>).siblings().removeClass(<span class="string">'current'</span>);</div><div class="line">        <span class="keyword">if</span> (lrcHighIndex &gt;= <span class="number">1</span>) &#123;</div><div class="line">            lrcMoveIndex = lrcHighIndex - <span class="number">2</span>;</div><div class="line">            moveDis += Util.getMoveDis(lrcMoveIndex);</div><div class="line">            Dom.lrcwrap.animate(&#123;</div><div class="line">                <span class="string">"top"</span>: <span class="string">"-"</span> + moveDis + <span class="string">"px"</span></div><div class="line">            &#125;, <span class="number">100</span>);</div><div class="line">            lrcMoveIndex++;</div><div class="line">        &#125;</div><div class="line">        lrcHighIndex++;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>html5</tag>
        <tag>player</tag>
      </tags>
  </entry>
  <entry>
    <title>IcoMoon将图标转换成web字体</title>
    <url>/2016/06/16/icoMoon/</url>
    <content><![CDATA[<p>本文总结一款免费开源的Web应用程序<a href="https://icomoon.io/app/#/select" target="_blank" rel="external">IcoMoon</a>制作web图标字体。</p>
<h1 id="使用web字体图标优势"><a href="#使用web字体图标优势" class="headerlink" title="使用web字体图标优势"></a>使用web字体图标优势</h1><p>适用性：一个图标字体比一系列的图像（特别是在Retina屏中使用双倍大小的图像）要小。一旦图标字体加载了，你的图标就会马上渲染出来，不需要下载一个图像。</p>
<p>可扩展性：图标字体可以用过font-size属性设置其任何大小。这使您能够随时输出不同大小的图标，然而，使用位图，你必须得为每个不同大小的图像输出一个不同文件。</p>
<p>灵活性：文字效果可以很容易地应用到你的图标上，包括<strong>颜色，阴影和翻转</strong>等效果。他们还可以在任何背景下显示。</p>
<p>兼容性：网页字体支持所有现代浏览器，包括IE低版本。详细兼容性可以点击这里。</p>
<h1 id="IconMoon优势"><a href="#IconMoon优势" class="headerlink" title="IconMoon优势"></a>IconMoon优势</h1><p>提供完整图标解决方案：<br>600+字符，您可以根据自己需求定制（如就选两个）；可以导入其他字体，也进行特别定制（类似fontforge功能）；定制字体提供打包导出功能（省去了字体转换），兼容IE6+，现代浏览器以及各类手机设备，且有demo实例，并对字符进行了HTML转化。</p>
<h1 id="IconMoon使用方法"><a href="#IconMoon使用方法" class="headerlink" title="IconMoon使用方法"></a>IconMoon使用方法</h1><p>导入或选择图标、对图标进行编辑、生成并输出成zip文件（含字体本身、HTML实例页面和相应的CSS）</p>
<h2 id="导入或选择图标"><a href="#导入或选择图标" class="headerlink" title="导入或选择图标"></a>导入或选择图标</h2><p>选择内置图标只需在界面单击相应图标，图标高亮即为选中。<br><img src="http://qiniu.huzerui.com/image/2016-06-16-how-to-use-iconmoon-1.jpg" alt="选择"><br>如果你通过AI做好了一个图标想导入，只需点击“Imorticons”按钮，然后选择您想要添加的SVG文件——您也可以一次添加多个文件。这些图标将会出现在“Your Custom Icons”区域中。如果他们是高亮的黄色显示，表示这些图标是你将要创建的图标字体。</p>
<h2 id="对图标进行编辑"><a href="#对图标进行编辑" class="headerlink" title="对图标进行编辑"></a>对图标进行编辑</h2><p><img src="http://qiniu.huzerui.com/image/2016-06-16-how-to-use-iconmoon-2.gif" alt="编辑"><br>如果你想调整图标的位置、大小或旋转，你可以点击“Edit”按钮。可以使用“Save Copy”按钮来创建图片的变化（例如，一个镜像的衅标）。添加一个有意义的图标标记，因为这将被用来生成类名。</p>
<h2 id="生成并输出成zip文件"><a href="#生成并输出成zip文件" class="headerlink" title="生成并输出成zip文件"></a>生成并输出成zip文件</h2><p>点击屏幕底部的“Font”按钮开始生成字体。这样你就可以指定哪个图标映射到哪个字符上，例如，如果你要设置一套六个旋转的球，你可以每 这六个球分别指定字符：q、w、e、r、t和y。你也可以根据你自己的爱好选择一个字体的名字。<br><img src="http://qiniu.huzerui.com/image/2016-06-16-how-to-use-iconmoon-3.jpg" alt="输出"><br>单击“Download”下载字体包到你的电脑上。他有一个字文件夹包含了字体本身（woff,eot,ttf格式），以及一个HTML示例页面和相应的CSS。甚至还有一个javascript文件和一个解决方法，如果你需要支持IE或IE7。</p>
<h1 id="如何使用这些图标字体"><a href="#如何使用这些图标字体" class="headerlink" title="如何使用这些图标字体"></a>如何使用这些图标字体</h1><p>@font-face引入字体库、调用图标字体</p>
<h2 id="font-face引入字体库"><a href="#font-face引入字体库" class="headerlink" title="@font-face引入字体库"></a>@font-face引入字体库</h2><p>语法：<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">@font-face &#123;</div><div class="line">   font-family: &lt;YourWebFontName&gt;;</div><div class="line">   src: &lt;source&gt; [&lt;format&gt;][,&lt;source&gt; [&lt;format&gt;]]*;</div><div class="line">   [font-weight: &lt;weight&gt;];</div><div class="line">   [font-style: &lt;style&gt;];</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>兼容多种浏览器示例；如果只需兼容现代浏览器，可以按需删除src一些代码。<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line">@<span class="keyword">font-face</span> &#123;</div><div class="line">    <span class="attribute">font-family</span>: <span class="string">'icomoon'</span>;</div><div class="line">    <span class="attribute">src</span>: <span class="built_in">url</span>(<span class="string">'fonts/icomoon.eot?1h4iu7'</span>); <span class="comment">/* IE9 Compat Modes */</span></div><div class="line">    <span class="attribute">src</span>: <span class="built_in">url</span>(<span class="string">'fonts/icomoon.eot?1h4iu7#iefix'</span>) <span class="built_in">format</span>(<span class="string">'embedded-opentype'</span>),<span class="comment">/* IE6-IE8 */</span></div><div class="line">         <span class="built_in">url</span>(<span class="string">'fonts/icomoon.ttf?1h4iu7'</span>) <span class="built_in">format</span>(<span class="string">'truetype'</span>), <span class="comment">/* Modern Browsers */</span></div><div class="line">         <span class="built_in">url</span>(<span class="string">'fonts/icomoon.woff?1h4iu7'</span>) <span class="built_in">format</span>(<span class="string">'woff'</span>), <span class="comment">/* Safari, Android, iOS */</span></div><div class="line">         <span class="built_in">url</span>(<span class="string">'fonts/icomoon.svg?1h4iu7#icomoon'</span>) <span class="built_in">format</span>(<span class="string">'svg'</span>);  <span class="comment">/* Legacy iOS */</span></div><div class="line">    <span class="attribute">font-weight</span>: normal;</div><div class="line">    <span class="attribute">font-style</span>: normal;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="调用图标字体"><a href="#调用图标字体" class="headerlink" title="调用图标字体"></a>调用图标字体</h2><p>通过css属性选择器指定类为”icon-“开头的元素的font-family，并为icon-创建的图标类名定义content。与fontAwesome图标字体使用方法是相同的<br>css<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-attr">[class^="icon-"]</span>, <span class="selector-attr">[class*=" icon-"]</span> &#123;</div><div class="line">    <span class="comment">/* 使用!important 使样式获得最高优先级  */</span></div><div class="line">    <span class="attribute">font-family</span>: <span class="string">'icomoon'</span> <span class="meta">!important</span>;</div><div class="line">    <span class="attribute">speak</span>: none;</div><div class="line">    <span class="attribute">font-style</span>: normal;</div><div class="line">    <span class="attribute">font-weight</span>: normal;</div><div class="line">    <span class="attribute">font-variant</span>: normal;</div><div class="line">    <span class="attribute">text-transform</span>: none;</div><div class="line">    <span class="attribute">line-height</span>: <span class="number">1</span>;</div><div class="line"></div><div class="line">    <span class="comment">/* 使字体变得平滑 =========== */</span></div><div class="line">    <span class="attribute">-webkit-font-smoothing</span>: antialiased;</div><div class="line">    <span class="attribute">-moz-osx-font-smoothing</span>: grayscale;</div><div class="line">&#125;</div><div class="line"><span class="selector-class">.icon-home</span><span class="selector-pseudo">:before</span> &#123;</div><div class="line">    <span class="attribute">content</span>: <span class="string">"\e900"</span>;</div><div class="line">&#125;</div><div class="line"><span class="selector-class">.icon-office</span><span class="selector-pseudo">:before</span> &#123;</div><div class="line">    <span class="attribute">content</span>: <span class="string">"\e904"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>然后html中定义类名调用即可：<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"icon-home"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>最简单的方法就是将文件夹中的style.css样式文件粘贴到自己项目中的样式表即可。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>css</tag>
        <tag>图标</tag>
      </tags>
  </entry>
  <entry>
    <title>移动端H5多页开发拍门砖经验</title>
    <url>/2018/05/17/html5-multipage-development-experience/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2018-05-17-mobile-h5-experience-poster.png" alt="H5多页"><br>两年前刚接触移动端开发，刚开始比较疑惑，每次遇到问题都是到社区里提问或者吸取前辈的经验分享，感谢热衷于分享的开发者为前端社区带来欣欣向上的生命力。本文结合先前写的文章和开发经验分享给大家，希望也能帮助刚步入移动端开发的新人解惑。以下会以其中一个以<strong>公积金页面</strong>开发项目作为例子，介绍移动端的一些<strong>常见问题</strong>和使<strong>用Vuejs作为lib进行多页开发</strong>的经验。</p>
<h1 id="移动端自适应布局"><a href="#移动端自适应布局" class="headerlink" title="移动端自适应布局"></a>移动端自适应布局</h1><p>在项目中移动端最常用的自适应布局方案就是flexbox结合rem。规范的分栏式使用flexbox，其他大部分不规则视图使用rem，对于rem最常用的方案就是淘宝开源的<a href="https://github.com/amfe/lib-flexible" target="_blank" rel="external">可伸缩布局方案</a>。</p>
<p>根据设备设备像素比设置scale的值（scale = 1 / deviceRatio），这样可以<strong>保持视口device-width始终等于设备物理像素</strong>，接着根据屏幕大小动态计算根字体大小，具体是将屏幕划分为100等分，每份为a，1rem就等于10a。</p>
<h2 id="标注"><a href="#标注" class="headerlink" title="标注"></a>标注</h2><p>通常我们会拿到750宽的设计稿，这是基于iPhone6的物理分辨率。有的设计师也许会偷懒，设计图上面没有任何的标注，如果我们边开发边量尺寸，无疑效率是比较低的。要么让设计师标注上，要么自食其力。</p>
<p>如果设计师实在没有时间，推荐使用<a href="http://www.getmarkman.com/" target="_blank" rel="external">markman</a>进行标注，免费版阉割了一些功能（比如无法保存本地）不过基本满足了我们的需求了。</p>
<p>后来我发现比markman更好的标注工具<a href="http://www.fancynode.com.cn/pxcook" target="_blank" rel="external">PxCook</a>，该工具可以显示<strong>PSD设计图</strong>中的图层的样式代码，对于前端来说简直方便极了。</p>
<p>标注完成后开始写我们的样式，使用了淘宝的<strong>lib-flexible</strong>库之后，我们的根字体基准值就为750/100*10 = 75px。此时我们从图中若某个标注为100px，那么css中就应该设置为100/75 = 1.333333rem。所以为了提高开发效率，可以使用px转化为rem的插件。下面是sublimeText和Vscode的转换插件：</p>
<h2 id="px转rem插件"><a href="#px转rem插件" class="headerlink" title="px转rem插件"></a>px转rem插件</h2><ul>
<li><p>sublimeText插件：<a href="https://packagecontrol.io/packages/rem-unit" target="_blank" rel="external">rem-unit</a><br><img src="https://user-gold-cdn.xitu.io/2018/5/22/1638672eb1471718?w=400&amp;h=150&amp;f=gif&amp;s=341881" alt="rem-unit"></p>
</li>
<li><p>Vscode插件： <a href="https://marketplace.visualstudio.com/items?itemName=cipchk.cssrem" target="_blank" rel="external">cssrem</a><br><img src="https://user-gold-cdn.xitu.io/2018/5/22/1638672eb14e93b0?w=467&amp;h=243&amp;f=gif&amp;s=260047" alt="pxtorem"></p>
</li>
</ul>
<h2 id="使用rem的几点总结"><a href="#使用rem的几点总结" class="headerlink" title="使用rem的几点总结"></a>使用rem的几点总结</h2><ul>
<li>在所有的单位中，font-size推荐使用px，然后结合媒体查询进行重要节点的控制，这样可以满足<strong>突出或者弱化</strong>某些字体的需求，而非整体调整。</li>
<li><strong>众向的单位可以全部使用px，横向的使用rem</strong>，因为移动设备宽度有限，而高度可以无限向下滑动。但这也有<strong>特例</strong>，比如对于一些活动注册页面，需要在一屏幕内完全显示，没有下拉，这时候所有众向或者横向都应该使用rem作为单位。如图：</li>
</ul>
<p><img src="https://user-gold-cdn.xitu.io/2018/5/22/16386a027d874cad?w=1280&amp;h=1108&amp;f=png&amp;s=405886" alt="shili"></p>
<p>左图的表单高度单位由于下边空距较大，使用px在不同屏幕显示更加；而右边的活动注册页由于不能出现滚动条，所有的众向高度、margin、padding都应该使用rem。</p>
<ul>
<li><strong>border、box-shadow、border-radius</strong>等一些效果应该使用px作为单位。</li>
</ul>
<h2 id="手机状态栏和浏览器导航栏的影响"><a href="#手机状态栏和浏览器导航栏的影响" class="headerlink" title="手机状态栏和浏览器导航栏的影响"></a>手机状态栏和浏览器导航栏的影响</h2><p>之前发布的文章中，有个SF的前端小伙伴提出的问题：<br>文中作者有重点强调布局全部铺满，和下方与很多空隙的处理方案是不同的，在工作中我遇到这种情况，设计师的设计稿宽度为750×1334,但实际的展示高度并没有那么多，因为上方有导航栏还包括手机自己的状态栏展示，所以整体高度就达不到750，但是设计师设计稿是严格按照750进行设计的，这种情况下使用rem，严格按照设计师尺寸进行还原就会出现屏幕出现滚动条情况，请问针对这种情况您是怎么处理的？是从设计稿上规范，还是从开发上有相应的措施</p>
<p>依旧以我的分享界面为例：<br>展示高度不同通常发生在微信及浏览器端，因为前者没有地址栏和工具栏，这样显示高度通常会和设计师设计的视图吻合。那如果按照纯padding，margin即使全部使用rem，在浏览器端依旧会超出一屏高度，对于分享页面这种不是我们想要看到的。这时候就要做出取舍，我对<strong>主体区域采用绝对定位</strong>，这样上面间隙虽然小，不过仍能保持在一个屏幕高度显示。若采用margin padding在设置，必然已出现滚动条。当然这样的前提是依赖设计图的，通常设计师会为了空间感有保留一定的间隙，也不会将主要对象高度设的过高，否则太撑满也不好看，开发上如果设计图宽高没有在一定界限之内，超出也无法避免，不过我们这种分享界面通常是通过微信分享好友，通过浏览器打开的视图效果出现滚动条其实也不怎么影响不是么？<br>下面附上微信端和浏览器端的效果图：</p>
<p>微信端：<img src="https://user-gold-cdn.xitu.io/2018/5/22/163869f629a7588e?w=640&amp;h=1137&amp;f=jpeg&amp;s=200233" alt="微信端"></p>
<p>浏览器端： <img src="https://user-gold-cdn.xitu.io/2018/5/22/163869f94a367bea?w=640&amp;h=1137&amp;f=jpeg&amp;s=206900" alt="浏览器端"></p>
<h1 id="Vuejs作为lib开发移动端页面"><a href="#Vuejs作为lib开发移动端页面" class="headerlink" title="Vuejs作为lib开发移动端页面"></a>Vuejs作为lib开发移动端页面</h1><h2 id="为何不使用SPA模式"><a href="#为何不使用SPA模式" class="headerlink" title="为何不使用SPA模式"></a>为何不使用SPA模式</h2><p>一般移动端使用vue是为了数据交互频繁而且快速开发的页面，为什么不使用单页SPA开发模式，原因大概几点。</p>
<ul>
<li>为了快速开发，快速上线</li>
<li>项目其他成员不熟悉SPA，不熟悉webpack</li>
<li>参与项目时项目已使用多页开发，短时间无法重构</li>
</ul>
<p>抛开使用单页的架构，开发多页应用时，一个页面交互逻辑与一个Vue实例对应。</p>
<h2 id="基于接口返回数据的属性注入"><a href="#基于接口返回数据的属性注入" class="headerlink" title="基于接口返回数据的属性注入"></a>基于接口返回数据的属性注入</h2><p>“基于接口返回数据的属性注入”是个人创建的话术，抛开此概念，先说一下表单数据的绑定方式。</p>
<h3 id="表单的数据绑定"><a href="#表单的数据绑定" class="headerlink" title="表单的数据绑定"></a>表单的数据绑定</h3><p>一个重要的点是<strong>有几份表单就分开几个表单对象进行数据绑定</strong>。</p>
<p>以上图公积金查询为例，由于不同城市会有不同的查询要素，可能登陆方式只有一种，也可能有几种。比如上图有三种登陆方式，在使用vue布局时，有两种方案。</p>
<ul>
<li>1、 只建立一个表单用于数据绑定，点击按钮触发判断</li>
<li>2、有几种登陆方式建立几个表单，用一个字段标识当前显示的表单</li>
</ul>
<p>由于使用第三方的接口，一开始也没有先进行接口返回数据结构的查看，采用了第一种错误的方式，错误一是每种登陆方式下面的登陆要素的数量也不同，错误二是数据绑定在同一个表单data下，当用户在用户名登陆方式输入用户名密码后，切换到客户号登陆方式，就会出现数据错乱的情况。</p>
<p>解决完布局问题后，我们需要根据设计图定义一些状态，比如当前登陆方式的切换、同意授权状态的切换、按钮是否可以点击的状态、是否处于请求中的状态。当然还有一些app穿过来的数据，这里就忽略了。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">data: &#123;</div><div class="line">    <span class="attr">tags</span>: &#123;</div><div class="line">        <span class="attr">arr</span>: [<span class="string">''</span>],</div><div class="line">        <span class="attr">activeIndex</span>: <span class="number">0</span></div><div class="line">    &#125;,</div><div class="line">    <span class="attr">isAgreeProxy</span>: <span class="literal">true</span>,</div><div class="line">    <span class="attr">isLoading</span>: <span class="literal">false</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>接着审查一下接口返回的数据，推荐使用chrome插件postman，比如呼和浩特的登陆要素如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="string">"code"</span>: <span class="number">2005</span>,</div><div class="line">    <span class="string">"data"</span>: [</div><div class="line">        &#123;</div><div class="line">            <span class="string">"name"</span>: <span class="string">"login_type"</span>,</div><div class="line">            <span class="string">"label"</span>: <span class="string">"身份证号"</span>,</div><div class="line">            <span class="string">"fields"</span>: [</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_name"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"身份证号"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"text"</span></div><div class="line">                &#125;,</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_pass"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"密码"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"password"</span></div><div class="line">                &#125;</div><div class="line">            ],</div><div class="line">            <span class="string">"value"</span>: <span class="string">"1"</span></div><div class="line">        &#125;,</div><div class="line">        &#123;</div><div class="line">            <span class="string">"name"</span>: <span class="string">" login_type"</span>,</div><div class="line">            <span class="string">"label"</span>: <span class="string">"公积金账号"</span>,</div><div class="line">            <span class="string">"fields"</span>: [</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_name"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"公积金账号"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"text"</span></div><div class="line">                &#125;,</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_pass"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"密码"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"password"</span></div><div class="line">                &#125;</div><div class="line">            ],</div><div class="line">            <span class="string">"value"</span>: <span class="string">"0"</span></div><div class="line">        &#125;</div><div class="line">    ],</div><div class="line">    <span class="string">"message"</span>: <span class="string">"登录要素请求成功"</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>可以看到呼和浩特有两种授权登陆方式，我们在data中定义了一个loginWays，初始为空数组，接着methods中定义一个请求接口的函数，里面就是基于返回数据的基础上为上面fields对象注入一个input字段用于绑定，这就是所谓的基于接口返回数据的属性注入。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">methods: &#123;</div><div class="line">    <span class="attr">queryloginWays</span>: <span class="function"><span class="keyword">function</span>(<span class="params">channel_type, channel_code</span>) </span>&#123;</div><div class="line">        <span class="keyword">var</span> params = <span class="keyword">new</span> URLSearchParams();</div><div class="line">        params.append(<span class="string">'channel_type'</span>, channel_type);</div><div class="line">        params.append(<span class="string">'channel_code'</span>, channel_code);</div><div class="line">        axios.post(<span class="keyword">this</span>.loginParamsProxy, params)</div><div class="line">            .then(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</div><div class="line">                <span class="built_in">console</span>.log(res);</div><div class="line">                <span class="keyword">var</span> code = res.code || res.data.code;</div><div class="line">                <span class="keyword">var</span> msg = res.message || res.data.message;</div><div class="line">                <span class="keyword">var</span> loginWays = res.data.data ? res.data.data : res.data;</div><div class="line">                <span class="comment">// 查询失败</span></div><div class="line">                <span class="keyword">if</span> (code != <span class="number">2005</span>) &#123;</div><div class="line">                    alert(msg);</div><div class="line">                    <span class="keyword">return</span>;</div><div class="line">                &#125;</div><div class="line">                <span class="comment">// 添加input字段用于v-model绑定</span></div><div class="line">                loginWays.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">loginWay</span>) </span>&#123;</div><div class="line">                    loginWay.fields.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">field</span>) </span>&#123;</div><div class="line">                        field.input = <span class="string">''</span>;</div><div class="line">                    &#125;)</div><div class="line">                &#125;)</div><div class="line">                <span class="keyword">this</span>.loginWays = loginWays;</div><div class="line">                <span class="keyword">this</span>.tags.arr = loginWays.map(<span class="function"><span class="keyword">function</span>(<span class="params">loginWay</span>) </span>&#123;</div><div class="line">                    <span class="keyword">return</span> loginWay.label;</div><div class="line">                &#125;)</div><div class="line">            &#125;.bind(<span class="keyword">this</span>))</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>即使返回的数据有我们不需要的数据也没有关系，这样保证我们不会遗失进行下一步登陆所需要的数据。</p>
<p>这样多个表单绑定数据问题解决了，那么怎么进行页面间数据传递？如果是app传过来，那么通常使用URL拼接的方式，使用window.location.search获得queryString后再进行截取；如果通过页面套入javaWeb中，那么直接使用”${字段名}”就能获取，注意要js中获取java字段需要加双引号。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">computed: &#123;</div><div class="line">        <span class="comment">// 真实姓名</span></div><div class="line">        realName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.getQueryVariable(<span class="string">'name'</span>) || <span class="string">''</span></div><div class="line">        &#125;,</div><div class="line">        <span class="comment">// 身份证</span></div><div class="line">        identity: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> <span class="built_in">parseInt</span>(<span class="keyword">this</span>.getQueryVariable(<span class="string">'identity'</span>)) || <span class="string">''</span></div><div class="line">        &#125;,</div><div class="line">        <span class="comment">/*If javaWeb</span></div><div class="line">        realName: function() &#123;</div><div class="line">            return this.getQueryVariable('name') || ''</div><div class="line">        &#125;,</div><div class="line">        identity: function() &#123;</div><div class="line">            return parseInt(this.getQueryVariable('identity')) || ''</div><div class="line">        &#125;*/</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">methods</span>: &#123;</div><div class="line">        <span class="attr">getQueryVariable</span>: <span class="function"><span class="keyword">function</span>(<span class="params">variable</span>) </span>&#123;</div><div class="line">            <span class="keyword">var</span> query = <span class="built_in">window</span>.location.search.substring(<span class="number">1</span>);</div><div class="line">            <span class="keyword">var</span> vars = query.split(<span class="string">'&amp;'</span>);</div><div class="line">            <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; vars.length; i++) &#123;</div><div class="line">                <span class="keyword">var</span> pair = vars[i].split(<span class="string">'='</span>);</div><div class="line">                <span class="keyword">if</span> (<span class="built_in">decodeURIComponent</span>(pair[<span class="number">0</span>]) == variable) &#123;</div><div class="line">                    <span class="keyword">return</span> <span class="built_in">decodeURIComponent</span>(pair[<span class="number">1</span>]);</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">            <span class="built_in">console</span>.log(<span class="string">'Query variable %s not found'</span>, variable);</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure></p>
<h1 id="关于前端跨域调试"><a href="#关于前端跨域调试" class="headerlink" title="关于前端跨域调试"></a>关于前端跨域调试</h1><p>在进行接口请求时，我们的页面通常是在sublime的本地服务器或者vscode本地服务器预览，所以请求接口会遇到跨域的问题，如果使用Gulp进行打包，可以使用插件http-proxy-middleware，或者使用nginx。</p>
<h2 id="使用Gulp"><a href="#使用Gulp" class="headerlink" title="使用Gulp"></a>使用Gulp</h2><p>在项目构建的时候通常我们源代码会放在src文件夹下，然后使用gulp进行代码的压缩、合并、图片的优化（根据需要）等等，我们会使用gulp。</p>
<p>解决跨域的问题可以用<a href="https://www.npmjs.com/package/gulp-connect" target="_blank" rel="external">gulp-connect</a>结合<a href="https://www.npmjs.com/package/http-proxy-middleware" target="_blank" rel="external">http-proxy-middleware</a>，此时我们在gulp-connect中的本地服务器进行预览调试。</p>
<p>gulpfile.js如下： 开发过程使用<code>gulp server:dev</code>命令，监听文件改动并使用livereload刷新，并且代理src目录；使用<code>gulp</code>命令进行打包；使用<code>gulp server:dist</code>代理dist生产目录。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> concat = <span class="built_in">require</span>(<span class="string">'gulp-concat'</span>);</div><div class="line"><span class="keyword">var</span> uglify = <span class="built_in">require</span>(<span class="string">'gulp-uglify'</span>);</div><div class="line"><span class="keyword">var</span> autoprefixer = <span class="built_in">require</span>(<span class="string">'gulp-autoprefixer'</span>);</div><div class="line"><span class="keyword">var</span> useref = <span class="built_in">require</span>(<span class="string">'gulp-useref'</span>);</div><div class="line"><span class="keyword">var</span> connect = <span class="built_in">require</span>(<span class="string">'gulp-connect'</span>);</div><div class="line"><span class="keyword">var</span> proxyMiddleware = <span class="built_in">require</span>(<span class="string">'http-proxy-middleware'</span>);</div><div class="line"></div><div class="line"><span class="comment">// 开发跨域代理  将localhost:8088/api 映射到 https://api.xxxxx.com/</span></div><div class="line">gulp.task(<span class="string">'server:dev'</span>, [<span class="string">'listen'</span>], <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> middleware = proxyMiddleware([<span class="string">'/api'</span>], &#123;</div><div class="line">        <span class="attr">target</span>: <span class="string">'https://api.xxxxx.com/'</span>,</div><div class="line">        <span class="attr">changeOrigin</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">pathRewrite</span>: &#123;</div><div class="line">            <span class="string">'^/api'</span>: <span class="string">'/'</span></div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">    connect.server(&#123;</div><div class="line">        <span class="attr">root</span>: env == <span class="string">'dev'</span> ? <span class="string">'./src'</span> : <span class="string">'./dist'</span>,</div><div class="line">        <span class="attr">port</span>: <span class="number">8088</span>,</div><div class="line">        <span class="attr">livereload</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">middleware</span>: <span class="function"><span class="keyword">function</span>(<span class="params">connect, opt</span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> [middleware]</div><div class="line">        &#125;</div><div class="line"></div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">// 打包后跨域代理</span></div><div class="line">gulp.task(<span class="string">'server:dist'</span>, [<span class="string">'listen'</span>], <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> middleware = proxyMiddleware([<span class="string">'/api'</span>], &#123;</div><div class="line">        <span class="attr">target</span>: <span class="string">'https://api.xxxxx.com/'</span>,</div><div class="line">        <span class="attr">changeOrigin</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">pathRewrite</span>: &#123;</div><div class="line">            <span class="string">'^/api'</span>: <span class="string">'/'</span></div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">    connect.server(&#123;</div><div class="line">        <span class="attr">root</span>: <span class="string">'./dist'</span>,</div><div class="line">        <span class="attr">port</span>: <span class="number">8088</span>,</div><div class="line">        <span class="attr">livereload</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">middleware</span>: <span class="function"><span class="keyword">function</span>(<span class="params">connect, opt</span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> [middleware]</div><div class="line">        &#125;</div><div class="line"></div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'html'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/*.html'</span>)</div><div class="line">        .pipe(useref())</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/css/main.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'main.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/css/'</span>));</div><div class="line"></div><div class="line">    gulp.src(<span class="string">'src/css/share.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'share.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/css/'</span>));</div><div class="line"></div><div class="line">    gulp.src(<span class="string">'src/vendors/css/*.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'vendors.min.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/vendors/css'</span>));</div><div class="line">    <span class="keyword">return</span> gulp</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'js'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'src/vendors/js/*.js'</span>)</div><div class="line">        .pipe(concat(<span class="string">'vendors.min.js'</span>))</div><div class="line">        .pipe(uglify())</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/vendors/js'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'img'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/imgs/*'</span>)</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/imgs'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'listen'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.watch(<span class="string">'./src/css/*.css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/css/*.css'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">    gulp.watch(<span class="string">'./src/js/*.js'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/js/*.js'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">    gulp.watch(<span class="string">'./src/*.html'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/*.html'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'default'</span>, [<span class="string">'html'</span>, <span class="string">'css'</span>, <span class="string">'js'</span>, <span class="string">'img'</span>]);</div></pre></td></tr></table></figure></p>
<h2 id="使用nginx"><a href="#使用nginx" class="headerlink" title="使用nginx"></a>使用nginx</h2><p>在nginx配置使用proxy_pass，需要注意一点：<br>如果在proxy_pass后面的url加/，表示绝对根路径；如果没有/，表示相对路径，把匹配的路径部分也给代理走。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">server &#123;</div><div class="line">	listen 80;</div><div class="line">	server_name  localhost;</div><div class="line">	root   [Your project root];</div><div class="line">	index  index.html index.htm default.html default.htm;</div><div class="line">	</div><div class="line">    location ^~/api &#123; </div><div class="line">      proxy_pass https://api.xxxxx.com/;</div><div class="line">    &#125; </div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h1 id="公众号网页的调试"><a href="#公众号网页的调试" class="headerlink" title="公众号网页的调试"></a>公众号网页的调试</h1><p>如果你开发的H5基于微信jsSDK，你一定接触过微信授权域名，微信会将授权数据传给一个回调页面，而回调页面必须在你配置的域名下（含子域名）。比如我们获取用户的openid操作。而微信配置域名回去该域名根目录下检测一个<code>xxx_verify_xxx.txt</code>文件，确保该域名是属于你的。</p>
<p>所以要想在微信开发调试工具中获取openid，我们需要使用一种叫做内网穿透的工具。下面是自己比较常用的两个工具：</p>
<ul>
<li><a href="https://ngrok.com/" target="_blank" rel="external">ngrok</a></li>
<li><a href="https://hsk.oray.com/" target="_blank" rel="external">花生壳</a></li>
</ul>
<h2 id="ngrok"><a href="#ngrok" class="headerlink" title="ngrok"></a>ngrok</h2><p>ngrok执行命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ngrok -config ngrok.cfg start web</div></pre></td></tr></table></figure></p>
<p>在ngrok.exe目录需要一个配置文件<code>ngrok.cfg</code><br>以下是配置示例：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">server_addr: &quot;tunnel.cn:4443&quot;</div><div class="line">trust_host_root_certs: false</div><div class="line">tunnels:</div><div class="line">  web:</div><div class="line">   subdomain: &quot;xxx&quot; </div><div class="line">   proto:</div><div class="line">    http: 8086</div><div class="line">    https: 8086</div></pre></td></tr></table></figure></p>
<p>启动后xxx.tunnel.cn:4443会指向你本地的8086端口，将<code>xxx_verify_xxx.txt</code>文件放到8086端口根目录即可配置授权域名成功。</p>
<h2 id="花生壳"><a href="#花生壳" class="headerlink" title="花生壳"></a>花生壳</h2><p>花生壳免费版对于个人开通仅需6元，然后每月会提供给你1G的流量，免费版不支持80端口，最多支持两个域名，需要下载桌面客户端。</p>
<p>添加域名映射很简单，免费版无法配置自定义域名，由花生壳自动生成。<br><img src="https://user-gold-cdn.xitu.io/2018/5/22/1638698584d70582?w=1692&amp;h=685&amp;f=png&amp;s=57366" alt="花生壳"><br>配置成功后启动客户端可查看当前的状态</p>
<p>感谢阅读，欢迎任何形式的技术提问和交流！</p>
<blockquote>
<ul>
<li>邮箱：me@huzerui.com</li>
<li>知乎：<a href="https://www.zhihu.com/people/huzerui/activities" target="_blank" rel="external">晚风轻拂</a></li>
<li><a href="https://juejin.im/post/5b03b2ee5188254284525e87" target="_blank" rel="external">掘金</a></li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>移动端</tag>
        <tag>H5</tag>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>使用jQuery封装插件</title>
    <url>/2016/06/12/jquery-plugin/</url>
    <content><![CDATA[<p>jQuery封装插件的方法有三种：封装对象方法的插件、封装全局函数的插件、选择器插件</p>
<h1 id="对象方法的插件"><a href="#对象方法的插件" class="headerlink" title="对象方法的插件"></a>对象方法的插件</h1><p>这种插件是将对象方法封装起来，用于对通过选择器获取的jQuery对象进行操作，是最常见的一种插件。此类插件可以发挥出jQuery选择器的强大优势，有相当一部分的jQuery的方法，都是在jQuery脚本库内部通过这种形式“插”在内核上的，例如parent()方法，appendTo()方法等。</p>
<h2 id="添加一个对象扩展方法"><a href="#添加一个对象扩展方法" class="headerlink" title="添加一个对象扩展方法"></a>添加一个对象扩展方法</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"> $.fn.changeColor= <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">this</span>.css( <span class="string">"color"</span>, <span class="string">"red"</span> );</div><div class="line">&#125;;</div><div class="line">   $.fn.changeFont= <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">this</span>.css( <span class="string">"font-size"</span>, <span class="string">"24px"</span> );</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<h2 id="添加多个对象扩展方法"><a href="#添加多个对象扩展方法" class="headerlink" title="添加多个对象扩展方法"></a>添加多个对象扩展方法</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>)</span>&#123;</div><div class="line">   $.fn.changeColor= <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.css( <span class="string">"color"</span>, <span class="string">"red"</span> );</div><div class="line">&#125;;</div><div class="line">   $.fn.changeFont=<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.css( <span class="string">"font-size"</span>, <span class="string">"24px"</span> );</div><div class="line">&#125;;</div><div class="line">&#125;)(jQuery);</div></pre></td></tr></table></figure>
<h2 id="使用-fn-extend-obj-方法"><a href="#使用-fn-extend-obj-方法" class="headerlink" title="使用$.fn.extend(obj)方法"></a>使用$.fn.extend(obj)方法</h2><p>插件中不需要默认参数<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>)</span>&#123;</div><div class="line">    $.fn.extend(&#123;</div><div class="line">        <span class="string">"pluginName"</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">            <span class="keyword">this</span>.each(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">                <span class="comment">//这里放置插件代码</span></div><div class="line">            &#125;);</div><div class="line">            <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">&#125;)(jQuery);</div></pre></td></tr></table></figure></p>
<p>插件中需要设置一些默认参数，可以使用\$.extend()进行扩展。在后面将对\$.fn.extend()和$.extend()进行详解<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>)</span>&#123;</div><div class="line">    $.fn.extend(&#123;</div><div class="line">        <span class="string">"pluginName"</span>:<span class="function"><span class="keyword">function</span>(<span class="params">options</span>)</span>&#123;</div><div class="line">            options=$.extend(&#123;</div><div class="line">                <span class="comment">//放置默认参数</span></div><div class="line">            &#125;,options);</div><div class="line">            <span class="keyword">this</span>.each(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">                <span class="comment">//这里放置插件代码</span></div><div class="line">            &#125;);</div><div class="line">            <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">&#125;)(jQuery);</div></pre></td></tr></table></figure></p>
<h1 id="全局函数的插件"><a href="#全局函数的插件" class="headerlink" title="全局函数的插件"></a>全局函数的插件</h1><p>可以将独立的函数加到jQuery命名空间下。如常用的jQuery.ajax()方去首尾空格的jQuery.trim()方法，都是jQuery内部作为全局函数的插件附加到内核上去的。 </p>
<h2 id="添加单个全局函数"><a href="#添加单个全局函数" class="headerlink" title="添加单个全局函数"></a>添加单个全局函数</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.ltrim = <span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> str.replace( <span class="regexp">/^\s+/</span>, <span class="string">""</span> );</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<h2 id="添加多个全局函数"><a href="#添加多个全局函数" class="headerlink" title="添加多个全局函数"></a>添加多个全局函数</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.ltrim = <span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> str.replace( <span class="regexp">/^\s+/</span>, <span class="string">""</span> );</div><div class="line">&#125;;</div><div class="line"> </div><div class="line">$.rtrim = <span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> str.replace( <span class="regexp">/\s+$/</span>, <span class="string">""</span> );</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<h2 id="使用-extend-进行扩展-适合全局函数比较多的情况"><a href="#使用-extend-进行扩展-适合全局函数比较多的情况" class="headerlink" title="使用$.extend()进行扩展(适合全局函数比较多的情况)"></a>使用$.extend()进行扩展(适合全局函数比较多的情况)</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.extend(&#123;</div><div class="line">  <span class="attr">ltrim</span>:<span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">      <span class="keyword">return</span> str.replace( <span class="regexp">/^\s+/</span>, <span class="string">""</span> );</div><div class="line">  &#125;,</div><div class="line">  <span class="attr">rtrim</span>:<span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">      <span class="keyword">return</span> str.replace( <span class="regexp">/\s+$/</span>, <span class="string">""</span> );</div><div class="line">  &#125;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h2 id="独立的命名空间"><a href="#独立的命名空间" class="headerlink" title="独立的命名空间"></a>独立的命名空间</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.myPlugin=&#123;</div><div class="line">   <span class="attr">ltrim</span>:<span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">       <span class="keyword">return</span> str.replace( <span class="regexp">/^\s+/</span>, <span class="string">""</span> );</div><div class="line">   &#125;,</div><div class="line">   <span class="attr">rtrim</span>:<span class="function"><span class="keyword">function</span>(<span class="params"> str </span>) </span>&#123;</div><div class="line">       <span class="keyword">return</span> str.replace( <span class="regexp">/\s+$/</span>, <span class="string">""</span> );</div><div class="line">   &#125;</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<h1 id="选择器插件"><a href="#选择器插件" class="headerlink" title="选择器插件"></a>选择器插件</h1><p>虽然jQuery的选择器十分强大，但在少数情况下，还是会需要用到选择器插件来扩充一些自己喜欢的选择器。 </p>
<h2 id="编写jQuery选择器需要知道jQuery选择器的参数。"><a href="#编写jQuery选择器需要知道jQuery选择器的参数。" class="headerlink" title="编写jQuery选择器需要知道jQuery选择器的参数。"></a>编写jQuery选择器需要知道jQuery选择器的参数。</h2><p>jQuery选择器函数一共接收3个参数，代码如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span>(<span class="params">a,i,m</span>)</span>&#123;</div><div class="line">    <span class="comment">//第一个参数为a，指向当前遍历的DOM元素</span></div><div class="line">    <span class="comment">//第二个参数为i，指的是当前遍历DOM元素的索引，从0开始</span></div><div class="line">    <span class="comment">//第三个参数m比较特别，它是有jQuery正则引擎进一步解析后的产物（用match匹配出来的），结果是一个数组</span></div><div class="line">        <span class="comment">//m[0]，以例子$("div:gt(1)")来讲，是:gt(1)这部分</span></div><div class="line">        <span class="comment">//m[1]，选择器的引导符，匹配例子中的:号</span></div><div class="line">        <span class="comment">//m[2]，例子中的gt，确定是调用哪个选择器函数</span></div><div class="line">        <span class="comment">//m[3]，括号里的数字1，是编写选择器函数最重要的参数</span></div><div class="line">        <span class="comment">//m[4]，例子没有体现，略</span></div><div class="line">&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="构建选择器函数"><a href="#构建选择器函数" class="headerlink" title="构建选择器函数"></a>构建选择器函数</h2><p>例如编写$(“div:between(2,5)”)能实现获取索引值为3,4元素的功能<br>构建：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span>(<span class="params">a,i,m</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> tmp=m[<span class="number">3</span>].split(<span class="string">","</span>);   <span class="comment">//将传进来的m[3]以逗号分隔开，转成一个数组</span></div><div class="line">    <span class="keyword">return</span> tmp[<span class="number">0</span>]<span class="number">-0</span>&lt;i&amp;&amp;i&lt;tmp[<span class="number">1</span>]<span class="number">-0</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>扩展成jQuery选择器<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    $.extend($.expr[<span class="string">":"</span>],&#123;</div><div class="line">        <span class="attr">between</span>:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">            <span class="keyword">var</span> tmp=m[<span class="number">3</span>].split(<span class="string">","</span>);  </div><div class="line">            <span class="keyword">return</span> tmp[<span class="number">0</span>]<span class="number">-0</span>&lt;i&amp;&amp;i&lt;tmp[<span class="number">1</span>]<span class="number">-0</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;)</div><div class="line">&#125;)(jQuery)</div></pre></td></tr></table></figure></p>
<h1 id="插件的基本要点"><a href="#插件的基本要点" class="headerlink" title="插件的基本要点"></a>插件的基本要点</h1><ul>
<li>Query插件的文件名推荐命名为jquery.[插件名].js，以免和其他JS库插件混淆。 </li>
<li>所有的对象方法都应当附加到jQuery.fn对象上，而所有的全局函数都应当附加到jQuery对象本身。 </li>
<li><p><strong>在插件的范围里， this关键字代表了这个插件将要执行的jQuery对象；而在其他包含callback的jQuery函数中，this关键字代表了原生的DOM元素</strong>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>)</span>&#123;</div><div class="line">    $.fn.m​​yPlugin =<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line"><span class="comment">//此处没有必要将this包在$号中如$(this)，因为this已经是一个jQuery对象。</span></div><div class="line"><span class="comment">//$(this)等同于 $($('#element'));</span></div><div class="line">        <span class="keyword">this</span>.fadeIn(<span class="string">'normal'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line"><span class="comment">//此处callback函数中this关键字代表一个DOM元素</span></div><div class="line">        &#125;);</div><div class="line">    &#125;;</div><div class="line">&#125;)(jQuery);</div><div class="line"> </div><div class="line">$(<span class="string">'#element'</span>).myPlugin();</div></pre></td></tr></table></figure>
</li>
<li><p>可以通过this.each方法来遍历所有元素。</p>
</li>
<li>在插件头部加上一个分号，以免他人的不规范代码给插件带来影响。 </li>
<li>所有的方法或函数插件，都应当以分号结尾，以免压缩时出现问题 </li>
<li>除非插件需要返回的是一些需要获取的变量，<strong>插件应该返回一个jQuery对象</strong>，以保证插件的可链式操作。 </li>
<li>利于jQuery.extend()方法设置插件方法的默认参数，增加插件的可用性。 </li>
</ul>
<h1 id="jQuery-fn-extend-与jQuery-extend-的对比"><a href="#jQuery-fn-extend-与jQuery-extend-的对比" class="headerlink" title="jQuery.fn.extend()与jQuery.extend()的对比"></a>jQuery.fn.extend()与jQuery.extend()的对比</h1><ul>
<li>jQuery.fn.extend()是对于jQuery对象（原型）扩展的方法，因为jQuery.fn=jQuery.prototype</li>
<li>jQuery.extend()除了用于扩展jQuery全局函数和选择器函数之外，还有一个重要的功能，就是扩展已有的Objext对象<br><strong>语法：jQuery.extend(target,obj1,…[objN])</strong></li>
</ul>
<p>1、例如合并setting对象和options对象，修改并返回settings对象<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> setting=&#123;<span class="attr">validate</span>:<span class="literal">false</span>,<span class="attr">limit</span>:<span class="number">5</span>,<span class="attr">name</span>:<span class="string">"foo"</span>&#125;</div><div class="line"><span class="keyword">var</span> options=&#123;<span class="attr">validate</span>:<span class="literal">true</span>,<span class="attr">name</span>:<span class="string">"bar"</span>&#125;</div><div class="line"><span class="keyword">var</span> newOptions=jQuery.extend(setting,options)</div><div class="line"><span class="comment">//结果</span></div><div class="line">newOptions=&#123;<span class="attr">validate</span>:<span class="literal">true</span>,<span class="attr">limit</span>:<span class="number">5</span>,<span class="attr">name</span>:<span class="string">"bar"</span>&#125;</div></pre></td></tr></table></figure></p>
<p>由此可见，内部机制是有扩展对象将覆盖与原对象相同的属性的值；若原对象不存在某属性，则进行扩展<br>2、jQuery.extend()常被用于设置插件方法的一系列默认参数，如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">options</span>)</span>&#123;</div><div class="line">    options=jQuery.extend(&#123;</div><div class="line">        <span class="attr">name</span>:<span class="string">"bar"</span>,</div><div class="line">        <span class="attr">length</span>:<span class="number">5</span>,</div><div class="line">        <span class="attr">dataType</span>:<span class="string">"xml"</span>       <span class="comment">/*默认参数*/</span></div><div class="line">    &#125;,options)               <span class="comment">/*options为传递的参数*/</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery核心总结</title>
    <url>/2016/04/10/jquery-summary/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-04-10-jquery-poster.jpg" alt="jQuery"><br>最近重温了《锋利的jQuery》这本书，受益匪浅。</p>
<p><strong>如果你和前端若只如初见，如果你和jQuery一见钟情，一纸文字铺满了你相恋的他（她）的所有。</strong></p>
<p>我把整理出来的知识点用网状图串联了起来，并且每个部分分点概述，将一些类似的方法和属性排列在一起。其实最重要的区块就是5个部分。</p>
<p><strong>根据用户交互的基本流程，通常代码是获取元素，然后绑定相应的事件，接着触发这个事件后可以选择不同的交互方式。</strong>比如增加节点改变Dom，或者对某个元素设置动画、亦或者进行aJax与后台进行数据交互。</p>
<p><img src="http://qiniu.huzerui.com/image/2016-04-10-jquery-knowledge-map.jpg" alt="知识点串联图"></p>
<h1 id="jQuery-选择器总结（继承CSS风格）"><a href="#jQuery-选择器总结（继承CSS风格）" class="headerlink" title="jQuery 选择器总结（继承CSS风格）"></a>jQuery 选择器总结（继承CSS风格）</h1><h2 id="常用"><a href="#常用" class="headerlink" title="常用"></a>常用</h2><p>标签、Id、class选择器、群组选择器、后代选择器（只有Id返回单个元素）</p>
<h2 id="层次选择器"><a href="#层次选择器" class="headerlink" title="层次选择器"></a>层次选择器</h2><h3 id="后代选择器"><a href="#后代选择器" class="headerlink" title="后代选择器"></a>后代选择器</h3><p>选取祖先所有的后代元素（后代包括孙、曾孙…..）     A  B</p>
<h3 id="子元素选择器"><a href="#子元素选择器" class="headerlink" title="子元素选择器"></a>子元素选择器</h3><p>选取父元素的子元素（不包括子元素的子元素）     A&gt;B</p>
<h3 id="后一个同辈元素"><a href="#后一个同辈元素" class="headerlink" title="后一个同辈元素"></a>后一个同辈元素</h3><p>选取指定元素元素后一个同辈元素（集合）          A+B   等价于next（）方法</p>
<h3 id="后面的所有同辈元素"><a href="#后面的所有同辈元素" class="headerlink" title="后面的所有同辈元素"></a>后面的所有同辈元素</h3><p>选取指定元素后面的所有同辈元素（集合）  A~B  等价于nextAll（）方法</p>
<p>这里将siblings（）方法与next（）和nextAll（）作对比，</p>
<p>siblings（）是选取匹配元素前后所有同辈元素，与位置无关，结果不包含自己本身</p>
<h2 id="过滤选择器"><a href="#过滤选择器" class="headerlink" title="过滤选择器"></a>过滤选择器</h2><p>注意：在所有过滤选择其中，只有 :first和 :last和 :eq（index）返回单个元素，其他都返回集合元素</p>
<h3 id="基本过滤选择器-index从0算起"><a href="#基本过滤选择器-index从0算起" class="headerlink" title="基本过滤选择器(index从0算起)"></a>基本过滤选择器(index从0算起)</h3><h4 id="选择首元素"><a href="#选择首元素" class="headerlink" title="选择首元素"></a>选择首元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:first</div></pre></td></tr></table></figure>
<h4 id="选择末尾元素"><a href="#选择末尾元素" class="headerlink" title="选择末尾元素"></a>选择末尾元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:last</div></pre></td></tr></table></figure>
<h4 id="去除所有与给定选择器匹配的元素"><a href="#去除所有与给定选择器匹配的元素" class="headerlink" title="去除所有与给定选择器匹配的元素"></a>去除所有与给定选择器匹配的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:not(selector)</div></pre></td></tr></table></figure>
<h4 id="索引匹配"><a href="#索引匹配" class="headerlink" title="索引匹配"></a>索引匹配</h4><ul>
<li>索引从0开始，选择索引是偶数的所有元素 </li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:even <span class="comment">//若匹配元素的子元素也匹配，也要计算在内</span></div></pre></td></tr></table></figure>
<ul>
<li>选择索引是奇数的所有元素 </li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:odd</div></pre></td></tr></table></figure>
<ul>
<li>index从0开始，选择索引等于index的元素 </li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:eq(index)</div></pre></td></tr></table></figure>
<ul>
<li>index从0开始，选择索引大于index的元素 </li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:gt(index)     <span class="comment">// gt是greater than的缩写</span></div></pre></td></tr></table></figure>
<ul>
<li>index从0开始，选择索引小于index的元素 </li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:lt(index)</div></pre></td></tr></table></figure>
<h4 id="选取所有的标题元素"><a href="#选取所有的标题元素" class="headerlink" title="选取所有的标题元素"></a>选取所有的标题元素</h4><p>例如h1,h2,h3等<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:header</div></pre></td></tr></table></figure></p>
<h4 id="选取当前正在执行动画的函数"><a href="#选取当前正在执行动画的函数" class="headerlink" title="选取当前正在执行动画的函数"></a>选取当前正在执行动画的函数</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:animated</div></pre></td></tr></table></figure>
<h4 id="选取当前获取焦点的元素"><a href="#选取当前获取焦点的元素" class="headerlink" title="选取当前获取焦点的元素"></a>选取当前获取焦点的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:forcus</div></pre></td></tr></table></figure>
<h3 id="内容过滤选择器"><a href="#内容过滤选择器" class="headerlink" title="内容过滤选择器"></a>内容过滤选择器</h3><p>选取文本内容中含有text的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:contain(text)</div></pre></td></tr></table></figure></p>
<p>选取含有选择器所匹配的元素的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:has(selector)</div></pre></td></tr></table></figure></p>
<p>选取含有子元素或者文本的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:parent  <span class="comment">//将文本元素计算在内</span></div></pre></td></tr></table></figure></p>
<p>选取不包含子元素或者文本的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:empty  <span class="comment">//将文本元素计算在内</span></div></pre></td></tr></table></figure></p>
<h3 id="可见性过滤选择器"><a href="#可见性过滤选择器" class="headerlink" title="可见性过滤选择器"></a>可见性过滤选择器</h3><h4 id="选取所有不可见元素"><a href="#选取所有不可见元素" class="headerlink" title="选取所有不可见元素"></a>选取所有不可见元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:hidden    <span class="comment">//包括display：none 和 visibility：hidden</span></div></pre></td></tr></table></figure>
<h4 id="选取所有可见元素"><a href="#选取所有可见元素" class="headerlink" title="选取所有可见元素"></a>选取所有可见元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:visible</div></pre></td></tr></table></figure>
<h3 id="属性过滤选择器"><a href="#属性过滤选择器" class="headerlink" title="属性过滤选择器"></a>属性过滤选择器</h3><h4 id="选取拥有此属性的元素"><a href="#选取拥有此属性的元素" class="headerlink" title="选取拥有此属性的元素"></a>选取拥有此属性的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute]</div></pre></td></tr></table></figure>
<h4 id="选取属性值为value的元素"><a href="#选取属性值为value的元素" class="headerlink" title="选取属性值为value的元素"></a>选取属性值为value的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute=value]</div></pre></td></tr></table></figure>
<h4 id="选取属性值不为value的元素"><a href="#选取属性值不为value的元素" class="headerlink" title="选取属性值不为value的元素"></a>选取属性值不为value的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute!=value]</div></pre></td></tr></table></figure>
<h4 id="选取属性值以value开始的元素"><a href="#选取属性值以value开始的元素" class="headerlink" title="选取属性值以value开始的元素"></a>选取属性值以value开始的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute^=value]</div></pre></td></tr></table></figure>
<h4 id="选取属性值以value结束的元素"><a href="#选取属性值以value结束的元素" class="headerlink" title="选取属性值以value结束的元素"></a>选取属性值以value结束的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute$=value]</div></pre></td></tr></table></figure>
<h4 id="选取属性值中含value的元素"><a href="#选取属性值中含value的元素" class="headerlink" title="选取属性值中含value的元素"></a>选取属性值中含value的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute*=value]</div></pre></td></tr></table></figure>
<h4 id="选取属性值等于value或者以value作为前缀（value后跟着连字符“-”）的元素"><a href="#选取属性值等于value或者以value作为前缀（value后跟着连字符“-”）的元素" class="headerlink" title="选取属性值等于value或者以value作为前缀（value后跟着连字符“-”）的元素"></a>选取属性值等于value或者以value作为前缀（value后跟着连字符“-”）的元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute|=value]</div></pre></td></tr></table></figure>
<h4 id="用属性选择器合并成复合选择器"><a href="#用属性选择器合并成复合选择器" class="headerlink" title="用属性选择器合并成复合选择器"></a>用属性选择器合并成复合选择器</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[attribute1][attribute]...</div></pre></td></tr></table></figure>
<h3 id="子元素过滤选择器（index从1算起）"><a href="#子元素过滤选择器（index从1算起）" class="headerlink" title="子元素过滤选择器（index从1算起）"></a>子元素过滤选择器（index从1算起）</h3><h4 id="选取每个父元素下的第index个子元素或者奇偶元素（index从1算起）"><a href="#选取每个父元素下的第index个子元素或者奇偶元素（index从1算起）" class="headerlink" title="选取每个父元素下的第index个子元素或者奇偶元素（index从1算起）"></a>选取每个父元素下的第index个子元素或者奇偶元素（index从1算起）</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//除了这三个参数，也可以写n的表达式，比如3n+1，n从1开始</span></div><div class="line">:nth-child(index,odd,even)</div></pre></td></tr></table></figure>
<h4 id="选取每个父元素下的第一个子元素"><a href="#选取每个父元素下的第一个子元素" class="headerlink" title="选取每个父元素下的第一个子元素"></a>选取每个父元素下的第一个子元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:first-child</div></pre></td></tr></table></figure>
<h4 id="选取每个父元素下的最后一个子元素"><a href="#选取每个父元素下的最后一个子元素" class="headerlink" title="选取每个父元素下的最后一个子元素"></a>选取每个父元素下的最后一个子元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:last-child</div></pre></td></tr></table></figure>
<p><strong>这里与基本过滤选择器的：first和：last做对比，：first和：last只返回单个元素，且不是针对子元素。</strong></p>
<h4 id="选取某个元素是它父元素中的唯一的子元素"><a href="#选取某个元素是它父元素中的唯一的子元素" class="headerlink" title="选取某个元素是它父元素中的唯一的子元素"></a>选取某个元素是它父元素中的唯一的子元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:only-child</div></pre></td></tr></table></figure>
<h3 id="表单对象属性过滤选择器"><a href="#表单对象属性过滤选择器" class="headerlink" title="表单对象属性过滤选择器"></a>表单对象属性过滤选择器</h3><h4 id="选取所有可用元素"><a href="#选取所有可用元素" class="headerlink" title="选取所有可用元素"></a>选取所有可用元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">：enabled</div></pre></td></tr></table></figure>
<h4 id="选取所有不可用元素"><a href="#选取所有不可用元素" class="headerlink" title="选取所有不可用元素"></a>选取所有不可用元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">：disabled</div></pre></td></tr></table></figure>
<h4 id="选取所有被选中的元素（单选框、复选框）"><a href="#选取所有被选中的元素（单选框、复选框）" class="headerlink" title="选取所有被选中的元素（单选框、复选框）"></a>选取所有被选中的元素（单选框、复选框）</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">：checked</div></pre></td></tr></table></figure>
<h4 id="选取所有被选中的选项元素（下拉列表中）"><a href="#选取所有被选中的选项元素（下拉列表中）" class="headerlink" title="选取所有被选中的选项元素（下拉列表中）"></a>选取所有被选中的选项元素（下拉列表中）</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">：selected</div></pre></td></tr></table></figure>
<h2 id="表单对象选择器"><a href="#表单对象选择器" class="headerlink" title="表单对象选择器"></a>表单对象选择器</h2><h3 id="选取所有的input、textarea、select和button元素"><a href="#选取所有的input、textarea、select和button元素" class="headerlink" title="选取所有的input、textarea、select和button元素"></a>选取所有的input、textarea、select和button元素</h3><h4 id="匹配表单内上述4种元素"><a href="#匹配表单内上述4种元素" class="headerlink" title="匹配表单内上述4种元素"></a>匹配表单内上述4种元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.(<span class="string">'#form1:input'</span>)</div><div class="line"><span class="string">`</span></div></pre></td></tr></table></figure>
<h4 id="只匹配input元素"><a href="#只匹配input元素" class="headerlink" title="只匹配input元素"></a>只匹配input元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.(<span class="string">'#form1 input'</span>)</div><div class="line"><span class="string">`</span></div></pre></td></tr></table></figure>
<h4 id="匹配单行文本框"><a href="#匹配单行文本框" class="headerlink" title="匹配单行文本框"></a>匹配单行文本框</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:text</div></pre></td></tr></table></figure>
<h4 id="匹配所有密码框"><a href="#匹配所有密码框" class="headerlink" title="匹配所有密码框"></a>匹配所有密码框</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:password</div></pre></td></tr></table></figure>
<h4 id="匹配所有的单选框"><a href="#匹配所有的单选框" class="headerlink" title="匹配所有的单选框"></a>匹配所有的单选框</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:radio</div></pre></td></tr></table></figure>
<h4 id="匹配所有的多选框"><a href="#匹配所有的多选框" class="headerlink" title="匹配所有的多选框"></a>匹配所有的多选框</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:checkbox</div></pre></td></tr></table></figure>
<h4 id="匹配所有的提交按钮"><a href="#匹配所有的提交按钮" class="headerlink" title="匹配所有的提交按钮"></a>匹配所有的提交按钮</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:submit</div></pre></td></tr></table></figure>
<h4 id="匹配所有的图像按钮"><a href="#匹配所有的图像按钮" class="headerlink" title="匹配所有的图像按钮"></a>匹配所有的图像按钮</h4> <figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:image</div></pre></td></tr></table></figure>
<h4 id="匹配所有的重置按钮"><a href="#匹配所有的重置按钮" class="headerlink" title="匹配所有的重置按钮"></a>匹配所有的重置按钮</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:rest</div></pre></td></tr></table></figure>
<h4 id="匹配所有的按钮"><a href="#匹配所有的按钮" class="headerlink" title="匹配所有的按钮"></a>匹配所有的按钮</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:button</div></pre></td></tr></table></figure>
<h4 id="匹配所有的上传域"><a href="#匹配所有的上传域" class="headerlink" title="匹配所有的上传域"></a>匹配所有的上传域</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:file</div></pre></td></tr></table></figure>
<h4 id="匹配所有不可见元素"><a href="#匹配所有不可见元素" class="headerlink" title="匹配所有不可见元素"></a>匹配所有不可见元素</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">:hidden</div></pre></td></tr></table></figure>
<h1 id="jQuery事件总结"><a href="#jQuery事件总结" class="headerlink" title="jQuery事件总结"></a>jQuery事件总结</h1><h2 id="加载Dom事件"><a href="#加载Dom事件" class="headerlink" title="加载Dom事件"></a>加载Dom事件</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="built_in">window</span>).load() 或 $(<span class="built_in">document</span>).ready()</div></pre></td></tr></table></figure>
<h2 id="事件绑定"><a href="#事件绑定" class="headerlink" title="事件绑定"></a>事件绑定</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">常用绑定方法   bind(type [,data] , fn )             </div><div class="line"><span class="comment">//type类型有blue、focus、load、resize、scroll、unload、click、dbclick、mousedown、mouseup、mousemove、mouseover、mouseout、mouseenter、mouseleave、change、select、submit、keydown、keypress和error等</span></div></pre></td></tr></table></figure>
<h2 id="简写的事件"><a href="#简写的事件" class="headerlink" title="简写的事件"></a>简写的事件</h2><p>比如click，mouseover,和mouseout</p>
<h2 id="合成事件"><a href="#合成事件" class="headerlink" title="合成事件"></a>合成事件</h2><h3 id="hover-事件"><a href="#hover-事件" class="headerlink" title="hover() 事件"></a>hover() 事件</h3><p>hover()事件是mouseenter和mouseleave的合成事件<br>hover()和mouseover、mouseout有区别。后者光标进入绑定元素的子元素时，会触发mouseout</p>
<h3 id="toggle-fn1-fn2…"><a href="#toggle-fn1-fn2…" class="headerlink" title="toggle(fn1,fn2…)"></a>toggle(fn1,fn2…)</h3><p>用于模拟鼠标连续单击事件第一次触发fn1，第二次触发fn2。依次触发到最后一个，随后重复对这几个函数轮番调用</p>
<h2 id="事件冒泡–阻止事件冒泡–事件对象"><a href="#事件冒泡–阻止事件冒泡–事件对象" class="headerlink" title="事件冒泡–阻止事件冒泡–事件对象"></a>事件冒泡–阻止事件冒泡–事件对象</h2><p>jQuery中不支持事件捕获，需要事件捕获应用js原生方法</p>
<h3 id="阻止事件冒泡的方法"><a href="#阻止事件冒泡的方法" class="headerlink" title="阻止事件冒泡的方法"></a>阻止事件冒泡的方法</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">bind(type,<span class="function"><span class="keyword">function</span>(<span class="params">event</span>)) </span>&#123;&#125;      <span class="comment">//事件对象</span></div></pre></td></tr></table></figure>
<p>bind方法中传入event对象并使用event.stopPropagation()<br>注意：如果你使用jQuery1.7版本或者更新，请使用on()方法，jQuery对绑定进行了封装，替代了原来的delegate()、live()、bind()方法</p>
<h3 id="阻止默认行为"><a href="#阻止默认行为" class="headerlink" title="阻止默认行为"></a>阻止默认行为</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.preventDefault()</div></pre></td></tr></table></figure>
<h3 id="两者都阻止"><a href="#两者都阻止" class="headerlink" title="两者都阻止"></a>两者都阻止</h3><p>在事件处理函数中return false即可</p>
<h3 id="事件对象的属性"><a href="#事件对象的属性" class="headerlink" title="事件对象的属性"></a>事件对象的属性</h3><p>获取事件的类型<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.type</div></pre></td></tr></table></figure></p>
<p>阻止默认的事件行为<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.preventDefault()</div></pre></td></tr></table></figure></p>
<p>阻止事件的冒泡行为<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.stopPropagation</div></pre></td></tr></table></figure></p>
<p>获取触发事件的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.target</div></pre></td></tr></table></figure></p>
<p>获取光标相对于页面的x坐标和y坐标<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.pageX   和 event.pageY</div></pre></td></tr></table></figure></p>
<p>获取鼠标单击事件（click)中鼠标的左、中、右键<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">event.which分别返回<span class="number">1</span>、<span class="number">2</span>、<span class="number">3</span></div></pre></td></tr></table></figure></p>
<h2 id="移除事件"><a href="#移除事件" class="headerlink" title="移除事件"></a>移除事件</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.unbind( [type] [fn])</div></pre></td></tr></table></figure>
<p>注意：如果你使用jQuery1.7版本或者更新，off()替代unbind()方法</p>
<h3 id="模拟操作"><a href="#模拟操作" class="headerlink" title="模拟操作"></a>模拟操作</h3><p>有时需要模拟用户操作来达到单击效果，例如在用户进入页面后，就触发click事件，而不需要用户主动单击。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.trigger(type，[data]) <span class="comment">//触发绑定的事件，默认执行浏览器操作</span></div></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.triggerHandler()  <span class="comment">//  触发绑定的事件，而不执行浏览器默认操作</span></div></pre></td></tr></table></figure>
<h1 id="jQuery-Dom总结"><a href="#jQuery-Dom总结" class="headerlink" title="jQuery-Dom总结"></a>jQuery-Dom总结</h1><h2 id="基本操作–查找、创建、插入、删除、复制、替换、包裹（7种操作）"><a href="#基本操作–查找、创建、插入、删除、复制、替换、包裹（7种操作）" class="headerlink" title="基本操作–查找、创建、插入、删除、复制、替换、包裹（7种操作）"></a>基本操作–查找、创建、插入、删除、复制、替换、包裹（7种操作）</h2><h3 id="查找元素节点"><a href="#查找元素节点" class="headerlink" title="查找元素节点"></a>查找元素节点</h3><p>jQuery选择器</p>
<h3 id="查找属性节点"><a href="#查找属性节点" class="headerlink" title="查找属性节点"></a>查找属性节点</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.attr()</div></pre></td></tr></table></figure>
<h3 id="创建元素节点"><a href="#创建元素节点" class="headerlink" title="创建元素节点"></a>创建元素节点</h3><p>选择器的形式<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="string">"html"</span>)</div></pre></td></tr></table></figure></p>
<h3 id="创建文本节点-与元素节点一起创建"><a href="#创建文本节点-与元素节点一起创建" class="headerlink" title="创建文本节点 (与元素节点一起创建)"></a>创建文本节点 (与元素节点一起创建)</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="string">"html"</span>)  <span class="comment">//将文本包含在html中</span></div></pre></td></tr></table></figure>
<h3 id="创建属性节点-与元素节点一起创建"><a href="#创建属性节点-与元素节点一起创建" class="headerlink" title="创建属性节点(与元素节点一起创建)"></a>创建属性节点(与元素节点一起创建)</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="string">"html"</span>) <span class="comment">//为元素添加属性</span></div></pre></td></tr></table></figure>
<h3 id="追加元素"><a href="#追加元素" class="headerlink" title="追加元素"></a>追加元素</h3><h4 id="在每个匹配元素的内部追加内容"><a href="#在每个匹配元素的内部追加内容" class="headerlink" title="在每个匹配元素的内部追加内容"></a>在每个匹配元素的内部追加内容</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.append()  或$.appendTo()</div></pre></td></tr></table></figure>
<h4 id="在每个匹配元素的内部前置内容"><a href="#在每个匹配元素的内部前置内容" class="headerlink" title="在每个匹配元素的内部前置内容"></a>在每个匹配元素的内部前置内容</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.prepend()  或$.prependTo()</div></pre></td></tr></table></figure>
<h4 id="在每个匹配元素之后插入内容"><a href="#在每个匹配元素之后插入内容" class="headerlink" title="在每个匹配元素之后插入内容"></a>在每个匹配元素之后插入内容</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.after()        或$.insertAfter()</div></pre></td></tr></table></figure>
<h4 id="在每个匹配元素之前插入内容"><a href="#在每个匹配元素之前插入内容" class="headerlink" title="在每个匹配元素之前插入内容"></a>在每个匹配元素之前插入内容</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.before()    或$.insertBefore()</div></pre></td></tr></table></figure>
<h3 id="删除节点有三个方法"><a href="#删除节点有三个方法" class="headerlink" title="删除节点有三个方法"></a>删除节点有三个方法</h3><h4 id="remove-、detach-、empty"><a href="#remove-、detach-、empty" class="headerlink" title="remove()、detach()、empty()"></a>remove()、detach()、empty()</h4><p>empty()不是真正意义上的删除，只是清空节点内部内容</p>
<h4 id="remove-、detach"><a href="#remove-、detach" class="headerlink" title="remove()、detach()"></a>remove()、detach()</h4><p>相同点：清除后会返回该节点的引用，即可以继续插入该节点到Dom</p>
<p>不同点：detach()会保留节点绑定的事件、附加的数据</p>
<h3 id="复制节点"><a href="#复制节点" class="headerlink" title="复制节点"></a>复制节点</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.clone（[<span class="literal">true</span>]）           <span class="comment">//传入参数true可选，表示同时复制节点的绑定事件</span></div></pre></td></tr></table></figure>
<h3 id="替换节点"><a href="#替换节点" class="headerlink" title="替换节点"></a>替换节点</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.replaceWith()     和$.replaceAll()   <span class="comment">//前者A用B来替换   后者A替换所有的B</span></div></pre></td></tr></table></figure>
<h3 id="将匹配元素进行单独的包裹"><a href="#将匹配元素进行单独的包裹" class="headerlink" title="将匹配元素进行单独的包裹"></a>将匹配元素进行单独的包裹</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.wrap() <span class="comment">//A用B来包裹</span></div></pre></td></tr></table></figure>
<h3 id="将匹配元素用一个元素包裹"><a href="#将匹配元素用一个元素包裹" class="headerlink" title="将匹配元素用一个元素包裹"></a>将匹配元素用一个元素包裹</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.wrapAll 和 $.wrapInner</div></pre></td></tr></table></figure>
<h2 id="属性操作–获取与设置属性、删除属性"><a href="#属性操作–获取与设置属性、删除属性" class="headerlink" title="属性操作–获取与设置属性、删除属性"></a>属性操作–获取与设置属性、删除属性</h2><h3 id="获取和设置属性"><a href="#获取和设置属性" class="headerlink" title="获取和设置属性"></a>获取和设置属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.attr()   和    $.attr(&#123; &#125;)</div></pre></td></tr></table></figure>
<h3 id="删除属性"><a href="#删除属性" class="headerlink" title="删除属性"></a>删除属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.removeAttr()</div></pre></td></tr></table></figure>
<h2 id="样式操作–获取与设置样式、添加样式、删除样式、切换样式、判断是否含有某个样式"><a href="#样式操作–获取与设置样式、添加样式、删除样式、切换样式、判断是否含有某个样式" class="headerlink" title="样式操作–获取与设置样式、添加样式、删除样式、切换样式、判断是否含有某个样式"></a>样式操作–获取与设置样式、添加样式、删除样式、切换样式、判断是否含有某个样式</h2><h3 id="获取与设置样式"><a href="#获取与设置样式" class="headerlink" title="获取与设置样式"></a>获取与设置样式</h3><p>其实就是用属性操作中的$.attr()方法获或设置class的值</p>
<h3 id="追加样式"><a href="#追加样式" class="headerlink" title="追加样式"></a>追加样式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.addClass()            <span class="comment">//设置是替换，追加是保留原来的class基础上增加</span></div></pre></td></tr></table></figure>
<h3 id="移除样式"><a href="#移除样式" class="headerlink" title="移除样式"></a>移除样式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.removeClass([class1] [class2]..)       </div><div class="line"><span class="comment">//可以删除多个class，参数之间空格。无参数表示删除所有的class</span></div></pre></td></tr></table></figure>
<h3 id="切换样式"><a href="#切换样式" class="headerlink" title="切换样式"></a>切换样式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.toggleClass([class1] [class2].. )          <span class="comment">//原来有传入的样式名，则删除；无则添加</span></div></pre></td></tr></table></figure>
<h3 id="判断元素是否有某个样式"><a href="#判断元素是否有某个样式" class="headerlink" title="判断元素是否有某个样式"></a>判断元素是否有某个样式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.hasClass()        <span class="comment">//有则返回true  无则返回false</span></div></pre></td></tr></table></figure>
<h2 id="文本操作–设置与获取HTML、文本和值"><a href="#文本操作–设置与获取HTML、文本和值" class="headerlink" title="文本操作–设置与获取HTML、文本和值"></a>文本操作–设置与获取HTML、文本和值</h2><h3 id="设置与获取HTML"><a href="#设置与获取HTML" class="headerlink" title="设置与获取HTML"></a>设置与获取HTML</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.html()</div></pre></td></tr></table></figure>
<h3 id="文本"><a href="#文本" class="headerlink" title="文本"></a>文本</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.text()       <span class="comment">//通常用于内容标签</span></div></pre></td></tr></table></figure>
<h3 id="值"><a href="#值" class="headerlink" title="值"></a>值</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.val()         <span class="comment">//通常用于文本框 、下拉列表、单选框、多选框等；如果元素为多选，则返回一个包含所有值得数组</span></div></pre></td></tr></table></figure>
<h2 id="遍历访问节点（常用于事件处理中）"><a href="#遍历访问节点（常用于事件处理中）" class="headerlink" title="遍历访问节点（常用于事件处理中）"></a>遍历访问节点（常用于事件处理中）</h2><h3 id="获取匹配元素的所有子元素集合"><a href="#获取匹配元素的所有子元素集合" class="headerlink" title="获取匹配元素的所有子元素集合"></a>获取匹配元素的所有子元素集合</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.children()                 <span class="comment">//与$.parent()相反</span></div></pre></td></tr></table></figure>
<p>这里还有一个$.parents()的方法，获得每个匹配元素的祖先元素，与parent()不同，获得第一个父节点时并没有停止查找，而是继续查找最后返回多个节点</p>
<p>另一个$.closet()与$.parent()类似，不同的是closet()返回匹配元素的最近的一个父节点</p>
<h3 id="取得匹配元素后面紧邻的同辈元素"><a href="#取得匹配元素后面紧邻的同辈元素" class="headerlink" title="取得匹配元素后面紧邻的同辈元素"></a>取得匹配元素后面紧邻的同辈元素</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.next()            后面紧邻所有   $.nextAll()</div></pre></td></tr></table></figure>
<p>取得匹配元素前面紧邻的同辈元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.prev()            前面紧邻所有   $.prevAll()</div></pre></td></tr></table></figure></p>
<p>取得匹配元素前后所有的同辈元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.siblings()</div></pre></td></tr></table></figure></p>
<h3 id="在当前选中元素中找到符合条件的后代，返回的是子元素"><a href="#在当前选中元素中找到符合条件的后代，返回的是子元素" class="headerlink" title="在当前选中元素中找到符合条件的后代，返回的是子元素"></a>在当前选中元素中找到符合条件的后代，返回的是子元素</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.find()</div></pre></td></tr></table></figure>
<h3 id="在当前选中元素中找到符合条件的当前元素集合，返回的是当前元素"><a href="#在当前选中元素中找到符合条件的当前元素集合，返回的是当前元素" class="headerlink" title="在当前选中元素中找到符合条件的当前元素集合，返回的是当前元素"></a>在当前选中元素中找到符合条件的当前元素集合，返回的是当前元素</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.filter()</div></pre></td></tr></table></figure>
<h3 id="对-jQuery-对象进行迭代，为每个匹配元素执行函数"><a href="#对-jQuery-对象进行迭代，为每个匹配元素执行函数" class="headerlink" title="对 jQuery 对象进行迭代，为每个匹配元素执行函数"></a>对 jQuery 对象进行迭代，为每个匹配元素执行函数</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.each(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;)</div></pre></td></tr></table></figure>
<p>…省略的许多方法与jQuery选择器类似</p>
<h2 id="CSS-DOM操作"><a href="#CSS-DOM操作" class="headerlink" title="CSS-DOM操作"></a>CSS-DOM操作</h2><h3 id="获取或设置元素的样式属性"><a href="#获取或设置元素的样式属性" class="headerlink" title="获取或设置元素的样式属性"></a>获取或设置元素的样式属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.css()</div></pre></td></tr></table></figure>
<h3 id="获取或设置元素的宽度或高度"><a href="#获取或设置元素的宽度或高度" class="headerlink" title="获取或设置元素的宽度或高度"></a>获取或设置元素的宽度或高度</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$. width(value)和$.height(value)<span class="comment">//  $. css("width")也可以获取元素宽度，但与元素的样式设置有关，可能获取到为auto；</span></div></pre></td></tr></table></figure>
<p>而$.width()获取的是元素的实际宽度</p>
<h3 id="关于元素定位常用的方法"><a href="#关于元素定位常用的方法" class="headerlink" title="关于元素定位常用的方法"></a>关于元素定位常用的方法</h3><h4 id="获取元素在当前视窗的相对偏移"><a href="#获取元素在当前视窗的相对偏移" class="headerlink" title="获取元素在当前视窗的相对偏移"></a>获取元素在当前视窗的相对偏移</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.offset().dir    <span class="comment">//dir 可以为top right bottom left</span></div></pre></td></tr></table></figure>
<h4 id="获取元素相对于最近一个position样式属性设置为relative或者absolute父节点的相对偏移"><a href="#获取元素相对于最近一个position样式属性设置为relative或者absolute父节点的相对偏移" class="headerlink" title="获取元素相对于最近一个position样式属性设置为relative或者absolute父节点的相对偏移"></a>获取元素相对于最近一个position样式属性设置为relative或者absolute父节点的相对偏移</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.position().dir  <span class="comment">//dir 可以为top right bottom left</span></div></pre></td></tr></table></figure>
<h4 id="获取元素的滚动条（假如有的话，通常是那些设置了overflow：auto的元素，而内容过长）距离顶端或左侧的距离"><a href="#获取元素的滚动条（假如有的话，通常是那些设置了overflow：auto的元素，而内容过长）距离顶端或左侧的距离" class="headerlink" title="获取元素的滚动条（假如有的话，通常是那些设置了overflow：auto的元素，而内容过长）距离顶端或左侧的距离"></a>获取元素的滚动条（假如有的话，通常是那些设置了overflow：auto的元素，而内容过长）距离顶端或左侧的距离</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.scrollTop()</div></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.scrollLeft()       <span class="comment">//通常上下布局的页面document一般都会有滚动条，这时候使用  $(document).scrollTop()可以很方便获取滚动条里页面顶部的距离</span></div></pre></td></tr></table></figure>
<h1 id="jQuery动画总结"><a href="#jQuery动画总结" class="headerlink" title="jQuery动画总结"></a>jQuery动画总结</h1><h2 id="基本动画-（速度参数-slow-normal-fast-自定义毫秒数）"><a href="#基本动画-（速度参数-slow-normal-fast-自定义毫秒数）" class="headerlink" title="基本动画    （速度参数 slow  normal  fast  自定义毫秒数）"></a>基本动画    （速度参数 slow  normal  fast  自定义毫秒数）</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">show()和hide()   <span class="comment">//显示隐藏 两者组合 toggle()</span></div></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">fadeIn() 和 fadeOut() <span class="comment">//淡入淡出两者组合 fadeToggle()</span></div></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">slideDown() 和slideUp  <span class="comment">//向下滑动显示  向上滑动隐藏两者组合 slideToggle()</span></div></pre></td></tr></table></figure>
<h2 id="自定义动画"><a href="#自定义动画" class="headerlink" title="自定义动画"></a>自定义动画</h2><p>注意: css()方法并不会加入动画队列中，而会在动画开始就立即执行，要在动画执行完成后改变css样式，可以将css()写入回调函数中</p>
<p>animate 同时进行的动画    只需传入多个键值对<br>animate 进行分步动画      只需按jQuery链式写法就好<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">animate(&#123; &#125;,speed,callback)  <span class="comment">//实现累加累减动画只需在px前用+=或-=</span></div></pre></td></tr></table></figure></p>
<h2 id="停止正在进行的动画"><a href="#停止正在进行的动画" class="headerlink" title="停止正在进行的动画"></a>停止正在进行的动画</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">stop([clearQueue],[gotoEnd])     <span class="comment">//默认情况下  clearQueue 接gotoEnd都是false</span></div></pre></td></tr></table></figure>
<p>要点：</p>
<ul>
<li><p>stop()  停止当前正在进行的动画(只阻止链式动画队列中当前运行的那一步动画)</p>
</li>
<li><p>stop(true，false)  停止当前的动画队列(正在进行+未进行，即链式动画队列)</p>
<p>使用情况：比如用户将光标移入某个元素，动画没结束就移出了。</p>
</li>
<li><p>stop(true，true)  停止当前的动画队列，并让动画直接到达结束状态</p>
<p>使用情况： 通常用于后一个动画需要基于前一个动画的末状态的情况</p>
</li>
</ul>
<h2 id="判断元素是否处于动画状态（避免动画累计导致和用户的行为不一致）"><a href="#判断元素是否处于动画状态（避免动画累计导致和用户的行为不一致）" class="headerlink" title="判断元素是否处于动画状态（避免动画累计导致和用户的行为不一致）"></a>判断元素是否处于动画状态（避免动画累计导致和用户的行为不一致）</h2><p>常用方法：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">if</span>(! $(element).is(<span class="string">":animated"</span>))&#123;</div><div class="line"></div><div class="line"><span class="comment">//如果当前没有进行动画，则添加新动画</span></div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="延迟动画的方法"><a href="#延迟动画的方法" class="headerlink" title="延迟动画的方法"></a>延迟动画的方法</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">delay(毫秒)</div></pre></td></tr></table></figure>
<h1 id="jQuery-ajax"><a href="#jQuery-ajax" class="headerlink" title="jQuery-ajax"></a>jQuery-ajax</h1><p>说明：ajax仅做简要总结，更多可参考<a href="http://www.jquery123.com/" target="_blank" rel="external">jQuery中文参考文档</a>,包含了所有新版API或弃用API的说明。</p>
<h2 id="jQuery-ajax-setting-方法"><a href="#jQuery-ajax-setting-方法" class="headerlink" title="jQuery.ajax([setting])方法"></a>jQuery.ajax([setting])方法</h2><h3 id="1-5-0以前版本"><a href="#1-5-0以前版本" class="headerlink" title="1.5.0以前版本"></a>1.5.0以前版本</h3><p>返回的是XHR对象 </p>
<ul>
<li><strong>type</strong>：类型， “POST”或“GET”，默认为“Get”</li>
<li><strong>url</strong>：发送请求的地址</li>
<li><strong>data</strong>：是一个对象，连同请求发送到服务器的数据</li>
<li><strong>dataType</strong>： 预期服务器返回的数据类型。如果不指定，jQuery将自动根据HTTP包MIME信息来只能判断，一般采用j- son格式，可以设置为”json”</li>
<li><strong>success</strong>：是一个方法，请求成功后的回调函数。传入返回后的数据，以及包含成功代码的字符串</li>
<li><strong>error</strong>：是一个方法，请求失败时调用此函数。传入XMLHttpRequest对象</li>
</ul>
<h3 id="1-5-0及更新版本"><a href="#1-5-0及更新版本" class="headerlink" title="1.5.0及更新版本"></a>1.5.0及更新版本</h3><p>jQuery使用原生promise对象封装了一个deffered对象，因此你可以新版可以写成链式回调函数<br>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$.ajax(<span class="string">"test.html"</span>)</div><div class="line">　.done(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; alert(<span class="string">"yes"</span>); &#125;)</div><div class="line">　.fail(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; alert(<span class="string">"no"</span>); &#125;);</div></pre></td></tr></table></figure>
<p>done()相当于success方法，fail()相当于error方法。采用链式写法以后，代码的可读性大大提高。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>Javascript数组方法</title>
    <url>/2016/06/14/js-array-method/</url>
    <content><![CDATA[<h1 id="ECMAScript3定义的数组方法"><a href="#ECMAScript3定义的数组方法" class="headerlink" title="ECMAScript3定义的数组方法"></a>ECMAScript3定义的数组方法</h1><ul>
<li><strong>转换</strong>：join()</li>
<li><strong>排序</strong>: reverse()、sort()</li>
<li><strong>连接</strong>：concat()</li>
<li><strong>裁剪</strong>: slice()</li>
<li><strong>插入、删除</strong>：splice()、push()、pop()、unshift()、shift()</li>
<li><strong>对象方法</strong>：toString()、toLocaleString()</li>
</ul>
<h2 id="join"><a href="#join" class="headerlink" title="join()"></a>join()</h2><p>将数组中所有元素转化为字符串并连接在一起，返回最后生成的字符串。可以指定一个可选的字符串在生成的字符串中来分隔数组中的各个元素。<strong>如果不指定分隔符，默认使用逗号</strong>。</p>
<h2 id="reverse"><a href="#reverse" class="headerlink" title="reverse()"></a>reverse()</h2><p>将数组中的元素顺序颠倒，返回逆序的数组。它<strong>不通过创建新的数组重新排列，而是在原先的数组中排列它们</strong>。</p>
<h2 id="sort"><a href="#sort" class="headerlink" title="sort()"></a>sort()</h2><p><strong>将数组中的元素排序并返回排序后的数组</strong>。当不带参数调用sort()时，数组元素以字母表顺序排序。<br>如果包含undefined的元素，它们会被排到数组的尾部。<br>如果要按照其他方式排序，必须给sort()方法传递一个比较函数，该函数的返回值决定了该函数所传入参数a,b在排序好的数组中的先后顺序。假设第一个参数在前，则应该返回一个小于0的数；相反，则返回一个大于0的数。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//按数值升序</span></div><div class="line">a.sort(<span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> a-b;</div><div class="line">&#125;)</div><div class="line"><span class="comment">//按数组降序</span></div><div class="line">a.sort(<span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> b-a;</div><div class="line">&#125;)</div><div class="line"><span class="comment">//随机排序</span></div><div class="line">a.sort(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> <span class="number">0.5</span>-<span class="built_in">Math</span>.random();</div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<h2 id="concat"><a href="#concat" class="headerlink" title="concat()"></a>concat()</h2><p>Array.concat()方法<strong>创建并返回一个新的数组</strong>，元素包括调用concat()的原始数组的元素和concat()的每个参数。如果这些参数中的任何一个自身是数组，则连接的是数组的元素；但是，concat<br>()不会扁平化数组的数组，可以理解为<strong>只能解剖一层</strong>。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line">a.concat(<span class="number">4</span>,<span class="number">5</span>);            <span class="comment">//返回[1,2,3,4,5]</span></div><div class="line">a.concat([<span class="number">4</span>,<span class="number">5</span>]);		  <span class="comment">//返回[1,2,3,4,5]</span></div><div class="line">a.concat([<span class="number">4</span>,<span class="number">5</span>],[<span class="number">6</span>,<span class="number">7</span>])     <span class="comment">//返回[1,2,3,4,5,6,7]</span></div><div class="line">a.concat(<span class="number">4</span>,[<span class="number">5</span>,[<span class="number">6</span>,<span class="number">7</span>]])     <span class="comment">//返回[1,2,3,4,5,[6,7]]</span></div></pre></td></tr></table></figure></p>
<p>由于返回的是新数组，该方法可用于<strong>实现数组复制</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> b=a.concat();</div><div class="line"><span class="built_in">console</span>.log(b)            <span class="comment">//返回[1,2,3]</span></div></pre></td></tr></table></figure></p>
<h2 id="slice"><a href="#slice" class="headerlink" title="slice()"></a>slice()</h2><p>Array.slice()方法<strong>创建并返回指定数组的子数组</strong>。<br>slice(start,end)  参数start为必须，end为可选，分别指定截取的数组的起、止位置；如果出现负数，它表示相对于数组最后一个元素的位置。<br>slice(start)   截取start开始到数组结束的所有元素<br>slice(start,end) 截取[start,end)闭开区间的数组元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];       </div><div class="line">a.slice(<span class="number">0</span>,<span class="number">3</span>);        <span class="comment">//返回[1,2,3]</span></div><div class="line">a.slice(<span class="number">3</span>);			 <span class="comment">//返回[4,5]</span></div><div class="line">a.slice(<span class="number">1</span>,<span class="number">-1</span>);	 	 <span class="comment">//返回[2,3,4]</span></div><div class="line">a.slice(<span class="number">-3</span>,<span class="number">-2</span>);		 <span class="comment">//返回[3]</span></div></pre></td></tr></table></figure></p>
<p>slice()方法可用于实现数组复制<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a.slice(<span class="number">0</span>);          <span class="comment">//返回[1,2,3,4,5]</span></div></pre></td></tr></table></figure></p>
<h2 id="splice"><a href="#splice" class="headerlink" title="splice()"></a>splice()</h2><p>Array.splice()是<strong>数组中插入或删除元素的通用方法</strong>。<strong>不同于concat()和slice()，splice()会修改调用的数组</strong><br>splice(index,howmany,item1,item2…)<br>第一个参数指定了插入或删除的<strong>起始位置</strong><br>第二个参数指定了应该从数组中<strong>删除的元素的个数</strong>，如果省略，从起始点开始到数组结尾的所有元素都将被删除<br>返回值：<strong>splice()返回一个由删除元素组成的数组，或者没有删除元素，就返回空数组</strong>。<br>删除：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</div><div class="line">a.splice(<span class="number">3</span>);           <span class="comment">//返回[4,5],a是[1,2,3]</span></div><div class="line">a.splice(<span class="number">1</span>,<span class="number">2</span>);		   <span class="comment">//返回[2,3],a是[1]</span></div></pre></td></tr></table></figure></p>
<p>插入：注意区别于concat()，splice()会插入数组本身而非数组的元素<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</div><div class="line">a.splice(<span class="number">2</span>,<span class="number">0</span>,<span class="string">'a'</span>,<span class="string">'b'</span>)  <span class="comment">//返回[],a是[1,2,'a','b',3,4,5]</span></div><div class="line">a.splice(<span class="number">2</span>,<span class="number">2</span>,[<span class="number">1</span>,<span class="number">2</span>],<span class="number">3</span>)  <span class="comment">//返回[3,4],a是[1,2,[1,2],3,4,5]</span></div></pre></td></tr></table></figure></p>
<h2 id="push-、pop-、unshift-、shift"><a href="#push-、pop-、unshift-、shift" class="headerlink" title="push()、pop()、unshift()、shift()"></a>push()、pop()、unshift()、shift()</h2><p>在<strong>数组的尾部</strong>插入元素、删除元素：<br>push()、pop()<br>在<strong>数组的头部</strong>插入元素、删除元素：<br>unshift()、shift()<br><strong>这些方法都可以被splice()替代</strong>；</p>
<h2 id="toString-和toLocaleString"><a href="#toString-和toLocaleString" class="headerlink" title="toString()和toLocaleString()"></a>toString()和toLocaleString()</h2><p><strong>toString()方法将数组的每个元素转化为字符串，返回用逗号分隔的字符串列表</strong>。注意，输出不包括方括号或其他任何形式包裹数组数值的分隔符。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>].toString();       <span class="comment">//返回'1,2,3'</span></div><div class="line">[<span class="string">"a"</span>,<span class="string">"b"</span>,<span class="string">"c"</span>].toString(); <span class="comment">//返回'a,b,c'</span></div><div class="line">[<span class="number">1</span>,[<span class="number">2</span>,<span class="string">'c'</span>]].toString();   <span class="comment">//返回'1,2,c'</span></div></pre></td></tr></table></figure></p>
<p>这里与不使用任何参数调用join()方法返回的字符串是一样的。</p>
<p><strong>toLocaleString()是数组对象的本地字符串表示</strong>，返回结果随机器不同而不同，最好不要在脚本中用来基本运算。</p>
<h1 id="ECMAScript5中的数组方法"><a href="#ECMAScript5中的数组方法" class="headerlink" title="ECMAScript5中的数组方法"></a>ECMAScript5中的数组方法</h1><p>ECMAScript5中数组方法概述：<br>大多数方法<strong>第一个参数接收一个函数</strong>，<strong>并且对数组中的每个元素调用一次该函数</strong>。对不存在的元素不调用该函数。<br>大多数情况下，调用提供的函数使用三个参数：<strong>数组元素、元素的索引和数组本身</strong>，通常只需要使用第一个参数，忽略后面两个参数。<br>大多数ECMASscript数组方法第一个参数是一个函数，第二个参数可选。如果有第二个参数，则调用的函数被看成第二个参数的方法，即第二个参数可以在调用函数时将作为它的this关键字的值来使用，起到修改指针的作用。</p>
<h2 id="forEach"><a href="#forEach" class="headerlink" title="forEach()"></a>forEach()</h2><p>array1.forEach(callbackfn[, thisArg])</p>
<table>
<thead>
<tr>
<th>参数</th>
<th style="text-align:center">定义</th>
</tr>
</thead>
<tbody>
<tr>
<td>array1</td>
<td style="text-align:center">必需。 一个数组对象。</td>
</tr>
<tr>
<td>callbackfn</td>
<td style="text-align:center">必需。 一个接受最多三个参数的函数。 对于数组中的每个元素，forEach 都会调用 callbackfn 函数一次。</td>
</tr>
<tr>
<td>thisArg可选</td>
<td style="text-align:center">可在 callbackfn 函数中为其引用 this 关键字的对象。 如果省略 thisArg，则 undefined 将用作 this 值。</td>
</tr>
</tbody>
</table>
<p>如果 callbackfn 参数不是函数对象，则将引发 TypeError 异常。</p>
<p>对于数组中的每个元素，forEach 方法都会调用 callbackfn 函数一次（采用升序索引顺序）。 不为数组中缺少的元素调用该回调函数。</p>
<p>除了数组对象之外，forEach 方法可由具有 length 属性且具有已按数字编制索引的属性名的任何对象使用。</p>
<h2 id="map"><a href="#map" class="headerlink" title="map()"></a>map()</h2><p>map()方法将调用数组的每个元素传递给指定的函数，并返回一个数组，它包含该函数的返回值。<br>map()方法返回的是新数组，<strong>它不修改调用的数组</strong>；如果是稀疏数组，返回的也是相同方式的稀疏数组。它具有相同的长度，相同的缺失元素。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line">b=a.map(<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> x*x;          <span class="comment">//b是[1,4,9]    </span></div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<h2 id="filter"><a href="#filter" class="headerlink" title="filter()"></a>filter()</h2><p>filter()方法创建并返回调用数组的一个子集。<strong>传递的函数用来进行逻辑判定</strong>的，该函数返回true或false。<br>返回true的元素将被添加成为这个子集的成员。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</div><div class="line">b=a.filter(<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> x&gt;<span class="number">3</span></div><div class="line">&#125;)                 <span class="comment">//b是[4,5]</span></div><div class="line">c=a.filter(<span class="function"><span class="keyword">function</span>(<span class="params">x,i</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> i%<span class="number">2</span>==<span class="number">0</span>；</div><div class="line">&#125;)                 <span class="comment">//c是[1,3,5]</span></div></pre></td></tr></table></figure></p>
<h2 id="every-和some-方法"><a href="#every-和some-方法" class="headerlink" title="every()和some()方法"></a>every()和some()方法</h2><p>所有：every()每个数组元素回调函数都返回true的时候才会返回true，当遇到false的时候终止执行，返回false；<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</div><div class="line">a.every(<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> x &lt; <span class="number">10</span>;      <span class="comment">//返回true</span></div><div class="line">&#125;);</div><div class="line">a.every(<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>;  <span class="comment">//返回false,并非所有元素都能被2整除   </span></div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<p>存在：some()<br>存在有一个数组元素的回调函数返回true的时候终止执行并返回true，否则返回false。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">a=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</div><div class="line">a.some(<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>;  <span class="comment">//返回true </span></div><div class="line">&#125;);</div><div class="line">a.some(<span class="built_in">isNaN</span>);    <span class="comment">//false</span></div></pre></td></tr></table></figure></p>
<h2 id="reduce-和reduceRight-方法"><a href="#reduce-和reduceRight-方法" class="headerlink" title="reduce()和reduceRight()方法"></a>reduce()和reduceRight()方法</h2><p>遍历数组，调用回调函数，将数组元素组合成一个值<br>参数1：回调函数：把两个值合为一个<br>参数2：value，一个初始值，可选<br>reduce从索引最小值开始<br>reduceRight反向，方法有两个参数<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=<span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>);</div><div class="line"><span class="built_in">console</span>.log(a.reduce(<span class="function"><span class="keyword">function</span>(<span class="params">v1,v2</span>)</span>&#123;</div><div class="line">   <span class="keyword">return</span> v1+v2;</div><div class="line">&#125;));              <span class="comment">// 21</span></div><div class="line"><span class="built_in">console</span>.log(a.reduceRight(<span class="function"><span class="keyword">function</span>(<span class="params">v1,v2</span>)</span>&#123;</div><div class="line">   <span class="keyword">return</span> v1-v2;</div><div class="line">&#125;,<span class="number">100</span>));          <span class="comment">// 79</span></div></pre></td></tr></table></figure></p>
<h2 id="indexOf-lastIndexOf-方法"><a href="#indexOf-lastIndexOf-方法" class="headerlink" title="indexOf()lastIndexOf()方法"></a>indexOf()lastIndexOf()方法</h2><p>用于查找数组内指定元素位置，查找到第一个后返回其索引，没有查找到返回-1，indexOf从头至尾搜索，lastIndexOf反向搜索。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> a=<span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>);</div><div class="line"><span class="built_in">console</span>.log(a.indexOf(<span class="number">2</span>));     <span class="comment">//1</span></div><div class="line"><span class="built_in">console</span>.log(a.lastIndexOf(<span class="number">2</span>)); <span class="comment">//4</span></div><div class="line"><span class="built_in">console</span>.log(a.IndexOf(<span class="number">4</span>));     <span class="comment">//-1，找不到</span></div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>数组</tag>
      </tags>
  </entry>
  <entry>
    <title>javascript中的call()和apply()方法</title>
    <url>/2016/06/15/js-call-and-apply/</url>
    <content><![CDATA[<p>我们知道函数调用有四种方式：<strong>直接调用、作为对象的方法调用、构造函数调用、间接调用</strong>。<br>而<strong>call()和apply()就是间接调用函数的方法</strong>。这两个方法都允许显示的指示调用上下文(this)的值，也就是说，任何函数可以作为任何对象的方法来调用，哪怕这个函数不是那个对象的方法。</p>
<p>假定有个函数f(),要想以对象o的方法来调用，可以这样使用call()和apply()<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">f.call(o);</div><div class="line">f.apply(o);</div></pre></td></tr></table></figure></p>
<p>每行代码的功能类似于(假设对象o预先不存在名为m的属性)<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">o.m=f;       <span class="comment">//将f存储为临时方法</span></div><div class="line">o.m();       <span class="comment">//调用</span></div><div class="line"><span class="keyword">delete</span> o.m;  <span class="comment">//删除临时方法</span></div></pre></td></tr></table></figure></p>
<h2 id="call"><a href="#call" class="headerlink" title="call()"></a>call()</h2><p>语法：<strong>call(thisObj, arg1, arg2 …argN)</strong><br>定义：<strong>调用一个对象的一个方法，以另一个对象替换当前对象</strong>。<br>说明：<br>1、call 方法可以用来代替另一个对象调用一个方法。<br>2、call 方法可将一个函数的对象上下文从初始的上下文改变为由 thisObj 指定的新对象。<br>3、如果没有提供 thisObj 参数（或者传入null、undefined），那么 Global 对象被用作 thisObj；ES5严格模式则将默认为this的值。<br>4、arg1、arg2..是待传入调用函数的实参，以逗号分隔。</p>
<h2 id="apply"><a href="#apply" class="headerlink" title="apply()"></a>apply()</h2><p>语法：<strong>apply(thisObj,argArray)</strong><br>定义：<strong>应用某一对象的一个方法，用另一个对象替换当前对象</strong>。<br>说明：<br>1、如果 argArray 不是一个有效的数组或者不是 arguments 对象，那么将导致一个 TypeError。<br>2、如果没有提供 argArray 和 thisObj 任何一个参数，那么 Global 对象将被用作 thisObj， 并且无法被传递任何参数。<br>3、argArray是待传入调用函数的参数组成的数组</p>
<h3 id="关于apply-的奇淫巧计"><a href="#关于apply-的奇淫巧计" class="headerlink" title="关于apply()的奇淫巧计"></a>关于apply()的奇淫巧计</h3><h4 id="apply-用来求数组中最大元素"><a href="#apply-用来求数组中最大元素" class="headerlink" title="apply()用来求数组中最大元素"></a>apply()用来求数组中最大元素</h4><p>由于Math.max()只能传入逗号分隔的参数，而apply()的第二个参数恰好可以传入数组或类数组元素。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> bigest=<span class="built_in">Math</span>.max.apply(<span class="built_in">Math</span>, array_of_numbers)</div></pre></td></tr></table></figure></p>
<h4 id="Array-prototype-push-可以实现两个数组合并"><a href="#Array-prototype-push-可以实现两个数组合并" class="headerlink" title="Array.prototype.push 可以实现两个数组合并"></a>Array.prototype.push 可以实现两个数组合并</h4><p>push方法没有提供push一个数组,但是它提供了push(param1,param,…paramN) 所以同样也可以通过apply来使用数组作为参数<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">arr1=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line">arr2=[<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</div><div class="line"><span class="built_in">Array</span>.prototype.push.apply(arr1,arr2);</div></pre></td></tr></table></figure></p>
<h2 id="常用实例"><a href="#常用实例" class="headerlink" title="常用实例"></a>常用实例</h2><h3 id="将某个对象的方法临时借给另一个对象调用"><a href="#将某个对象的方法临时借给另一个对象调用" class="headerlink" title="将某个对象的方法临时借给另一个对象调用"></a>将某个对象的方法临时借给另一个对象调用</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params"></span>)</span>&#123;    </div><div class="line">    <span class="keyword">this</span>.name = <span class="string">"Animal"</span>;    </div><div class="line">    <span class="keyword">this</span>.showName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;    </div><div class="line">        alert(<span class="keyword">this</span>.name);    </div><div class="line">    &#125;    </div><div class="line">&#125;    </div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span>(<span class="params"></span>)</span>&#123;    </div><div class="line">    <span class="keyword">this</span>.name = <span class="string">"Cat"</span>;    </div><div class="line">&#125;    </div><div class="line"><span class="keyword">var</span> animal = <span class="keyword">new</span> Animal();    </div><div class="line"><span class="keyword">var</span> cat = <span class="keyword">new</span> Cat();    </div><div class="line"><span class="comment">//通过call或apply方法，将原本属于Animal对象的showName()方法交给对象cat来使用了。    </span></div><div class="line"><span class="comment">//输入结果为"Cat"    </span></div><div class="line">animal.showName.call(cat,<span class="string">","</span>);    </div><div class="line"><span class="comment">//animal.showName.apply(cat,[]);</span></div></pre></td></tr></table></figure>
<h3 id="实现继承"><a href="#实现继承" class="headerlink" title="实现继承"></a>实现继承</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params">name</span>)</span>&#123;      </div><div class="line">    <span class="keyword">this</span>.name = name;      </div><div class="line">    <span class="keyword">this</span>.showName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;      </div><div class="line">        alert(<span class="keyword">this</span>.name);      </div><div class="line">    &#125;      </div><div class="line">&#125;      </div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Cat</span>(<span class="params">name</span>)</span>&#123;    </div><div class="line">    Animal.call(<span class="keyword">this</span>, name);      <span class="comment">//将Anima函数临时作为this的方法进行调用，并且传入Cat方法的参数name</span></div><div class="line">&#125;           </div><div class="line"><span class="keyword">var</span> cat = <span class="keyword">new</span> Cat(<span class="string">"Black Cat"</span>);   <span class="comment">//使用new调用Cat函数之后，this指向实例出来的对象cat；如果不使用new调用，则this指向全局对象。</span></div><div class="line">cat.showName();       <span class="comment">//"Black Cat"</span></div></pre></td></tr></table></figure>
<h3 id="多重继承"><a href="#多重继承" class="headerlink" title="多重继承"></a>多重继承</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Class1</span>(<span class="params"></span>)  </span></div><div class="line">&#123;  </div><div class="line">    <span class="keyword">this</span>.showSub = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)  </span></div><div class="line">    &#123;  </div><div class="line">        alert(a-b);  </div><div class="line">    &#125;  </div><div class="line">&#125;;   </div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Class2</span>(<span class="params"></span>)  </span></div><div class="line">&#123;  </div><div class="line">    <span class="keyword">this</span>.showAdd = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)  </span></div><div class="line">    &#123;  </div><div class="line">        alert(a+b);  </div><div class="line">    &#125;  </div><div class="line">&#125;;   </div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Class3</span>(<span class="params"></span>)  </span></div><div class="line">&#123;  </div><div class="line">    Class1.call(<span class="keyword">this</span>);  </div><div class="line">    Class2.call(<span class="keyword">this</span>);  </div><div class="line">&#125;;</div><div class="line"><span class="keyword">var</span> obj=<span class="keyword">new</span> Class3();   <span class="comment">//此时obj就多了showSub和showAdd的方法</span></div></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript获取DOM尺寸大小和元素位置</title>
    <url>/2016/06/15/js-dom-elepos-and-elesize/</url>
    <content><![CDATA[<p>JavaScript处理一些DOM元素的动态效果经常需要一些元素的尺寸大小和位置。body节点主要用于获取页面宽高和滚动条的滚动距离，以下出现body节点可以替换为其他DOM元素表示在该元素下获取或设置这些属性。作为HTML元素，都具有下列属性：<br><img src="http://qiniu.huzerui.com/image/2016-06-15-js-get-dom-position-and-size-1.jpg" alt="大小、位置属性"><br>这些属性的笼统特征：<br>client：元素内容+内边距 大小，不包括边框、外边距、滚动条部分<br>offset：元素内容+内边距+边框  不包括外边距和滚动条部分<br>scroll：与元素的滚动相关</p>
<h1 id="尺寸大小"><a href="#尺寸大小" class="headerlink" title="尺寸大小"></a>尺寸大小</h1><p>一张网页的全部面积，就是它的大小。通常情况下，网页的大小由内容和CSS样式表决定。<br>视口是指在浏览器窗口中看到的那部分网页面积。</p>
<h2 id="获取网页的大小（都不包括外边距、滚动条）"><a href="#获取网页的大小（都不包括外边距、滚动条）" class="headerlink" title="获取网页的大小（都不包括外边距、滚动条）"></a>获取网页的大小（都不包括外边距、滚动条）</h2><p><strong>宽（值随窗口大小发生改变）：</strong> document.body.clientWidth（不包括border，只包括内容+padding）<br>或者 document.body.offsetWidth（包括border）<br>或者 document.body.scrollWidth<br><strong>高(值固定)：</strong> document.body.clientHeigth（不包括border，只包括内容+padding）<br>或者 document.body.offsetHeight（包括border）<br>或者 document.body.scrollHeight<br><strong>说明：</strong>通常使用document.body.scrollWidth和document.body.scrollHeight</p>
<h2 id="获取屏幕相关属性"><a href="#获取屏幕相关属性" class="headerlink" title="获取屏幕相关属性"></a>获取屏幕相关属性</h2><p>使用window.screen对象的属性。这些属性有个特点，不会随着窗口的伸缩而改变数值<br><strong>屏幕分辨率的高：</strong>window.screen.height<br><strong>屏幕分辨率的宽：</strong>window.screen.width<br><strong>屏幕可用工作区高度：</strong>window.screen.availHeight<br><strong>屏幕可用工作区宽度：</strong>window.screen.availWidth=屏幕分辨率的宽</p>
<h2 id="获取视口大小"><a href="#获取视口大小" class="headerlink" title="获取视口大小"></a>获取视口大小</h2><p><strong>在声明了DOCTYPE的浏览器中：</strong><br>document.documentElement.clientWidth<br>document.documentElement.clientHeight<br>说明：IE，FF，Safari皆支持该方法，opera虽支持该属性，但是返回的是页面尺寸<br><strong>除了IE7及以下版本的所有浏览器都将此信息保存在window对象中</strong><br>window.innerWidth<br>window.innerHeight<br>说明：包含滚动条<br><strong>IE7及以下版本方获取视口方法</strong><br>document.body.offsetWidth<br>document.body.offsetHeight</p>
<h1 id="元素位置"><a href="#元素位置" class="headerlink" title="元素位置"></a>元素位置</h1><h2 id="绝对位置"><a href="#绝对位置" class="headerlink" title="绝对位置"></a>绝对位置</h2><p><strong>绝对位置：</strong>网页元素的绝对位置，指该元素的左上角相对于整张网页左上角的坐标。这个绝对位置要通过计算才能得到。<br><strong>获取绝对高度:</strong> DOMElement.offsetTop<br><strong>获取绝对宽度：</strong>DOMElement.offsetLeft</p>
<h2 id="相对位置"><a href="#相对位置" class="headerlink" title="相对位置"></a>相对位置</h2><p><strong>相对位置：</strong>网页元素的相对位置，指该元素左上角相对于浏览器窗口左上角的坐标。</p>
<p>由此有：元素绝对位置坐标=元素相对位置坐标+滚动条滚动的距离<br>要知道元素的相对位置就要知道滚动条滚动的距离，那么滚动条的滚动距离怎么获取呢？</p>
<h3 id="获取滚动条的滚动距离"><a href="#获取滚动条的滚动距离" class="headerlink" title="获取滚动条的滚动距离"></a>获取滚动条的滚动距离</h3><p><strong>竖：</strong>document.body.scrollTop       或window.scrollY<br><strong>横：</strong>document.body.scrollLeft      或window.scrollX</p>
<h3 id="窗口相对于屏幕的位置"><a href="#窗口相对于屏幕的位置" class="headerlink" title="窗口相对于屏幕的位置"></a>窗口相对于屏幕的位置</h3><p><strong>窗口相对于屏幕的X坐标：</strong>window.screenLeft（通常为0）<br><strong>窗口相对于屏幕的Y坐标：</strong>window.screenTop</p>
<h1 id="jQuery如何获取"><a href="#jQuery如何获取" class="headerlink" title="jQuery如何获取"></a>jQuery如何获取</h1><p>使用原生的js方法获取DOM尺寸大小和元素位置仍然不太方便，比如兼容性，jQuery封装的函数就很好的解决了这个问题。</p>
<h2 id="时下页面大小"><a href="#时下页面大小" class="headerlink" title="时下页面大小"></a>时下页面大小</h2><p>当窗口大小改变，宽度值改变，高度值固定<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="built_in">document</span>).width()   <span class="comment">//浏览器时下窗口文档的宽度  </span></div><div class="line">$(<span class="built_in">document</span>).height()　<span class="comment">//浏览器时下窗口文档的高度</span></div></pre></td></tr></table></figure></p>
<h2 id="时下元素大小"><a href="#时下元素大小" class="headerlink" title="时下元素大小"></a>时下元素大小</h2><p>当窗口大小改变，宽度值改变，高度值固定<br>以下以body节点为例，可替换成DOM元素调用方法<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="built_in">document</span>.body).width()　　　　　　<span class="comment">//浏览器时下窗口文档body的宽度   </span></div><div class="line">$(<span class="built_in">document</span>.body).height()　　　　　 <span class="comment">//浏览器时下窗口文档body的高度   </span></div><div class="line">$(<span class="built_in">document</span>.body).outerWidth(<span class="literal">true</span>)　 <span class="comment">//浏览器时下窗口文档body的总宽度(包括padding、border) </span></div><div class="line">$(<span class="built_in">document</span>.body).outerHeight(<span class="literal">true</span>)　<span class="comment">//浏览器时下窗口文档body的总高度(包括padding、border)</span></div></pre></td></tr></table></figure></p>
<h2 id="时下可视区大小"><a href="#时下可视区大小" class="headerlink" title="时下可视区大小"></a>时下可视区大小</h2><p>当窗口大小改变，宽度、高度皆改变<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="built_in">window</span>).height() 　<span class="comment">//浏览器时下窗口可视区域高度    </span></div><div class="line">$(<span class="built_in">window</span>).width() 　 <span class="comment">//浏览器时下窗口可视区域宽度</span></div></pre></td></tr></table></figure></p>
]]></content>
      <tags>
        <tag>js</tag>
        <tag>dom</tag>
      </tags>
  </entry>
  <entry>
    <title>javascript字符串对象方法</title>
    <url>/2016/06/14/js-string-method/</url>
    <content><![CDATA[<h1 id="字符方法"><a href="#字符方法" class="headerlink" title="字符方法"></a>字符方法</h1><h2 id="chatAt-和chatCodeAt"><a href="#chatAt-和chatCodeAt" class="headerlink" title="chatAt()和chatCodeAt"></a>chatAt()和chatCodeAt</h2><p>charAt()和charCodeAt()<br>这两个方法接收一个参数，即基于0的字符位置。<br>chatAt()以单字符字符串的形式返回给定位置的那个字符。<br>chatCodeAt()返回给定位置的那个字符的字符编码。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> stringValue=<span class="string">"hellow"</span>;</div><div class="line">stringValue.chatAt(<span class="number">1</span>);      <span class="comment">//"e"</span></div><div class="line">stringValue.chatCodeAt(<span class="number">1</span>);  <span class="comment">//"101"</span></div></pre></td></tr></table></figure></p>
<h1 id="字符串操作方法"><a href="#字符串操作方法" class="headerlink" title="字符串操作方法"></a>字符串操作方法</h1><h2 id="concat"><a href="#concat" class="headerlink" title="concat()"></a>concat()</h2><p>concat()用于将一个或多个字符串拼接起来，返回拼接得到的新创建的字符串。<br>但实践中大多数情况还是使用(+)加号操作符，比concat()简单易行。</p>
<h2 id="slice-、substring-、substr"><a href="#slice-、substring-、substr" class="headerlink" title="slice()、substring()、substr()"></a>slice()、substring()、substr()</h2><p>三个基于子字符串创建新字符串的方法，创建并返回被操作字符串的子字符串。<strong>注意方法名称都是小写</strong><br><strong>共同点：</strong><br>参数1：必选，指定子字符串开始位置。<br>参数2：可选，不指定时将字符串的长度作为结束位置。<br><strong>差异：</strong><br><strong>1、当指定参数2时</strong></p>
<table>
<thead>
<tr>
<th>方法</th>
<th style="text-align:center">参数2含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>slice()、substring()</td>
<td style="text-align:center">指定子字符串最后一个字符的位置</td>
</tr>
<tr>
<td>substr</td>
<td style="text-align:center">返回或截取的字符个数</td>
</tr>
</tbody>
</table>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> stringValue=<span class="string">"hello world"</span>;</div><div class="line">stringValue.length;         <span class="comment">//11</span></div><div class="line">stringValue.slice(<span class="number">3</span>);       <span class="comment">//"lo world"</span></div><div class="line">stringValue.substring(<span class="number">3</span>);   <span class="comment">//"lo world"</span></div><div class="line">stringValue.substr(<span class="number">3</span>);		<span class="comment">//"lo world"</span></div><div class="line">stringValue.slice(<span class="number">3</span>,<span class="number">7</span>);		<span class="comment">//"lo wo"</span></div><div class="line">stringValue.substring(<span class="number">3</span>,<span class="number">7</span>); <span class="comment">//"lo wo"</span></div><div class="line">stringValue.substr(<span class="number">3</span>,<span class="number">7</span>);    <span class="comment">//"lo worl"</span></div></pre></td></tr></table></figure>
<p><strong>2、当传递这些方法的参数是负数时</strong></p>
<table>
<thead>
<tr>
<th>方法</th>
<th style="text-align:center">行为</th>
</tr>
</thead>
<tbody>
<tr>
<td>slice()</td>
<td style="text-align:center">所有传入的负值与字符串的长度相加</td>
</tr>
<tr>
<td>substring()</td>
<td style="text-align:center">所有的负值参数都转化为0</td>
</tr>
<tr>
<td>substr()</td>
<td style="text-align:center">将负的第一个参数加上字符串的长度，将负的第二个参数转换为0</td>
</tr>
</tbody>
</table>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> stringValue=<span class="string">"hello world"</span>;</div><div class="line">stringValue.length;             <span class="comment">//11</span></div><div class="line">stringValue.slice(<span class="number">-3</span>);          <span class="comment">//"rld"</span></div><div class="line">stringValue.substring(<span class="number">-3</span>);      <span class="comment">//"hello world"</span></div><div class="line">stringValue.substr(<span class="number">-3</span>);			<span class="comment">//"rld"</span></div><div class="line">stringValue.slice(<span class="number">3</span>,<span class="number">-4</span>);		<span class="comment">//"lo wo"</span></div><div class="line">stringValue.substring(<span class="number">3</span>,<span class="number">-4</span>);    <span class="comment">//"hel"</span></div><div class="line">stringValue.substr(<span class="number">3</span>,<span class="number">-4</span>);       <span class="comment">//""</span></div></pre></td></tr></table></figure>
<h1 id="字符串位置方法"><a href="#字符串位置方法" class="headerlink" title="字符串位置方法"></a>字符串位置方法</h1><h2 id="indexOf-和lastIndexOf"><a href="#indexOf-和lastIndexOf" class="headerlink" title="indexOf()和lastIndexOf"></a>indexOf()和lastIndexOf</h2><p>从字符串中查找子字符串的位置，如果找到返回子字符串的位置；找不到返回-1<br>参数1：指定检索的子字符串<br>参数2：可选，指定开始检索的位置<br>indexOf()从开头向末尾搜索<br>lastIndexOf()从末尾向开头搜索<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">stringValue=<span class="string">"hello world"</span>;</div><div class="line">stringValue.indexOf(<span class="string">'o'</span>,<span class="number">6</span>);           <span class="comment">//7</span></div><div class="line">stringValue.lastIndexOf(<span class="string">'o'</span>,<span class="number">6</span>);       <span class="comment">//4,此时从指定位置向前检索</span></div></pre></td></tr></table></figure></p>
<h1 id="trim-方法"><a href="#trim-方法" class="headerlink" title="trim()方法"></a>trim()方法</h1><p>创建一个字符串的副本，删除前置及后缀的所有空格，然后返回结果。<br>注意：字符串中的空格不管<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">stringValue=<span class="string">" hellow world  "</span>;</div><div class="line">stringValue.trim();         <span class="comment">//"hellow world"</span></div><div class="line">stringValue;                <span class="comment">//" hellow world  ";</span></div></pre></td></tr></table></figure></p>
<h1 id="字符串大小写转换方法"><a href="#字符串大小写转换方法" class="headerlink" title="字符串大小写转换方法"></a>字符串大小写转换方法</h1><p>toLowerCase()和toUpperCase()</p>
<h1 id="字符串模式匹配方法"><a href="#字符串模式匹配方法" class="headerlink" title="字符串模式匹配方法"></a>字符串模式匹配方法</h1><h2 id="match"><a href="#match" class="headerlink" title="match()"></a>match()</h2><p>在字符串调用这个方法，本质上与调用RegExp对象的exec()方法相同。<br>参数：match()接收一个参数，要么是正则表达式，要么是正则对象。<br>返回值：matches返回一个数组，数组的第一项是与整个模式匹配的字符串，之后的每一项（如果有）保存着与正则表达式中捕获组匹配的字符串。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> text=<span class="string">"cat,bat,hat"</span>;</div><div class="line"><span class="keyword">var</span> pattern=<span class="regexp">/.at/</span>;</div><div class="line"><span class="keyword">var</span> matches=text.match(pattern);</div><div class="line">matches.index              <span class="comment">//0</span></div><div class="line">matches[<span class="number">0</span>]                 <span class="comment">//"cat"</span></div><div class="line">pattern.lastIndex          <span class="comment">//0</span></div></pre></td></tr></table></figure></p>
<h2 id="search"><a href="#search" class="headerlink" title="search()"></a>search()</h2><p>参数：search()接收一个参数，要么是正则表达式，要么是正则对象。<br>返回值：返回字符串中第一个匹配项的索引，如果没有找到匹配项，则返回-1.<br>注意：search()方法始终从字符串开头向后查找模式<br>例子：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> text=<span class="string">"cat,bat,hat"</span>;</div><div class="line"><span class="keyword">var</span> pos=text.search(<span class="regexp">/cat/</span>);    <span class="comment">//0</span></div></pre></td></tr></table></figure></p>
<h2 id="replace"><a href="#replace" class="headerlink" title="replace()"></a>replace()</h2><p>诞生原因：为了简化替换子字符串的操作，ECMAScript提供了replace()方法<br>参数：第一个参数正则对象或者字符串，第二个参数是一个字符串或者函数。<br>返回值：生成的替换后的字符串<br>注意<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">text=<span class="string">"cat,bat,hat"</span>;</div><div class="line">result=text.replace(<span class="string">"at"</span>,<span class="string">"ond"</span>)     <span class="comment">//"cond,bat,hat"</span></div><div class="line">result=text.replace(<span class="regexp">/at/g</span>,<span class="string">"ond"</span>)    <span class="comment">//"cond,cont,hont"</span></div></pre></td></tr></table></figure></p>
<p>更精细的替换操作，使用函数作为replace()的第二个参数<br>在只有一个匹配项的情况下，会向这个函数传递3个参数：模式的匹配项、模式的匹配项在字符串中的位置、原始字符串。<br>在正则表达式中定义了多个捕获组的情况下，这个函数的参数依次是：模式的匹配项、第一个捕获组的匹配项、第二个捕获组的匹配项…，最后两个参数是模式的匹配项在字符串中的位置、原始字符串。<br>这个函数应该返回一个字符串，表示准备替换的预备项。<br>例子：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">htmlEscape</span>(<span class="params">text</span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> text.replace(<span class="regexp">/[&lt;&gt;"&amp;]/g</span>,<span class="function"><span class="keyword">function</span>(<span class="params">match,pos,originText</span>)</span>&#123;</div><div class="line">		<span class="keyword">switch</span>(<span class="keyword">case</span>)&#123;</div><div class="line">			<span class="keyword">case</span> <span class="string">"&lt;"</span>:</div><div class="line">				<span class="keyword">return</span> &amp;lt;</div><div class="line">			<span class="keyword">case</span> <span class="string">"&gt;"</span>:</div><div class="line">				<span class="keyword">return</span> &amp;gt;</div><div class="line">			<span class="keyword">case</span> <span class="string">"\""</span>:</div><div class="line">				<span class="keyword">return</span> &amp;quot;</div><div class="line">			<span class="keyword">case</span> <span class="string">"&amp;"</span>:</div><div class="line">				<span class="keyword">return</span> &amp;amp;</div><div class="line">		&#125;</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line">htmlEscape(<span class="string">"&lt;p class=\"desc\"&gt;hellow&lt;/p&gt;"</span>)  </div><div class="line"><span class="comment">//输出 &amp;lt;p class=&amp;quot;desc;&amp;quot;&amp;gt;hellow&amp;lt;/p&amp;gt;</span></div></pre></td></tr></table></figure></p>
<h2 id="split"><a href="#split" class="headerlink" title="split()"></a>split()</h2><p>基于指定的分割符将字符串分割成多个子字符串，并将结果存放在一个数组中。<br>参数：第一个参数是字符串或者正则表达式,匹配的项将作为分隔符<br>      第二个参数可选，用于指定数组的大小<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">color=<span class="string">"red,blue,pink"</span>;</div><div class="line">colors1=color.split(<span class="string">","</span>);   <span class="comment">//["red","blue","pink"]</span></div><div class="line">colors2=color.split(<span class="string">","</span>,<span class="number">2</span>); <span class="comment">//["red","blue"]</span></div><div class="line">colors3=color.split(<span class="regexp">/[^,]+/</span>);   </div><div class="line"><span class="comment">//["",",",",",""]   由于/^,/匹配的是除逗号以外的一个或多个字符，而"red"和"pink"刚好在开头和结尾，因此数组第一个元素和最后一个为空字符</span></div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>string</tag>
      </tags>
  </entry>
  <entry>
    <title>Javascript开启浏览器全屏模式</title>
    <url>/2016/11/04/let-your-browser-open-fullscreen-mode/</url>
    <content><![CDATA[<p>通常在某些情况下，我们需要让浏览器开启全屏模式，以便获得更好的视觉体验，先看下全屏模式简单的几个API。</p>
<h1 id="浏览器默认绑定"><a href="#浏览器默认绑定" class="headerlink" title="浏览器默认绑定"></a>浏览器默认绑定</h1><p>非全屏模式下， document的F11按键绑定开启全屏模式<br>全屏模式下， document的esc和F11 按键绑定关闭全屏模式</p>
<h1 id="屏幕全屏模式改变事件"><a href="#屏幕全屏模式改变事件" class="headerlink" title="屏幕全屏模式改变事件"></a>屏幕全屏模式改变事件</h1><p>当成功进入全屏模式后， document会收到一个fullscreenchange 事件。 当退出全屏状态后， document又会收到fullscreenchange 事件。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> screenChange = <span class="string">'webkitfullscreenchange'</span> || <span class="string">'mozfullscreenchange'</span> || <span class="string">'fullscreenchange'</span></div></pre></td></tr></table></figure></p>
<h1 id="判断当前是否处于全屏状态"><a href="#判断当前是否处于全屏状态" class="headerlink" title="判断当前是否处于全屏状态"></a>判断当前是否处于全屏状态</h1><p><strong>非标准(避免使用)：</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> isFullScreen = <span class="built_in">document</span>.fullScreen || <span class="built_in">document</span>.mozFullScreen || <span class="built_in">document</span>.webkitIsFullScreen;</div></pre></td></tr></table></figure></p>
<p><strong>标准：</strong><br>fullscreenElement 属性会告诉你当前全屏显示的 element。 如果该值为非空，则document进入了全屏模式。如果为空则不在全屏模式。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> isFullScreen = <span class="built_in">document</span>.fullscreenElement || <span class="built_in">document</span>.mozFullScreenElement ||<span class="built_in">document</span>.webkitFullscreenElement</div></pre></td></tr></table></figure></p>
<h1 id="判断是否可以进入请求全屏状态"><a href="#判断是否可以进入请求全屏状态" class="headerlink" title="判断是否可以进入请求全屏状态"></a>判断是否可以进入请求全屏状态</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> isFullScreenAvailable = <span class="built_in">document</span>.fullScreenEnabled || <span class="built_in">document</span>.mozFullScreenEnabled || <span class="built_in">document</span>.webkitFullscreenEnabled;</div></pre></td></tr></table></figure>
<h1 id="开启全屏模式"><a href="#开启全屏模式" class="headerlink" title="开启全屏模式"></a>开启全屏模式</h1><p>参数：element为想要全屏展示的元素比如video，如果想要浏览器全屏展示，则传入document.documentElement即可。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">launchFullScreen</span>(<span class="params">element</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (element.requestFullscreen) &#123;</div><div class="line">        element.requestFullscreen();</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (element.mozRequestFullScreen) &#123;</div><div class="line">        element.mozRequestFullScreen();</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (element.webkitRequestFullscreen) &#123;</div><div class="line">        element.webkitRequestFullscreen();</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (element.msRequestFullscreen) &#123;</div><div class="line">        element.msRequestFullscreen();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>注意: <strong>requestFullscreen是规范的书写模式（ s是小写）</strong>， 但在Gecko内核中仍使用带前缀的大写模式mozRequestFullScreen。</p>
<h1 id="关闭全屏模式"><a href="#关闭全屏模式" class="headerlink" title="关闭全屏模式"></a>关闭全屏模式</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">exitFullscreen</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (<span class="built_in">document</span>.exitFullscreen) &#123;</div><div class="line">        <span class="built_in">document</span>.exitFullscreen();</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">document</span>.mozCancelFullScreen) &#123;</div><div class="line">        <span class="built_in">document</span>.mozCancelFullScreen();</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">document</span>.webkitExitFullscreen) &#123;</div><div class="line">        <span class="built_in">document</span>.webkitExitFullscreen();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h1 id="全屏模式只能由手势触发"><a href="#全屏模式只能由手势触发" class="headerlink" title="全屏模式只能由手势触发"></a>全屏模式只能由手势触发</h1><p>了解API后，假如我们监听window.onload事件执行launchFullScreen方法，Chrome浏览器会提示“开启全屏模式API只能由用户手势触发”。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">"Failed to execute 'requestFullScreen' on 'Element': API can only be initiated by a user gesture."</span></div></pre></td></tr></table></figure></p>
<p>原因是浏览器采用安全的机制， 将这种强制全屏模式意为“恶意行为”， <strong>一切非用户主观意愿带来的变化都是不允许的</strong>。</p>
<p>因此如果你的应用有全屏需求，有两种方案。<br>1.页面初始化给用户一个“F11开启全屏” 的提示， 并且在延迟几秒之后消失。<br>2.页面设置一个全屏按钮，单击全屏按钮进入全屏模式，并且隐藏按钮（视觉效果最佳）。</p>
<p>对于第二种方案，需要监听键盘事件：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="built_in">document</span>.addEventListener(<span class="string">"keydown"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> currKey = <span class="number">0</span></div><div class="line">    <span class="comment">//在FireFox或Opera中，隐藏的变量e是存在的，那么e||event返回e，如果在IE中，隐藏变量e是不存在，则返回event。</span></div><div class="line">    <span class="keyword">var</span> e = e || event;  </div><div class="line">    <span class="comment">//IE中，只有keyCode属性，而FireFox中有which和charCode属性，Opera中有keyCode和which属性</span></div><div class="line">    <span class="keyword">var</span> currKey = e.keyCode || e.which || e.charCode;</div><div class="line">    <span class="keyword">if</span> (currKey == <span class="number">112</span>) &#123;</div><div class="line">        launchFullScreen(<span class="built_in">document</span>.documentElement);</div><div class="line">    &#125;</div><div class="line">&#125;, <span class="literal">false</span>);</div></pre></td></tr></table></figure></p>
<p>具备了兼容各种浏览器按键模式的监听，但不知道<strong>keycode</strong>肿么办，112是哪个键？</p>
<h2 id="字母和数字键的键码值-keyCode"><a href="#字母和数字键的键码值-keyCode" class="headerlink" title="字母和数字键的键码值(keyCode)"></a>字母和数字键的键码值(keyCode)</h2><table>
<thead>
<tr>
<th style="text-align:center">按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">A</td>
<td>65</td>
<td>J</td>
<td>74</td>
<td>S</td>
<td>83</td>
<td>1</td>
<td>49</td>
</tr>
<tr>
<td style="text-align:center">B</td>
<td>66</td>
<td>K</td>
<td>75</td>
<td>T</td>
<td>84</td>
<td>2</td>
<td>50</td>
</tr>
<tr>
<td style="text-align:center">C</td>
<td>67</td>
<td>L</td>
<td>76</td>
<td>U</td>
<td>85</td>
<td>3</td>
<td>51</td>
</tr>
<tr>
<td style="text-align:center">D</td>
<td>68</td>
<td>M</td>
<td>77</td>
<td>V</td>
<td>86</td>
<td>4</td>
<td>52</td>
</tr>
<tr>
<td style="text-align:center">E</td>
<td>69</td>
<td>N</td>
<td>78</td>
<td>W</td>
<td>87</td>
<td>5</td>
<td>53</td>
</tr>
<tr>
<td style="text-align:center">F</td>
<td>70</td>
<td>O</td>
<td>79</td>
<td>X</td>
<td>88</td>
<td>6</td>
<td>54</td>
</tr>
<tr>
<td style="text-align:center">G</td>
<td>71</td>
<td>P</td>
<td>80</td>
<td>Y</td>
<td>89</td>
<td>7</td>
<td>55</td>
</tr>
<tr>
<td style="text-align:center">H</td>
<td>72</td>
<td>Q</td>
<td>81</td>
<td>Z</td>
<td>90</td>
<td>8</td>
<td>56</td>
</tr>
<tr>
<td style="text-align:center">I</td>
<td>73</td>
<td>R</td>
<td>82</td>
<td>0</td>
<td>48</td>
<td>9</td>
<td>57</td>
</tr>
</tbody>
</table>
<h2 id="数字键盘上的键的键码值-keyCode"><a href="#数字键盘上的键的键码值-keyCode" class="headerlink" title="数字键盘上的键的键码值(keyCode)"></a>数字键盘上的键的键码值(keyCode)</h2><table>
<thead>
<tr>
<th style="text-align:center">按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">0</td>
<td>96</td>
<td>8</td>
<td>104</td>
</tr>
<tr>
<td style="text-align:center">1</td>
<td>97</td>
<td>9</td>
<td>105</td>
</tr>
<tr>
<td style="text-align:center">2</td>
<td>98</td>
<td>*</td>
<td>106</td>
</tr>
<tr>
<td style="text-align:center">3</td>
<td>99</td>
<td>+</td>
<td>107</td>
</tr>
<tr>
<td style="text-align:center">4</td>
<td>100</td>
<td>Enter</td>
<td>108</td>
</tr>
<tr>
<td style="text-align:center">5</td>
<td>101</td>
<td>-</td>
<td>109</td>
</tr>
<tr>
<td style="text-align:center">6</td>
<td>102</td>
<td>.</td>
<td>110</td>
</tr>
<tr>
<td style="text-align:center">7</td>
<td>103</td>
<td>/</td>
<td>111</td>
</tr>
</tbody>
</table>
<h2 id="功能键键码值-keyCode"><a href="#功能键键码值-keyCode" class="headerlink" title="功能键键码值(keyCode)"></a>功能键键码值(keyCode)</h2><table>
<thead>
<tr>
<th style="text-align:center">按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">F1</td>
<td>112</td>
<td>F7</td>
<td>118</td>
</tr>
<tr>
<td style="text-align:center">F2</td>
<td>113</td>
<td>F8</td>
<td>119</td>
</tr>
<tr>
<td style="text-align:center">F3</td>
<td>114</td>
<td>F9</td>
<td>120</td>
</tr>
<tr>
<td style="text-align:center">F4</td>
<td>115</td>
<td>F10</td>
<td>121</td>
</tr>
<tr>
<td style="text-align:center">F5</td>
<td>116</td>
<td>F11</td>
<td>122</td>
</tr>
<tr>
<td style="text-align:center">F6</td>
<td>117</td>
<td>F12</td>
<td>123</td>
</tr>
</tbody>
</table>
<h2 id="控制键键码值-keyCode"><a href="#控制键键码值-keyCode" class="headerlink" title="控制键键码值(keyCode)"></a>控制键键码值(keyCode)</h2><table>
<thead>
<tr>
<th style="text-align:center">按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
<th>按键</th>
<th>键码</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">BackSpace</td>
<td>8</td>
<td>Esc</td>
<td>27</td>
<td>Right Arrow</td>
<td>39</td>
<td>-_</td>
<td>189</td>
</tr>
<tr>
<td style="text-align:center">Tab</td>
<td>9</td>
<td>Spacebar</td>
<td>32</td>
<td>Dw Arrow</td>
<td>40</td>
<td>.&gt;</td>
<td>190</td>
</tr>
<tr>
<td style="text-align:center">Clear</td>
<td>12</td>
<td>Page Up</td>
<td>33</td>
<td>Insert</td>
<td>45</td>
<td>/?</td>
<td>191</td>
</tr>
<tr>
<td style="text-align:center">Enter</td>
<td>13</td>
<td>Page Down</td>
<td>34</td>
<td>Delete</td>
<td>46</td>
<td>`~</td>
<td>192</td>
</tr>
<tr>
<td style="text-align:center">Shift</td>
<td>16</td>
<td>End</td>
<td>35</td>
<td>Num Lock</td>
<td>144</td>
<td>[{</td>
<td>219</td>
</tr>
<tr>
<td style="text-align:center">Control</td>
<td>17</td>
<td>Home</td>
<td>36</td>
<td>;:</td>
<td>186</td>
<td>\</td>
<td>220</td>
</tr>
<tr>
<td style="text-align:center">Alt</td>
<td>18</td>
<td>Left Arrow</td>
<td>37</td>
<td>=+</td>
<td>187</td>
<td>]}</td>
<td>221</td>
</tr>
<tr>
<td style="text-align:center">Cape Lock</td>
<td>20</td>
<td>Up Arrow</td>
<td>38</td>
<td>,&lt;</td>
<td>188</td>
<td>‘“</td>
<td>222</td>
</tr>
</tbody>
</table>
<h1 id="其他非标准化的方法"><a href="#其他非标准化的方法" class="headerlink" title="其他非标准化的方法"></a>其他非标准化的方法</h1><p><strong>非标准化的方法指的是进入草案前浏览器实现的一些方法。有的目前仍保留，有的已废除，避免使用。</strong><br><del>window.fullScreen(Firefox)</del><br><del>HTMLMediaElement.webkitDisplayingFullscreen</del><br><del>HTMLMediaElement.webkitEnterFullscreen</del><br><del>HTMLMediaElement.webkitExitFullscreen</del><br><del>HTMLMediaElement.webkitSupportsFullscreen</del></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>常用的Lodash方法</title>
    <url>/2017/01/22/lodash-useful-function/</url>
    <content><![CDATA[<p>Lodash目前非常流行的JavaScript的工具集合框架，被前端开发者广泛地使用，下面是一些平常开发过程中常用的函数。</p>
<p>在介绍函数前，对lodash函数名应有如下的了解：</p>
<ul>
<li>含sorted的方法接受有序数组</li>
<li>同名含By的方法额外接受一个迭代器参数</li>
<li>同名含Last的方法返回的是符合筛选条件元素的最后一个</li>
</ul>
<h2 id="对象扩展："><a href="#对象扩展：" class="headerlink" title="对象扩展："></a>对象扩展：</h2><p>_.assign</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> foo = &#123; <span class="attr">a</span>: <span class="string">"a property"</span> &#125;;</div><div class="line"><span class="keyword">var</span> bar = &#123; <span class="attr">b</span>: <span class="number">4</span>, <span class="attr">c</span>: <span class="string">"an other property"</span> &#125;</div><div class="line"></div><div class="line"><span class="keyword">var</span> result = _.assign(&#123; <span class="attr">a</span>: <span class="string">"an old property"</span> &#125;, foo, bar);</div><div class="line"><span class="comment">// result =&gt; &#123; a: 'a property', b: 4, c: 'an other property' &#125;</span></div></pre></td></tr></table></figure>
<h2 id="防抖函数"><a href="#防抖函数" class="headerlink" title="防抖函数"></a>防抖函数</h2><p>_.debounce</p>
<p>在触发防抖函数的事件后面加个延迟时间，如果在该延迟时间内没有继续触发事件，那么执行防抖函数。设想一个场景，电梯门打开的时候，如果在2s内陆续有人按电梯按钮，那么每次电梯都会再重新等待2s，直至2s中没人按按钮再触发关门操作。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">validateEmail</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="comment">// Validate email here and show error message if not valid</span></div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">var</span> emailInput = <span class="built_in">document</span>.getElementById(<span class="string">"email-field"</span>);</div><div class="line">emailInput.addEventListener(<span class="string">"keyup"</span>, _.debounce(validateEmail, <span class="number">500</span>));</div></pre></td></tr></table></figure></p>
<h2 id="迭代函数n次"><a href="#迭代函数n次" class="headerlink" title="迭代函数n次"></a>迭代函数n次</h2><ul>
<li>返回：每次返回结果构成的数组</li>
<li>应用：生成大量测试数据</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getRandomInteger</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.round(<span class="built_in">Math</span>.random() * <span class="number">100</span>);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">var</span> result = _.times(<span class="number">5</span>, getRandomNumber);</div><div class="line"><span class="comment">// result =&gt; [64, 70, 29, 10, 23]</span></div></pre></td></tr></table></figure>
<h2 id="查找数组中的特定对象"><a href="#查找数组中的特定对象" class="headerlink" title="查找数组中的特定对象"></a>查找数组中的特定对象</h2><p>接收多个对象属性查找<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> users = [</div><div class="line">  &#123; <span class="attr">firstName</span>: <span class="string">"John"</span>, <span class="attr">lastName</span>: <span class="string">"Doe"</span>, <span class="attr">age</span>: <span class="number">28</span>, <span class="attr">gender</span>: <span class="string">"male"</span> &#125;,</div><div class="line">  &#123; <span class="attr">firstName</span>: <span class="string">"Jane"</span>, <span class="attr">lastName</span>: <span class="string">"Doe"</span>, <span class="attr">age</span>: <span class="number">5</span>, <span class="attr">gender</span>: <span class="string">"female"</span> &#125;,</div><div class="line">  &#123; <span class="attr">firstName</span>: <span class="string">"Jim"</span>, <span class="attr">lastName</span>: <span class="string">"Carrey"</span>, <span class="attr">age</span>: <span class="number">54</span>, <span class="attr">gender</span>: <span class="string">"male"</span> &#125;,</div><div class="line">  &#123; <span class="attr">firstName</span>: <span class="string">"Kate"</span>, <span class="attr">lastName</span>: <span class="string">"Winslet"</span>, <span class="attr">age</span>: <span class="number">40</span>, <span class="attr">gender</span>: <span class="string">"female"</span> &#125;</div><div class="line">];</div><div class="line"></div><div class="line"><span class="keyword">var</span> user = _.find(users, &#123; <span class="attr">lastName</span>: <span class="string">"Doe"</span>, <span class="attr">gender</span>: <span class="string">"male"</span> &#125;);</div><div class="line"><span class="comment">// user -&gt; &#123; firstName: "John", lastName: "Doe", age: 28, gender: "male" &#125;</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> underAgeUser = _.find(users, <span class="function"><span class="keyword">function</span>(<span class="params">user</span>) </span>&#123;</div><div class="line">	<span class="keyword">return</span> user.age &lt; <span class="number">18</span>;</div><div class="line">&#125;);</div><div class="line"><span class="comment">// underAgeUser -&gt; &#123; firstName: "Jane", lastName: "Doe", age: 5, gender: "female" &#125;</span></div></pre></td></tr></table></figure></p>
<h2 id="设置及获取对象属性"><a href="#设置及获取对象属性" class="headerlink" title="设置及获取对象属性"></a>设置及获取对象属性</h2><p>根据对象路径进行设置或获取</p>
<ul>
<li>_set方法：如果路径不存在，会自动创建路径。像下面的例子，使用该方法不会再出现“Cannot set property ‘items’ of undefined” error.”这种错误</li>
<li>_get方法：如果对象路径不存在，返回undefined而不会报错，第三个参数接收默认值。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> bar = &#123; <span class="attr">foo</span>: &#123; <span class="attr">key</span>: <span class="string">"foo"</span> &#125; &#125;;</div><div class="line">_.set(bar, <span class="string">"foo.items[0]"</span>, <span class="string">"An item"</span>);</div><div class="line"><span class="comment">// bar =&gt; &#123; foo: &#123; key: "foo", items: ["An item"] &#125; &#125;</span></div><div class="line"><span class="keyword">var</span> name = _.get(bar, <span class="string">"name"</span>, <span class="string">"John Doe"</span>);</div><div class="line"><span class="comment">// name =&gt; John Doe</span></div></pre></td></tr></table></figure>
<h2 id="将对象数组重组成对象映射"><a href="#将对象数组重组成对象映射" class="headerlink" title="将对象数组重组成对象映射"></a>将对象数组重组成对象映射</h2><ul>
<li>只要服务器返回的是个集合对象，就可以通过该方法将集合转成对象映射</li>
<li>第二个参数也可以是函数，函数的第一个参数默认是数组中的一个对象</li>
</ul>
<p>如从100篇文章中选取出id为34abc的文章，当获取到文章数组后，怎么做？<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> posts = [</div><div class="line">    &#123; <span class="attr">id</span>: <span class="string">"1abc"</span>, <span class="attr">title</span>: <span class="string">"First blog post"</span>, <span class="attr">content</span>: <span class="string">"..."</span> &#125;,</div><div class="line">    &#123; <span class="attr">id</span>: <span class="string">"2abc"</span>, <span class="attr">title</span>: <span class="string">"Second blog post"</span>, <span class="attr">content</span>: <span class="string">"..."</span> &#125;,</div><div class="line">    <span class="comment">// more blog posts</span></div><div class="line">    &#123; <span class="attr">id</span>: <span class="string">"34abc"</span>, <span class="attr">title</span>: <span class="string">"The blog post we want"</span>, <span class="attr">content</span>: <span class="string">"..."</span> &#125;</div><div class="line">    <span class="comment">// even more blog posts</span></div><div class="line">];</div><div class="line"></div><div class="line">posts = _.keyBy(posts, <span class="string">"id"</span>);</div><div class="line"></div><div class="line"><span class="keyword">var</span> post = posts[<span class="string">"34abc"</span>]</div><div class="line"><span class="comment">// post -&gt; &#123; id: "34abc", title: "The blog post we want", content: "..." &#125;</span></div></pre></td></tr></table></figure></p>
<h2 id="遍历集合元素并返回特定格式的对象"><a href="#遍历集合元素并返回特定格式的对象" class="headerlink" title="遍历集合元素并返回特定格式的对象"></a>遍历集合元素并返回特定格式的对象</h2><ul>
<li>通过 iteratee 遍历集合中的每个元素。 每次返回的值会作为下一次 iteratee 使用。 如果没有提供accumulator（第三个参数，即初始值），则集合中的第一个元素作为 accumulator。</li>
<li>常见的错误是忽略了返回结果(return result)和没有传入函数的第三个参数(默认值)</li>
</ul>
<p>下面的例子从数组筛选符合年龄条件并由年龄进行分组的数据<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> users = [</div><div class="line">    &#123; <span class="attr">name</span>: <span class="string">"John"</span>, <span class="attr">age</span>: <span class="number">30</span> &#125;,</div><div class="line">    &#123; <span class="attr">name</span>: <span class="string">"Jane"</span>, <span class="attr">age</span>: <span class="number">28</span> &#125;,</div><div class="line">    &#123; <span class="attr">name</span>: <span class="string">"Bill"</span>, <span class="attr">age</span>: <span class="number">65</span> &#125;,</div><div class="line">    &#123; <span class="attr">name</span>: <span class="string">"Emily"</span>, <span class="attr">age</span>: <span class="number">17</span> &#125;,</div><div class="line">    &#123; <span class="attr">name</span>: <span class="string">"Jack"</span>, <span class="attr">age</span>: <span class="number">30</span> &#125;</div><div class="line">]</div><div class="line"></div><div class="line"><span class="keyword">var</span> reducedUsers = _.reduce(users, <span class="function"><span class="keyword">function</span> (<span class="params">result, user</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span>(user.age &gt;= <span class="number">18</span> &amp;&amp; user.age &lt;= <span class="number">59</span>) &#123;</div><div class="line">        (result[user.age] || (result[user.age] = [])).push(user);</div><div class="line">    &#125;</div><div class="line">  </div><div class="line">    <span class="keyword">return</span> result;</div><div class="line">&#125;, &#123;&#125;);</div><div class="line"></div><div class="line"><span class="comment">// reducedUsers -&gt; &#123; </span></div><div class="line"><span class="comment">//     28: [&#123; name: "Jane", age: 28 &#125;], </span></div><div class="line"><span class="comment">//     30: [&#123; name: "John", age: 30 &#125;, &#123; name: "Jack", age: 30 &#125;] </span></div><div class="line"><span class="comment">// &#125;</span></div></pre></td></tr></table></figure></p>
<h2 id="深拷贝"><a href="#深拷贝" class="headerlink" title="深拷贝"></a>深拷贝</h2><p>_.cloneDeep()<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> original = &#123; <span class="attr">foo</span>: <span class="string">"bar"</span> &#125;;</div><div class="line"><span class="keyword">var</span> copy = original;</div><div class="line">copy.foo = <span class="string">"new value"</span>;</div><div class="line"><span class="comment">// copy -&gt; &#123; foo: "new value" &#125; Yeah!</span></div><div class="line"><span class="comment">// original -&gt; &#123; foo: "new value" &#125; Oops!</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> original = &#123; <span class="attr">foo</span>: <span class="string">"bar"</span> &#125;;</div><div class="line"><span class="keyword">var</span> copy = _.cloneDeep(original);</div><div class="line">copy.foo = <span class="string">"new value"</span>;</div><div class="line"><span class="comment">// copy -&gt; &#123; foo: "new value" &#125; Yeah!</span></div><div class="line"><span class="comment">// original -&gt; &#123; foo: "bar" &#125; Yeah!</span></div></pre></td></tr></table></figure></p>
<h2 id="浅拷贝"><a href="#浅拷贝" class="headerlink" title="浅拷贝"></a>浅拷贝</h2><p>_.clone()</p>
<p>对于对象内部的引用类型不会拷贝，而是指向同一个存在堆中的地址<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> objects = [&#123; <span class="string">'a'</span>: <span class="number">1</span> &#125;, &#123; <span class="string">'b'</span>: <span class="number">2</span> &#125;];</div><div class="line"></div><div class="line"><span class="keyword">var</span> shallow = _.clone(objects);</div><div class="line"><span class="built_in">console</span>.log(shallow[<span class="number">0</span>] === objects[<span class="number">0</span>]);</div><div class="line"><span class="comment">// =&gt; true</span></div></pre></td></tr></table></figure></p>
<h2 id="数组去重"><a href="#数组去重" class="headerlink" title="数组去重"></a>数组去重</h2><p><em>uniq() 与 </em>. sortedUniq()<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> sortedArray = [<span class="number">1</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">3</span>, <span class="number">3</span>, <span class="number">5</span>, <span class="number">8</span>, <span class="number">8</span>];</div><div class="line"><span class="keyword">var</span> result = _.sortedUniq(sortedArray);</div><div class="line"><span class="comment">// -&gt; [1, 2, 3, 5, 8]</span></div></pre></td></tr></table></figure></p>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>_.uniq</td>
<td>数组去重</td>
</tr>
<tr>
<td>_.sortedUniq</td>
<td>对于已排序的数组性能更高</td>
</tr>
</tbody>
</table>
<h2 id="向有序数组中插入元素，依旧保证有序"><a href="#向有序数组中插入元素，依旧保证有序" class="headerlink" title="向有序数组中插入元素，依旧保证有序"></a>向有序数组中插入元素，依旧保证有序</h2><p>返回：插入元素在数组中的索引</p>
<p>方法：都含有sorted表示接受有序数组</p>
<ul>
<li>_.sortedIndex</li>
<li>_.sortedIndexBy</li>
<li>_.sortedLastIndex</li>
<li>_.sortedLastIndexBy</li>
</ul>
<p>含Last方法和不含Last方法的区别是：<br>Last方法在保持有序的前提下会把value插进最大的那个位置<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> objects = [&#123; <span class="string">'x'</span>: <span class="number">4</span> &#125;, &#123; <span class="string">'x'</span>: <span class="number">5</span> &#125;];</div><div class="line"> </div><div class="line">_.sortedLastIndexBy(objects, &#123; <span class="string">'x'</span>: <span class="number">4</span> &#125;, <span class="function"><span class="keyword">function</span>(<span class="params">o</span>) </span>&#123; <span class="keyword">return</span> o.x; &#125;);</div><div class="line"><span class="comment">// =&gt; 1</span></div><div class="line"> </div><div class="line">_.sortedLastIndexBy(objects, &#123; <span class="string">'x'</span>: <span class="number">4</span> &#125;, <span class="string">'x'</span>);</div><div class="line"><span class="comment">// =&gt; 1</span></div></pre></td></tr></table></figure></p>
<h2 id="参考及延伸"><a href="#参考及延伸" class="headerlink" title="参考及延伸"></a>参考及延伸</h2><blockquote>
<p>Lodash官方文档(4.17.4)：<a href="https://lodash.com/docs/4.17.4" target="_blank" rel="external">https://lodash.com/docs/4.17.4</a><br>Lodash中文文档(3.10.1)：<a href="http://lodashjs.com/docs/" target="_blank" rel="external">http://lodashjs.com/docs/</a><br>Lodash/fp模块：<a href="https://github.com/lodash/lodash/wiki/FP-Guide" target="_blank" rel="external">https://github.com/lodash/lodash/wiki/FP-Guide</a><br>什么是Lodash/fp模块：<a href="http://www.cnblogs.com/legendlee/p/5601524.html" target="_blank" rel="external">http://www.cnblogs.com/legendlee/p/5601524.html</a><br>Webpack按需打包lodash：<a href="http://www.tuicool.com/articles/niyeMrN" target="_blank" rel="external">http://www.tuicool.com/articles/niyeMrN</a></p>
</blockquote>
]]></content>
      <tags>
        <tag>lodash</tag>
      </tags>
  </entry>
  <entry>
    <title>移动端图片画廊</title>
    <url>/2016/09/04/mobile-gallery/</url>
    <content><![CDATA[<p>移动端图片画廊，采用css3的flex布局结合object-fit属性，实现类似Google Photo 等高自适应的图片布局效果<br><img src="/img/post/2016-09-04-mobile-photo-galleries.gif" alt="demo"><br>你也可以点击<a href="http://alex1504.oschina.io/demo/mobile-gallery/" target="_blank" rel="external">在线地址</a>  查看预览:</p>
<h2 id="关于flex-box布局"><a href="#关于flex-box布局" class="headerlink" title="关于flex-box布局"></a>关于flex-box布局</h2><p>flex布局简直是移动端的神器，因为移动端不存在兼容性问题，几乎所有手机浏览器都采用相同的标准。</p>
<h2 id="几分钟快速了解flex-box布局"><a href="#几分钟快速了解flex-box布局" class="headerlink" title="几分钟快速了解flex-box布局"></a>几分钟快速了解flex-box布局</h2><h3 id="采用flex布局（作用于容器）"><a href="#采用flex布局（作用于容器）" class="headerlink" title="采用flex布局（作用于容器）"></a>采用flex布局（作用于容器）</h3><p>块状元素：<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.box</span>&#123;</div><div class="line">    <span class="attribute">display</span>:flex;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>行内元素：<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.box</span>&#123; </div><div class="line">    <span class="attribute">display</span>: inline-flex;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h3 id="容器的属性"><a href="#容器的属性" class="headerlink" title="容器的属性"></a>容器的属性</h3><p>寻找好基友：<br> flex-direction、 flex-wrap、flex-flow<br>justify-content、align-items、align-content</p>
<p>解释：</p>
<ul>
<li><strong>flex-direction</strong>：决定主轴的方向（即项目的排列方向）</li>
<li><strong>flex-wrap</strong>：默认情况下，项目都排在一条线（又称”轴线”）上。flex-wrap属性定义，如果一条轴线排不下，如何换行</li>
<li><strong>strong text</strong>flex-flow：flex-direction属性和flex-wrap属性的简写形式，默认值为row nowrap。</li>
<li><strong>justify-content</strong>：定义了项目在主轴上的对齐方式（flex-start | flex-end | center | space-between | space-around）</li>
<li><strong>align-items</strong>：定义项目在交叉轴上如何对齐（flex-start | flex-end | center | baseline | stretch）</li>
<li><strong>align-content</strong>：定义了多根轴线的对齐方式。如果项目只有一根轴线，该属性不起作用。（flex-start | flex-end | center | space-between | space-around | stretch;）</li>
</ul>
<h3 id="项目的属性"><a href="#项目的属性" class="headerlink" title="项目的属性"></a>项目的属性</h3><p>基友：<br>flex-grow、flex-shrink、flex-basis、flex<br>order   （规整有序）<br>alignself  （特立独行）</p>
<p>解释：<br><strong>flex-grow</strong>：定义项目的放大比例，默认为0，即如果存在剩余空间，也不放大。<br><strong>flex-shrink</strong>：定义了项目的缩小比例，默认为1，即如果空间不足，该项目将缩小。<br><strong>flex-basis</strong>：定义了在分配多余空间之前，项目占据的主轴空间（main size）。浏览器根据这个属性，计算主轴是否有多余空间。它的默认值为auto，即项目的本来大小。<br><strong>flex</strong>：flex-grow, flex-shrink 和 flex-basis的简写，默认值为0 1 auto。后两个属性可选。</p>
<p><strong>order</strong>：定义项目的排列顺序。数值越小，排列越靠前，默认为0。</p>
<p><strong>align-self</strong>：属性允许单个项目有与其他项目不一样的对齐方式，可覆盖align-items属性。默认值为auto，表示继承父元素的align-items属性，如果没有父元素，则等同于stretch。</p>
<h2 id="关于object-fit属性"><a href="#关于object-fit属性" class="headerlink" title="关于object-fit属性"></a>关于object-fit属性</h2><p>适用范围：替换元素</p>
<pre><code>&lt;img&gt;、&lt;input&gt;、&lt;textarea&gt;、&lt;select&gt;、&lt;object&gt;等
浏览器根据其标签的元素与属性来判断显示具体的内容
</code></pre><p>值：<br><strong>fill</strong>: 默认值。替换内容拉伸填满整个content box, 不保证保持原有的比例。<br><strong>contain</strong>: 保持原有尺寸比例。保证替换内容尺寸一定可以在容器里面放得下。因此，此参数可能会在容器内留下空白。<br><strong>cover</strong>: 保持原有尺寸比例。保证替换内容尺寸一定大于容器尺寸，宽度和高度至少有一个和容器一致。因此，此参数可能会让替换内容（如图片）部分区域不可见。<br><strong>none</strong>: 保持原有尺寸比例。同时保持替换内容原始尺寸大小。<br><strong>scale-down</strong>: 就好像依次设置了none或contain, 最终呈现的是尺寸比较小的那个。</p>
<h2 id="DEMO解释"><a href="#DEMO解释" class="headerlink" title="DEMO解释"></a>DEMO解释</h2><p>html:<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"page"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-gallery"</span>&gt;</span></div><div class="line">        <span class="comment">&lt;!-- &lt;div&gt;&lt;img class="animated bounceIn" src="img/1.jpg"&gt;&lt;/div&gt; --&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"m-viewbox hide"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">img</span> <span class="attr">class</span>=<span class="string">"bigimg animated bounceIn"</span> <span class="attr">src</span>=<span class="string">""</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>css:<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">.m-gallery&#123;</div><div class="line">    display: flex; //采用flex布局</div><div class="line">    flex-wrap: wrap;  //规定一行放不下flex元素时自动换行</div><div class="line">&#125;</div><div class="line">.m-gallery div&#123;</div><div class="line">    height: 100px;</div><div class="line">    flex-grow:1;   //每个flex元素占的宽度相同</div><div class="line">    margin: 2px;</div><div class="line">&#125;</div><div class="line"></div><div class="line">.m-gallery  img&#123;</div><div class="line">    height: 100px;</div><div class="line">    min-width: 100%;     </div><div class="line">    max-width: 100%;</div><div class="line">    object-fit: cover;      //使图片等比拉伸，可能会被裁减</div><div class="line">    vertical-align: bottom;</div><div class="line">&#125;</div><div class="line">@media (max-width: 1000px) and (min-width: 900px) &#123;</div><div class="line">  .m-gallery::after &#123;</div><div class="line">      content: '';</div><div class="line">      flex-grow: 999999999;     //当最后一行图片太少的时候，比如只有一张，因为 grow 的关系，它将占满一整行，通过伪元素来占满剩余空间，防止图片拉伸</div><div class="line">   &#125;</div><div class="line">&#125;</div><div class="line">@media (max-width: 1100px) and (min-width: 1000px) &#123;</div><div class="line"> .m-gallery::after &#123;</div><div class="line">      content: '';</div><div class="line">      flex-grow: 999999999;</div><div class="line">   &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>移动端</tag>
        <tag>图片画廊</tag>
      </tags>
  </entry>
  <entry>
    <title>移动端自适应布局解决方案——rem</title>
    <url>/2016/06/16/mobile-rem/</url>
    <content><![CDATA[<p>自适应布局方案有<strong>百分比布局、flex布局、弹性flex布局</strong>等，但是都有一些缺点。</p>
<ul>
<li><p>百分比布局缺点：字体大小需要另外一套自适应方法来调整；当屏幕宽度大于700px后，继续按照百分比元素会偏大，这个时候调整起来会比较麻烦。</p>
</li>
<li><p>flex布局、弹性flex布局：在移动端会出现一些支持的兼容问题。</p>
</li>
</ul>
<h1 id="rem"><a href="#rem" class="headerlink" title="rem"></a>rem</h1><p>W3C官网描述是“font size of the root element”，即rem是相对于根元素。<br>我们只需要在HTML根元素确定一个参考值，css中其他使用rem作为单位的值都基于这个值进行计算。</p>
<h1 id="rem实现宽度自适应的原理"><a href="#rem实现宽度自适应的原理" class="headerlink" title="rem实现宽度自适应的原理"></a>rem实现宽度自适应的原理</h1><p>通过JS使页面<html>的fontsize会根据屏幕的宽度自动调整。<br>具体是根据屏幕宽度和所设字体大小的比值是固定的，获取屏幕宽度后，按照固定比例缩小后作为rem的单位长度实现自适应。</html></p>
<p>另外值得说的是即使采用了rem布局方案，<strong>页面上不一定所有的元素都是采用rem作为单位。</strong>，比如下面淘宝的案例。底部固定的导航条采用的是高度使用固定的像素值，宽度flex布局的方案。<br><img src="http://qiniu.huzerui.com/image/2016-06-16-rem-adaptive-layout-1.jpg" alt="淘宝案例"><br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">footer&#123;</div><div class="line">	height:114px;</div><div class="line">	</div><div class="line">	/*这三个样式需要补充prefix*/</div><div class="line">    /*通过一起使用box-align和box-pack属性,对div框的子元素进行居中*/</div><div class="line">	display:flex;</div><div class="line">	box-pack: justify;</div><div class="line">	box-align: center;</div><div class="line">    </div><div class="line">	justify-content: space-between; //在flex容器内的各项没有占用主轴所有可用的空间时,元素的各项周围留有空白</div><div class="line">	align-content: center;  //在flex容器内的各项没有占用纵轴上所有可用的空间时，元素居中对齐</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h1 id="代码详解"><a href="#代码详解" class="headerlink" title="代码详解"></a>代码详解</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">doc, win</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> docEl = doc.documentElement,</div><div class="line">        resizeEvt = <span class="string">'orientationchange'</span> <span class="keyword">in</span> <span class="built_in">window</span> ? <span class="string">'orientationchange'</span> : <span class="string">'resize'</span>,</div><div class="line">        recalc = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">var</span> clientWidth = docEl.clientWidth;</div><div class="line">            <span class="keyword">if</span> (!clientWidth) <span class="keyword">return</span>;</div><div class="line">            <span class="keyword">if</span>(clientWidth&gt;=<span class="number">640</span>)&#123;</div><div class="line">                docEl.style.fontSize = <span class="string">'100px'</span>;</div><div class="line">            &#125;<span class="keyword">else</span>&#123;</div><div class="line">                docEl.style.fontSize = <span class="number">100</span> * (clientWidth / <span class="number">640</span>) + <span class="string">'px'</span>;</div><div class="line">            &#125;</div><div class="line">        &#125;;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (!doc.addEventListener) <span class="keyword">return</span>;</div><div class="line">    win.addEventListener(resizeEvt, recalc, <span class="literal">false</span>);</div><div class="line">    doc.addEventListener(<span class="string">'DOMContentLoaded'</span>, recalc, <span class="literal">false</span>);</div><div class="line">&#125;)(<span class="built_in">document</span>, <span class="built_in">window</span>);</div></pre></td></tr></table></figure>
<p>代码解释：<br>如果页面的宽度超过了640px，那么页面中html的font-size恒为100px，否则，页面中html的font-size的大小为： 100 * (当前页面宽度 / 640) </p>
<h2 id="为什么是640px？"><a href="#为什么是640px？" class="headerlink" title="为什么是640px？"></a>为什么是640px？</h2><p>三个概念：屏幕分辨率、设备像素、css像素<br>屏幕分辨率和设备像素是物理概念，而CSS像素是WEB编程的概念；屏幕分辨率和设备像素的差别在于设备像素显示密度。<br>当设备屏幕分辨率=100%的时候，浏览器CSS像素尺寸和设备像素相等，而当像素密度(pixel density)为1的时候，屏幕分辨率和设备像素相等。</p>
<p>对于手机屏幕来说，640px的页面宽度是一个安全的最大宽度，保证了移动端页面两边不会留白。注意这里的px是css逻辑像素，与设备的物理像素是有区别的。如iPhone 5使用的是Retina视网膜屏幕，使用2px x 2px的 device pixel 代表 1px x 1px 的 css pixel，所以设备像素数为640 x 1136px，而它的CSS逻辑像素数为320 x 568px。</p>
<h2 id="为什么要设置html的font-size？"><a href="#为什么要设置html的font-size？" class="headerlink" title="为什么要设置html的font-size？"></a>为什么要设置html的font-size？</h2><p>rem就是根元素（即：html）的字体大小。html中的所有标签样式凡是涉及到尺寸的（如： height,width,padding,margin,font-size。甚至，left,top等）你都可以放心大胆的用rem作单位。<br>如果你把html的font-size设为20px，前面说过，rem就是html的字体大小，那么1rem = 20px。</p>
<p>浏览器一般都有最小字体限制，比如谷歌浏览器，最小中文字体就是12px，所以实际上没有办法让1rem=1px</p>
<p>根据上面的js代码，如果页面宽度低于640px,那么页面中html的font-size也会按照（当前页面宽度/640）的比例变化。这样，页面中凡是应用了rem的作尺寸单位的元素都会随着页面变化而等比例缩放了。</p>
<h2 id="怎么计算出不同分辨率下font-size的值？"><a href="#怎么计算出不同分辨率下font-size的值？" class="headerlink" title="怎么计算出不同分辨率下font-size的值？"></a>怎么计算出不同分辨率下font-size的值？</h2><p>假设页面设计稿是按照640的标准尺寸设计的，（当然这个尺寸肯定不一定是640，可以是320，或者480，又或是375）来看一组表格。<br><img src="http://qiniu.huzerui.com/image/2016-06-16-rem-adaptive-layout-2.jpg" alt="rem"></p>
<p>以640的宽度去切的，怎么计算不同宽度下font-site的值，大家看表格上面的数值变化应该能明白。举个例子：384/640 = 0.6，384是640的0.6倍，所以384页面宽度下的font-size也等于它的0.6倍，这时384的font-size就等于12px。在不同设备的宽度计算方式以此类推。</p>
<p>除了上面通过JS去动态计算根元素的font-size，也可采用媒体查询。一般我们在做web app都会先统计自己网站有哪些主流的屏幕设备，然后去针对那些设备去做media query设置也可以实现适配，例如下面这样(参考)：<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-tag">html</span>&#123;</div><div class="line">    <span class="attribute">font-size</span>:<span class="number">10px</span></div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">321px</span>) and (max-width:<span class="number">375px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">11px</span></div><div class="line">    &#125;   </div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">376px</span>) and (max-width:<span class="number">414px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">12px</span></div><div class="line">    &#125;</div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">415px</span>) and (max-width:<span class="number">639px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">15px</span></div><div class="line">    &#125;</div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">640px</span>) and (max-width:<span class="number">719px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">20px</span></div><div class="line">    &#125;</div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">720px</span>) and (max-width:<span class="number">749px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">22.5px</span></div><div class="line">    &#125;</div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">750px</span>) and (max-width:<span class="number">799px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">23.5px</span></div><div class="line">    &#125;</div><div class="line">&#125; </div><div class="line">@<span class="keyword">media</span> screen and (min-width:<span class="number">800px</span>)&#123;</div><div class="line">    <span class="selector-tag">html</span>&#123;</div><div class="line">        <span class="attribute">font-size</span>:<span class="number">25px</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用媒体查询的缺点是不能所有设备全适配，但是用JS是可以实现全适配。具体使用上根据需求来定。</p>
]]></content>
      <tags>
        <tag>css</tag>
        <tag>webapp</tag>
        <tag>rem</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式语法详解</title>
    <url>/2016/06/02/regexp-grammar/</url>
    <content><![CDATA[<h1 id="正则表达式的定义"><a href="#正则表达式的定义" class="headerlink" title="正则表达式的定义"></a>正则表达式的定义</h1><p>可以使用Regexp（）构造函数来创建Regexp对象，不过更多的是通过一种特殊的直接量语法来创建。直接量定义为包含在一对斜杠（/）之间的字符。例如：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> pattern=<span class="regexp">/s$/</span></div></pre></td></tr></table></figure></p>
<p>ES3规范规定，一个正则表达式直接量在执行它时转换为一个Regexp对象，同一段代码表示的正则每次运算都指向同一个对象；ES5规范做了相反规定，每次运算都返回新的对象。</p>
<h1 id="直接量字符"><a href="#直接量字符" class="headerlink" title="直接量字符"></a>直接量字符</h1><p>直接量字符包含所有的字母和数字，以及转义字符。<br><strong>直接量字符列表：</strong></p>
<table>
<thead>
<tr>
<th>字符</th>
<th style="text-align:center">匹配</th>
</tr>
</thead>
<tbody>
<tr>
<td>字母和数字组合</td>
<td style="text-align:center">自身</td>
</tr>
<tr>
<td>\o</td>
<td style="text-align:center">NUL字符（\u0000）</td>
</tr>
<tr>
<td>\t</td>
<td style="text-align:center">制表符（\u0009）</td>
</tr>
<tr>
<td>\n</td>
<td style="text-align:center">换行符（）</td>
</tr>
<tr>
<td>\v</td>
<td style="text-align:center">垂直制表符</td>
</tr>
<tr>
<td>\f</td>
<td style="text-align:center">换页符</td>
</tr>
<tr>
<td>\r</td>
<td style="text-align:center">回车符</td>
</tr>
<tr>
<td>\xnn</td>
<td style="text-align:center">由16进制数nn指定的拉丁字符</td>
</tr>
<tr>
<td>\uxxxx</td>
<td style="text-align:center">由16进制数xxxx指定的Unicode字符</td>
</tr>
<tr>
<td>\cX</td>
<td style="text-align:center">控制字符^X，例如\cJ等价于换行符\n</td>
</tr>
</tbody>
</table>
<p><strong>在正则表达式中，许多标点符号具有特殊的含义，他们是：</strong><br><strong>^ $ . * + ? = ! : | \ / ( ) [ ] { } </strong><br>如果想匹配这些字符，<strong>同样需要\进行转义</strong>。其他标点符号（比如：@  ~  引号  逗号 分号  横杆）没有特殊含义，将按照字面含义进行匹配</p>
<h1 id="正则中需要转义的特殊字符"><a href="#正则中需要转义的特殊字符" class="headerlink" title="正则中需要转义的特殊字符"></a>正则中需要转义的特殊字符</h1><table>
<thead>
<tr>
<th>特别字符</th>
<th style="text-align:center">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>$</td>
<td style="text-align:center">匹配输入字符串的结尾位置。如果设置了RegExp对象的Multiline属性，则 $ 也匹配\n或\r。要匹配 $字符本身，请使用\</td>
</tr>
<tr>
<td>()</td>
<td style="text-align:center">标记一个子表达式的开始和结束位置，子表达式可以获取供以后使用。要匹配这些字符，请使用 \ ( 和 \ )</td>
</tr>
<tr>
<td>*</td>
<td style="text-align:center">匹配前面的子表达式零次或多次。要匹配 * 字符，请使用\</td>
</tr>
<tr>
<td>+</td>
<td style="text-align:center">匹配前面的子表达式一次或多次。要匹配 + 字符，请使用 \</td>
</tr>
<tr>
<td>.</td>
<td style="text-align:center">匹配除换行符 \n之外的任何单字符。要匹配 “.”，请使用 \</td>
</tr>
<tr>
<td>[]</td>
<td style="text-align:center">标记一个中括号表达式的开始。要匹配 [，请使用 \ [</td>
</tr>
<tr>
<td>?</td>
<td style="text-align:center">匹配前面的子表达式零次或一次，或指明一个非贪婪限定符。要匹配 ? 字符，请使用 \?。</td>
</tr>
<tr>
<td>\</td>
<td style="text-align:center">将下一个字符标记为或特殊字符、或原义字符、或向后引用、或八进制转义符例如， ‘n’ 匹配字符 ‘n’。’\n’ 匹配换行符。序列 ‘\ \’ 匹配 “\”，而 ‘\ (‘ 则匹配 “(”。</td>
</tr>
<tr>
<td>^</td>
<td style="text-align:center">匹配输入字符串的开始位置，除非在方括号表达式中使用，此时它表示不接受该字符集合。要匹配 ^ 字符本身，请使用 \^。</td>
</tr>
<tr>
<td>{}</td>
<td style="text-align:center">标记限定符表达式的开始。要匹配 {，请使用 \ {。</td>
</tr>
<tr>
<td>竖杠</td>
<td style="text-align:center">指明两项之间的一个选择。要匹配竖杠，请使用 \竖杠。</td>
</tr>
</tbody>
</table>
<h1 id="字符类"><a href="#字符类" class="headerlink" title="字符类"></a>字符类</h1><p>将字面量字符单独放进放括号内就组成了字符类。<br><strong>“^”符号可以否定字符类</strong><br><strong>“-”可以用来表示字符的范围</strong></p>
<table>
<thead>
<tr>
<th>字符</th>
<th style="text-align:center">匹配</th>
</tr>
</thead>
<tbody>
<tr>
<td>[…]</td>
<td style="text-align:center">方括号内的任意字符</td>
</tr>
<tr>
<td>[^…]</td>
<td style="text-align:center">不在方括号内的任意字符</td>
</tr>
<tr>
<td>.</td>
<td style="text-align:center">除换行符和其他unicode行终止符之外的任意字符）</td>
</tr>
<tr>
<td>\w</td>
<td style="text-align:center">任何ASCII字符组成的单词，等价于[a-zA-Z0-9_]</td>
</tr>
<tr>
<td>\W</td>
<td style="text-align:center">任何不是ASCII字符组成的单词，等价于[^a-zA-Z0-9_]</td>
</tr>
<tr>
<td>\s</td>
<td style="text-align:center">任何unicode空白字符</td>
</tr>
<tr>
<td>\S</td>
<td style="text-align:center">任何非unicode空白符的字符，<font color="#16a085">注意\w和\S不同</font></td>
</tr>
<tr>
<td>\d</td>
<td style="text-align:center">任何ASCII数字，等价于[0-9]</td>
</tr>
<tr>
<td>\D</td>
<td style="text-align:center">除了ASCII数字之外的任何字符，等价于[^0-9]</td>
</tr>
<tr>
<td>[\b]</td>
<td style="text-align:center">退格直接量（特例）</td>
</tr>
</tbody>
</table>
<p><strong>\w 表示匹配大小写英文字母、数字以及下划线，等价于[a-zA-Z0-9_]。</strong><br><strong>\S 表示匹配非空白字符，范围广，只要不是空格、换行符、制表符、换页符即可</strong></p>
<h1 id="重复"><a href="#重复" class="headerlink" title="重复"></a>重复</h1><p>正则模式之后跟随用以指定字符重复的标记。由于某些重复种类非常常用，因此就有一些专门用于表示这种情况的特殊字符。<br><strong>正则表达式的重复字符语法：</strong></p>
<table>
<thead>
<tr>
<th>字符</th>
<th style="text-align:center">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>{n,m}</td>
<td style="text-align:center">匹配前一项至少n次，但不能超过m次</td>
</tr>
<tr>
<td>{n,}</td>
<td style="text-align:center">匹配前一项n次或者更多次</td>
</tr>
<tr>
<td>{n}</td>
<td style="text-align:center">匹配前一项n次</td>
</tr>
<tr>
<td>?</td>
<td style="text-align:center">匹配前一项0次或者1次，也就是说<strong>前一项可选</strong>，等价于{0,1}</td>
</tr>
<tr>
<td>+</td>
<td style="text-align:center">匹配前一项1次或者多次，等价于{1,}</td>
</tr>
<tr>
<td>*</td>
<td style="text-align:center">匹配前一项0次或者多次，等价于{0,}</td>
</tr>
</tbody>
</table>
<p>例子：<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">/\d&#123;2,4&#125;/            //匹配2~4个数字</div><div class="line">/\w&#123;3&#125;\d?/           //匹配3个单词和一个可选的数字</div><div class="line">/\s+java\s+/         //匹配前后带有一个或多个空格的字符串“java”</div><div class="line">/[^(]*/			     //匹配0个或者多个非左括号的字符</div></pre></td></tr></table></figure></p>
<p>在使用星号和问号时要注意，由于这些字符可能匹配0个字符，因此他们允许什么都不匹配。例如正则/ a*/实际上与字符串“bbbb”匹配，因为这个字符串含有0个a。</p>
<p><strong>贪婪匹配和非贪婪匹配</strong><br>以上的重复匹配属于贪婪的匹配，即尽可能多的匹配；若要尽可能地少匹配，只需在重复字符后面跟随一个“?”即可。<br>比如正则表达式/a+/可以匹配一个或多个连续的字母a。当使用“aaa”作为匹配字符串时，正则表达式会匹配它的三个字符。但是/a+?/也可以匹配一个或多个连续字母a，同样以“aaa”作为匹配字符串，此时只匹配第一个a。</p>
<p>但是，非贪婪匹配可能和所匹配的预期不一致。<strong>这里提一下正则的机制，它总是会寻找字符串中第一个可能匹配的位置</strong>。比如/a+?b/，当用它来匹配“aaab”时，它实际上匹配了整个字符串，而非匹配一个a和最后一个b。</p>
<h1 id="选择、分组和引用"><a href="#选择、分组和引用" class="headerlink" title="选择、分组和引用"></a>选择、分组和引用</h1><p><strong>选择字符</strong>：|<br>说明：选择字符匹配尝试从左到右，直到发现匹配项。如果左边的选择项匹配，就忽略右边匹配项。</p>
<p><strong>组合字符</strong>：(…) 和(? : …)</p>
<p><strong>引用字符</strong>：\n，n为大于等于1的正整数，表示引用与第n个分组相匹配的<strong>文本的引用</strong>,而非子表达式模式的引用。例如：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">/[<span class="string">'"][^'</span><span class="string">"]*['"</span>]/</div></pre></td></tr></table></figure></p>
<p>匹配的是位于单引号或双引号之内的0个或多个字符，但是，它并不要求左侧和右侧的引号相同。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">/([<span class="string">'"])[^'</span><span class="string">"]*\1/</span></div></pre></td></tr></table></figure></p>
<p>匹配位于单引号或双引号之内的0个或多个字符，并且左右两侧引号必须相同，这就是<strong>文本引用</strong>产生的结果。</p>
<p><strong>正则表达式的选择、分组、和引用字符</strong></p>
<table>
<thead>
<tr>
<th>字符</th>
<th style="text-align:center">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>竖杠</td>
<td style="text-align:center">选择，匹配的是该符号左边的子表达式或右边的子表达式</td>
</tr>
<tr>
<td>(…)</td>
<td style="text-align:center">将几个项组合为一个单元，这个单元可以通过“*”，“+”，“?”等符号加以修饰，而且可以记住和这个组合相匹配的字符串以供此后的引用使用。</td>
</tr>
<tr>
<td>(?:…)</td>
<td style="text-align:center">只组合，把项组合成一个单元，但不记忆与改组相匹配的字符</td>
</tr>
<tr>
<td>\n</td>
<td style="text-align:center">引用第n个分组第一次匹配的字符，组是圆括号中的子表达式（也有可能是嵌套的），组索引是从左到右的左括号数，“?：”形式的分组不编码</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>正则</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式常用属性及方法</title>
    <url>/2016/06/02/regexp-property-method/</url>
    <content><![CDATA[<h1 id="正则实例对象的5个属性"><a href="#正则实例对象的5个属性" class="headerlink" title="正则实例对象的5个属性"></a>正则实例对象的5个属性</h1><ul>
<li><strong>global：</strong>是否全局搜索，默认是false</li>
<li><strong>ignoreCase：</strong>是否大小写敏感，默认是false</li>
<li><strong>multiline：</strong>多行搜索，默认值是false</li>
<li><strong>lastIndex：</strong>是当前表达式模式首次匹配内容中最后一个字符的下一个位置，每次正则表达式成功匹配时，lastIndex属性值都会随之改变。</li>
<li><strong>source：</strong>正则表达式的文本字符串</li>
</ul>
<h1 id="正则表达式相关方法"><a href="#正则表达式相关方法" class="headerlink" title="正则表达式相关方法"></a>正则表达式相关方法</h1><h2 id="正则表达式对象"><a href="#正则表达式对象" class="headerlink" title="正则表达式对象"></a>正则表达式对象</h2><h3 id="regObj-test-strObj"><a href="#regObj-test-strObj" class="headerlink" title="regObj.test(strObj)"></a>regObj.test(strObj)</h3><p>方法用于测试字符串参数中是否存正则表达式模式，如果存在则返回true，否则返回false</p>
<h3 id="regObj-exec-strObj"><a href="#regObj-exec-strObj" class="headerlink" title="regObj.exec(strObj)"></a>regObj.exec(strObj)</h3><p>方法用于正则表达式模式在字符串中运行查找，如果 exec() 找到了匹配的文本，则返回一个结果数组。否则，返回 null。除了数组元素和 length 属性之外，exec() 方法还返回两个属性。index 属性声明的是匹配文本的第一个字符的位置。input 属性则存放的是被检索的字符串 string。</p>
<p>调用非全局的 RegExp对象的 exec() 时，返回数组的第 0 个元素是与正则表达式相匹配的文本，第 1 个元素是与 RegExpObject 的第 1 个子表达式相匹配的文本（如果有的话），第 2 个元素是与 RegExp对象的第 2 个子表达式相匹配的文本（如果有的话），以此类推。</p>
<p>调用全局的RegExp对象的 exec() 时，它会在 RegExp实例的 lastIndex 属性指定的字符处开始检索字符串 string。当 exec() 找到了与表达式相匹配的文本时，在匹配后，它将把 RegExp实例的 lastIndex 属性设置为匹配文本的最后一个字符的下一个位置。可以通过反复调用 exec() 方法来遍历字符串中的所有匹配文本。当 exec() 再也找不到匹配的文本时，它将返回 null，并把 lastIndex 属性重置为 0。</p>
<h2 id="字符串对象"><a href="#字符串对象" class="headerlink" title="字符串对象"></a>字符串对象</h2><h3 id="strObj-search-RegObj"><a href="#strObj-search-RegObj" class="headerlink" title="strObj.search(RegObj)"></a>strObj.search(RegObj)</h3><p>search() 方法用于检索字符串中指定的子字符串，或检索与正则表达式相匹配的子字符串。search() 方法不执行全局匹配，它将忽略标志 g。它同时忽略 regexp 的 lastIndex 属性，并且总是从字符串的开始进行检索，这意味着它总是返回 stringObject 的第一个匹配的位置。</p>
<h3 id="strObj-match-RegObj"><a href="#strObj-match-RegObj" class="headerlink" title="strObj.match(RegObj)"></a>strObj.match(RegObj)</h3><p>match() 方法将检索字符串 stringObject，以找到一个或多个与 regexp 匹配的文本。但regexp是否具有标志 g对结果影响很大。</p>
<p>如果 regexp 没有标志 g，那么 match() 方法就只能在 strObj 中执行一次匹配。如果没有找到任何匹配的文本， match() 将返回 null。否则，它将返回一个数组，其中存放了与它找到的匹配文本有关的信息。该数组的第 0 个元素存放的是匹配文本，而其余的元素存放的是与正则表达式的子表达式匹配的文本。除了这些常规的数组元素之外，返回的数组还含有两个对象属性。index 属性声明的是匹配文本的起始字符在 stringObject 中的位置，input 属性声明的是对 stringObject 的引用。</p>
<h3 id="strObj-replace-regObj-replaceStr"><a href="#strObj-replace-regObj-replaceStr" class="headerlink" title="strObj.replace(regObj,replaceStr)"></a>strObj.replace(regObj,replaceStr)</h3><p>关于strng对象的replace方法，我们最常用的时传入两个字符串的做法，但这种做法有个缺陷，只能replace一次<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'abcabcabc'</span>.replace(<span class="string">'bc'</span>,<span class="string">'X'</span>); <span class="comment">//aXabcabc</span></div></pre></td></tr></table></figure></p>
<p>replace方法的第一个参数还可以传入RegExp对象，传入正则表达式可以时replace方法更加强大灵活<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'abcabcabc'</span>.replace(<span class="regexp">/bc/g</span>,<span class="string">'X'</span>); <span class="comment">//aXaXaX</span></div><div class="line"><span class="string">'abcaBcabC'</span>.replace(<span class="regexp">/bc/gi</span>,<span class="string">'X'</span>); <span class="comment">//aXaXaX</span></div></pre></td></tr></table></figure></p>
<h3 id="strObj-replace-regObj-function"><a href="#strObj-replace-regObj-function" class="headerlink" title="strObj.replace(regObj,function(){})"></a>strObj.replace(regObj,function(){})</h3><p>可以通过修改replace方法的第二个参数，使replace更加强大，在前面的介绍中，只能把所有匹配替换为固定内容，但如果我希望把一个字符串中所有数字，都用小括号包起来该怎么弄</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'2398rufdjg9w45hgiuerhg83ghvif'</span>.replace(<span class="regexp">/\d+/g</span>,<span class="function"><span class="keyword">function</span>(<span class="params">r</span>)</span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="string">'('</span>+r+<span class="string">')'</span>;</div><div class="line">&#125;); <span class="comment">//"(2398)rufdjg(9)w(45)hgiuerhg(83)ghvif"</span></div></pre></td></tr></table></figure>
<p>把replace方法的第二个参数传入一个function，这个function会在每次匹配替换的时候调用，算是个每次替换的回调函数，我们使用了回调函数的第一个参数，也就是匹配内容，其实回调函数一共有四个参数</p>
<ul>
<li>第一个参数很简单，是匹配字符串</li>
<li>第二个参数是正则表达式分组内容，没有分组则没有该参数</li>
<li>第三个参数是匹配项在字符串中的index</li>
<li>第四个参数则是原字符串</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'2398rufdjg9w45hgiuerhg83ghvif'</span>.replace(<span class="regexp">/\d+/g</span>,<span class="function"><span class="keyword">function</span>(<span class="params">a,b,c</span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(a+<span class="string">'\t'</span>+b+<span class="string">'\t'</span>+c);</div><div class="line">    <span class="keyword">return</span> <span class="string">'('</span>+a+<span class="string">')'</span>;</div><div class="line">&#125;); </div><div class="line"></div><div class="line"><span class="number">2398</span>    <span class="number">0</span>    <span class="number">2398</span>rufdjg9w45hgiuerhg83ghvif</div><div class="line"><span class="number">9</span>    <span class="number">10</span>    <span class="number">2398</span>rufdjg9w45hgiuerhg83ghvif</div><div class="line"><span class="number">45</span>    <span class="number">12</span>    <span class="number">2398</span>rufdjg9w45hgiuerhg83ghvif</div><div class="line"><span class="number">83</span>    <span class="number">22</span>    <span class="number">2398</span>rufdjg9w45hgiuerhg83ghvif</div></pre></td></tr></table></figure>
<p>这是没有分组的情况，打印出来的分别是 匹配内容、匹配项index和原字符串，看个有分组的例子，如果我们希望把一个字符串的&lt;%%&gt;外壳去掉，&lt;%1%&gt;&lt;%2%&gt;&lt;%3%&gt; 变成123</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'&lt;%1%&gt;&lt;%2%&gt;&lt;%3%&gt;'</span>.replace(<span class="regexp">/&lt;%([^%&gt;]+)%&gt;/g</span>,<span class="function"><span class="keyword">function</span>(<span class="params">a,b,c,d</span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(a+<span class="string">'\t'</span>+b+<span class="string">'\t'</span>+c+<span class="string">'\t'</span>+d);</div><div class="line">    <span class="keyword">return</span> b;</div><div class="line">&#125;) <span class="comment">//123</span></div><div class="line"></div><div class="line">&lt;%<span class="number">1</span>%&gt;    <span class="number">1</span>    <span class="number">0</span>    &lt;%<span class="number">1</span>%&gt;<span class="xml"><span class="tag">&lt;<span class="name">%2%</span>&gt;</span><span class="tag">&lt;<span class="name">%3%</span>&gt;</span> </span></div><div class="line"><span class="tag">&lt;<span class="name">%2%</span>&gt;</span>    2    5    <span class="tag">&lt;<span class="name">%1%</span>&gt;</span><span class="tag">&lt;<span class="name">%2%</span>&gt;</span><span class="tag">&lt;<span class="name">%3%</span>&gt;</span> </div><div class="line"><span class="tag">&lt;<span class="name">%3%</span>&gt;</span>    3    10    <span class="tag">&lt;<span class="name">%1%</span>&gt;</span><span class="tag">&lt;<span class="name">%2%</span>&gt;</span><span class="tag">&lt;<span class="name">%3%</span>&gt;</span></div></pre></td></tr></table></figure>
<p>根据这种参数replace可以实现很多强大的功能，尤其是在复杂的字符串替换语句中经常使用。</p>
<h3 id="strObj-split-regObj"><a href="#strObj-split-regObj" class="headerlink" title="strObj.split(regObj)"></a>strObj.split(regObj)</h3><p>我们经常使用split方法把字符串分割为字符数组<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'a,b,c,d'</span>.split(<span class="string">','</span>); <span class="comment">//["a", "b", "c", "d"]</span></div></pre></td></tr></table></figure></p>
<p>和replace方法类似，在一些复杂的分割情况下我们可以使用正则表达式解决<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">'a1b2c3d'</span>.split(<span class="regexp">/\d/</span>); <span class="comment">//["a", "b", "c", "d"]</span></div></pre></td></tr></table></figure></p>
<p>这样就可以按照数字分割字符串了。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>正则</tag>
      </tags>
  </entry>
  <entry>
    <title>省、市、区三级联动菜单Demo</title>
    <url>/2016/06/08/related-menu/</url>
    <content><![CDATA[<ul>
<li>前端模拟数据，省略请求数据</li>
<li>封装成jQuery插件形式</li>
<li>省、市、区选择弹出浮层信息</li>
</ul>
<p>核心代码：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//初始化省</span></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">province</span>(<span class="params"></span>)</span>&#123;</div><div class="line">        $.each(areaJson, <span class="function"><span class="keyword">function</span>(<span class="params">index, val</span>) </span>&#123; </div><div class="line">             temp_option+=<span class="string">"&lt;option value="</span>+val.p+<span class="string">"&gt;"</span>+val.p+<span class="string">"&lt;/option&gt;"</span>;</div><div class="line">             oProvince.html(temp_option);</div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">//根据省赋值市</span></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">city</span>(<span class="params"></span>)</span>&#123;</div><div class="line">        temp_option=<span class="string">""</span>+chooseC;</div><div class="line">        <span class="keyword">var</span> proIndex=oProvince.get(<span class="number">0</span>).selectedIndex<span class="number">-1</span>;</div><div class="line">        <span class="keyword">if</span>(proIndex&gt;=<span class="number">0</span>)&#123;</div><div class="line">            $.each(areaJson[proIndex].c, <span class="function"><span class="keyword">function</span>(<span class="params">index, val</span>) </span>&#123;</div><div class="line">                 temp_option+=<span class="string">"&lt;option value="</span>+val.ct+<span class="string">"&gt;"</span>+val.ct+<span class="string">"&lt;/option&gt;"</span>;</div><div class="line">                 oCity.html(temp_option);</div><div class="line">                 oDis.html(chooseD);</div><div class="line">            &#125;);</div><div class="line">        &#125;<span class="keyword">else</span>&#123;</div><div class="line">            oCity.html(chooseC);</div><div class="line">            oDis.html(chooseD);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    <span class="comment">//赋值区|县</span></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">district</span>(<span class="params"></span>)</span>&#123;</div><div class="line">        temp_option=<span class="string">""</span>+chooseD;</div><div class="line">        <span class="keyword">var</span> proIndex=oProvince.get(<span class="number">0</span>).selectedIndex<span class="number">-1</span>;</div><div class="line">        <span class="keyword">var</span> cityIndex=oCity.get(<span class="number">0</span>).selectedIndex<span class="number">-1</span>;</div><div class="line">        <span class="keyword">if</span>( proIndex&gt;=<span class="number">0</span> &amp;&amp; cityIndex&gt;=<span class="number">0</span>)&#123;</div><div class="line">            <span class="keyword">if</span>(<span class="keyword">typeof</span>(areaJson[proIndex].c[cityIndex].d)!==<span class="string">"undefined"</span>)&#123;</div><div class="line">                $.each(areaJson[proIndex].c[cityIndex].d, <span class="function"><span class="keyword">function</span>(<span class="params">index, val</span>) </span>&#123;</div><div class="line">                     temp_option+=<span class="string">"&lt;option value="</span>+val.dt+<span class="string">"&gt;"</span>+val.dt+<span class="string">"&lt;/option&gt;"</span>;</div><div class="line">                     oDis.html(temp_option);</div><div class="line">                &#125;);</div><div class="line">            &#125;<span class="keyword">else</span>&#123;</div><div class="line">                showInfo();</div><div class="line">            &#125;                   </div><div class="line">        &#125;<span class="keyword">else</span>&#123;</div><div class="line">            oDis.html(chooseD);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">showInfo</span>(<span class="params"></span>)</span>&#123;</div><div class="line">        <span class="keyword">var</span> proIndex=oProvince.get(<span class="number">0</span>).selectedIndex;</div><div class="line">        <span class="keyword">var</span> cityIndex=oCity.get(<span class="number">0</span>).selectedIndex;</div><div class="line">        <span class="keyword">var</span> disIndex=oDis.get(<span class="number">0</span>).selectedIndex;</div><div class="line">        <span class="keyword">var</span> p=oProvince.get(<span class="number">0</span>).options[proIndex].value;</div><div class="line">        <span class="keyword">var</span> c=oCity.get(<span class="number">0</span>).options[cityIndex].value;</div><div class="line">        <span class="keyword">var</span> d=disIndex&gt;<span class="number">0</span>?oDis.get(<span class="number">0</span>).options[disIndex].value:<span class="string">""</span>;</div><div class="line">        oShow.css(<span class="string">"display"</span>,<span class="string">"block"</span>).find(<span class="string">"p"</span>).html(p+c+d);</div><div class="line">    &#125;</div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">closeInfo</span>(<span class="params"></span>)</span>&#123;</div><div class="line">        <span class="keyword">var</span> btnClose=oShow.find(<span class="string">".btn-close"</span>);</div><div class="line">        <span class="keyword">var</span> btnOk=oShow.find(<span class="string">"button"</span>).eq(<span class="number">0</span>);</div><div class="line">        <span class="keyword">var</span> btnReset=oShow.find(<span class="string">"button"</span>).eq(<span class="number">1</span>);</div><div class="line">        <span class="keyword">var</span> btnArr=[btnClose,btnOk,btnReset];</div><div class="line">        $.each(btnArr, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">             $(<span class="keyword">this</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">                oShow.css(<span class="string">"display"</span>,<span class="string">"none"</span>);</div><div class="line">             &#125;);</div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//调用初始化省</span></div><div class="line">    province();</div><div class="line">    <span class="comment">//省发生变化改变市</span></div><div class="line">    oProvince.change(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        city();</div><div class="line">    &#125;);</div><div class="line">    <span class="comment">//市发生变化改变区|县</span></div><div class="line">    oCity.change(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        district();</div><div class="line">    &#125;);</div><div class="line">    <span class="comment">//区|县选择完后弹出浮层信息框</span></div><div class="line">    oDis.change(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        showInfo();</div><div class="line">    &#125;);</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>如何在CentOS7上安装Python3并设置本地编程环境</title>
    <url>/2018/07/08/setup-python3-on-centos7/</url>
    <content><![CDATA[<p>本文基于引用文章（见尾部）和个人搭建python3.7.0环境的过程进行总结和分享。</p>
<h2 id="安装环境说明"><a href="#安装环境说明" class="headerlink" title="安装环境说明"></a>安装环境说明</h2><ul>
<li>主机：centos7</li>
<li>安装python版本：3.7.0</li>
</ul>
<h2 id="安装依赖环境"><a href="#安装依赖环境" class="headerlink" title="安装依赖环境"></a>安装依赖环境</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line"># yum -y install zlib-devel bzip2-devel openssl-devel ncurses-devel sqlite-devel readline-devel tk-devel gdbm-devel db4-devel libpcap-devel xz-devel libffi-devel</div></pre></td></tr></table></figure>
<p>-y参数：自动选择，不弹出选择对话，默认yes</p>
<p>若不先安装依赖环境，在后面编译的步骤会报错缺少依赖，如：找不到zlib包<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">zipimport.ZipImportError: can&apos;t decompress data; zlib not available</div></pre></td></tr></table></figure></p>
<p>注意：上面尾部的包libffi-devel，是python3.7版本的新依赖，若此包没安装，编译时会报错<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">ModuleNotFoundError: No module named &apos;_ctypes&apos;</div></pre></td></tr></table></figure></p>
<h2 id="获取python源文件"><a href="#获取python源文件" class="headerlink" title="获取python源文件"></a>获取python源文件</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">wget https://www.python.org/ftp/python/3.7.0/Python-3.7.0.tgz</div></pre></td></tr></table></figure>
<h2 id="解压缩"><a href="#解压缩" class="headerlink" title="解压缩"></a>解压缩</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">tar -zxvf Python-3.7.0.tgz</div></pre></td></tr></table></figure>
<h2 id="执行configure检测安装平台特征"><a href="#执行configure检测安装平台特征" class="headerlink" title="执行configure检测安装平台特征"></a>执行configure检测安装平台特征</h2><p>./configure是用来检测你的安装平台的目标特征的。比如它会检测你是不是有CC或GCC，并不是需要CC或GCC，它是个shell脚本。</p>
<p>这一步一般用来生成 Makefile，为下一步的编译做准备</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">cd Python-3.7.0</div><div class="line">./configure</div></pre></td></tr></table></figure>
<p>不出意外，会提示<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">configure: error: no acceptable C compiler found in $PATH</div></pre></td></tr></table></figure></p>
<p>原因是没有安装gcc，因为python是用C写的，所以需要用gcc进行编译，所以需要先安装gcc</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line"># 顺道把c++编译器也安装了</div><div class="line">yum install gcc gcc-c++</div></pre></td></tr></table></figure>
<h2 id="再次执行-configure"><a href="#再次执行-configure" class="headerlink" title="再次执行./configure"></a>再次执行./configure</h2><p>几分钟之内发现一系列的checking日志被打出（觉得检测时间太久此步可忽略）：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">checking linux/tipc.h presence... yes</div><div class="line">checking for linux/tipc.h... yes</div><div class="line">checking linux/random.h usability... yes</div><div class="line">checking linux/random.h presence... yes</div><div class="line">checking for linux/random.h... yes</div><div class="line">checking spawn.h usability... yes</div><div class="line">checking spawn.h presence... yes</div><div class="line">checking for spawn.h... yes</div><div class="line">checking util.h usability... no</div><div class="line">checking util.h presence... no</div><div class="line">checking for util.h... no</div><div class="line">checking alloca.h usability... yes</div><div class="line">checking alloca.h presence... yes</div><div class="line">checking for alloca.h... yes</div><div class="line">checking endian.h usability... yes     ....</div></pre></td></tr></table></figure></p>
<p>ok，没有报错进入下一步编译</p>
<h2 id="执行make进行编译，执行make-install进行安装"><a href="#执行make进行编译，执行make-install进行安装" class="headerlink" title="执行make进行编译，执行make install进行安装"></a>执行make进行编译，执行make install进行安装</h2><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line"># (注：make install足够的权限)</div><div class="line">make &amp;&amp; make install</div></pre></td></tr></table></figure>
<ul>
<li><p>make：编译，可能遇到的错误：make <em>*</em> 没有指明目标并且找不到 makefile。 没有Makefile，解决方法是要先./configure 一下生成Makefile。</p>
</li>
<li><p>make install：将程序安装至系统中。如果原始码编译无误，且执行结果正确，便可以把程序安装至系统预设的可执行文件存放路径。如果用bin_PROGRAMS宏的话，程序会被安装至/usr/local/bin这个目录。</p>
</li>
</ul>
<p>查看是否安装成功：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">python3 -V    // 输出 Python3.7.0</div><div class="line">python -V     // 输出 python2.7.6</div></pre></td></tr></table></figure></p>
<h2 id="设置虚拟环境"><a href="#设置虚拟环境" class="headerlink" title="设置虚拟环境"></a>设置虚拟环境</h2><p>成功安装python后，我们需要为python项目创建虚拟环境。</p>
<p>虚拟环境为Python项目创建一个隔离空间，确保每个项目都有自己的一组依赖项，这些依赖项不会破坏任何其他项目。</p>
<p>设置编程环境使我们能够更好地控制Python项目以及如何处理不同版本的包。在使用第三方软件包时，这一点尤为重要。</p>
<p>新建项目目录<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">cd ~</div><div class="line">mkdir myproject</div><div class="line">cd myproject</div></pre></td></tr></table></figure></p>
<p>运行以下命令来创建独立环境：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">python3 -m venv my_env</div></pre></td></tr></table></figure></p>
<p>本质上，此命令创建一个新目录（在本例中称为my_env），其中包含我们可以使用以下ls命令查看的一些项：<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">bin  include  lib  lib64  pyvenv.cfg</div></pre></td></tr></table></figure></p>
<p>到此为止，环境创建完成，要使用该虚拟环境，还需要执行激活虚拟环境命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">source my_env/bin/activate</div></pre></td></tr></table></figure></p>
<p>可以看到linux操作提示符前缀变为<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">(my_env) [root@localhost myproject]#</div></pre></td></tr></table></figure></p>
<p>这个前缀让我们知道环境my_env当前是活动的，这意味着当我们在这里创建程序时，它们将只使用这个特定环境的设置和包。</p>
<p>注意：我们使用python3创建的虚拟环境，在虚拟环境类，我们的python版本默认为python3.7.0，退出虚拟环境后python默认版本仍为2.7版本。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">(my_env) [root@localhost bin]# python -V   // 输出Python 3.7.0</div></pre></td></tr></table></figure></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">[root@localhost ~]# python -V         // 输出Python 2.7.5</div></pre></td></tr></table></figure>
<p>想退出python虚拟环境，只需执行deactivate命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">(my_env) [root@localhost myproject]# deactivate</div></pre></td></tr></table></figure></p>
<blockquote>
<p>参考链接：<a href="https://www.digitalocean.com/community/tutorials/how-to-install-python-3-and-set-up-a-local-programming-environment-on-centos-7" target="_blank" rel="external">https://www.digitalocean.com/community/tutorials/how-to-install-python-3-and-set-up-a-local-programming-environment-on-centos-7</a></p>
</blockquote>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>关于异步函数的回调问题</title>
    <url>/2017/03/30/solve-asyncfun-callback-problem/</url>
    <content><![CDATA[<p>我们在项目中经常需要根据异步请求结果进行相关操作，以往常用的方法是callback，但现在如果结合babel编译，用promise或者async会更加优雅。如下是错误的做法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 异步之后进行相关操作的错误做法</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFun</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">var</span> flag = <span class="literal">false</span>;</div><div class="line">	$.ajax(&#123;</div><div class="line">		<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">		<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">	&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">		<span class="keyword">if</span>(res.code == <span class="number">1</span>)&#123;</div><div class="line">			flag = <span class="literal">true</span></div><div class="line">		&#125;</div><div class="line">	&#125;)</div><div class="line">	<span class="keyword">return</span> flag;    <span class="comment">// 这里始终返回false，因为异步请求不阻塞后面代码执行</span></div><div class="line">&#125;</div><div class="line"><span class="keyword">if</span>(asyncFun())&#123;</div><div class="line">	<span class="comment">// 由于flag始终为false，很明显这里永远不会执行</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>解决方案有三种：传统回调方式，ES5Promise对象，ES6 async/await方式</p>
<h1 id="回调函数方式"><a href="#回调函数方式" class="headerlink" title="回调函数方式"></a>回调函数方式</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 定义一个异步函数</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFun</span>(<span class="params">cb</span>)</span>&#123;</div><div class="line">	$.ajax(&#123;</div><div class="line">		<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">		<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">	&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">		cb &amp;&amp; cb(res)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line">asyncFun(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">	<span class="comment">// 根据异步请求返回的结果进行相关操作</span></div><div class="line">	<span class="keyword">if</span>(res.code == <span class="number">1</span>)&#123;</div><div class="line"></div><div class="line">	&#125;</div><div class="line">&#125;)</div></pre></td></tr></table></figure>
<h1 id="使用ES6引入的promise对象"><a href="#使用ES6引入的promise对象" class="headerlink" title="使用ES6引入的promise对象"></a>使用ES6引入的promise对象</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFun</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>)</span>&#123;</div><div class="line">		$.ajax(&#123;</div><div class="line">			<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">			<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">		&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">			resolve(res)</div><div class="line">		&#125;)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line"></div><div class="line">asyncFun().then(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">	<span class="keyword">if</span>(res.code == <span class="number">1</span>)&#123;</div><div class="line">		<span class="comment">// 你要进行相关的操作</span></div><div class="line">	&#125;</div><div class="line">&#125;)</div></pre></td></tr></table></figure>
<p>如果后面的操作不只一次，你可以定义多个返回Promise对象的函数，像这样<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFun2</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">	<span class="comment">// 这里可以获取asyncFun传过来的res</span></div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>)</span>&#123;</div><div class="line">		$.ajax(&#123;</div><div class="line">			<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">			<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">		&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res2</span>)</span>&#123;</div><div class="line">			resolve(res2)</div><div class="line">		&#125;)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFun3</span>(<span class="params">res2</span>)</span>&#123;</div><div class="line">	<span class="comment">// 这里可以获取asyncFun2传过来的res2</span></div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>)</span>&#123;</div><div class="line">		$.ajax(&#123;</div><div class="line">			<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">			<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">		&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res3</span>)</span>&#123;</div><div class="line">			resolve(res3)</div><div class="line">		&#125;)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line">asyncFun().then(asyncFun2).then(asyncFun3)</div></pre></td></tr></table></figure></p>
<p>说明：</p>
<ul>
<li>这种写法十分类似jQuery的链式写法，只要返回<strong>Promise对象，就拥有then()及catch()</strong>方法，后者用于捕捉回调运行时发生的错误。</li>
<li>Promise构造函数接受一个函数作为参数，该函数的两个参数分别是resolve方法和reject方法。如果异步操作成功，则用resolve方法将Promise对象的状态变为“成功”（即从<strong>pending变为resolved</strong>）；如果异步操作失败，则用reject方法将状态变为“失败”（即从<strong>pending变为rejected</strong>）</li>
<li>then方法其实有两个参数，<strong>第一个参数</strong>作为上一步操作<strong>resolve时的回调函数</strong>，如果上一步操作后<strong>状态变为rejected</strong>，则以<strong>第二个参数作为回调函数</strong></li>
</ul>
<h1 id="使用ES7关键字async-await——异步终极解决方案"><a href="#使用ES7关键字async-await——异步终极解决方案" class="headerlink" title="使用ES7关键字async/await——异步终极解决方案"></a>使用ES7关键字async/await——异步终极解决方案</h1><p>ES7 中有了更加标准的解决方案，新增了<strong>async/await</strong>两个关键词，async可以声明一个异步函数，此函数需要返回一个 Promise对象。await 可以等待一个 Promise 对象 resolve，并拿到结果。<br>现在要实现上面的需求，可以这样：</p>
<p>基本规则</p>
<ul>
<li>async 表示这是一个async函数，await只能用在这个函数里面。</li>
<li>await 表示在这里等待promise返回结果了，再继续执行。</li>
<li>await 后面跟着的应该是一个promise对象（当然，其他返回值也没关系，只是会立即执行，不过那样就没有意义）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getData</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve,reject</span>)</span>&#123;</div><div class="line">		$.ajax(&#123;</div><div class="line">			<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">			<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">		&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">			resolve(res)</div><div class="line">		&#125;)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">asyncFun</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">var</span> res = <span class="keyword">await</span> getData();</div><div class="line">	<span class="keyword">if</span>(res.code == <span class="number">1</span>)&#123;</div><div class="line">		<span class="comment">//进行相关操作</span></div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h1 id="使用async-await捕获异常"><a href="#使用async-await捕获异常" class="headerlink" title="使用async/await捕获异常"></a>使用async/await捕获异常</h1><p>这里说下捕获异步操作中的异常问题，既然then不用写，那么catch也可以不用了，可以直接用标准的try/catch语法捕捉错误。</p>
<p>常犯的错误是使用try/catch捕获异步函数的异常，这时候因为try/catch块执行完了，异步函数很可能还没有完成，异常还没有被实际抛出。当异常被抛出的时候，由于catch块已经执行完了，此时就会导致进程崩溃。如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 错误的示例</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getData</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	$.ajax(&#123;</div><div class="line">		<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">		<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">	&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">		<span class="comment">// 模拟异常</span></div><div class="line">		<span class="literal">null</span>();</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line"><span class="keyword">try</span>&#123;</div><div class="line">	getData();</div><div class="line">	<span class="built_in">console</span>.log(<span class="string">'我会被打印'</span>);</div><div class="line">&#125;<span class="keyword">catch</span>(e)&#123;</div><div class="line">	<span class="built_in">console</span>.log(<span class="string">'error'</span>)</div><div class="line">&#125;</div><div class="line"><span class="comment">// '我会被打印'</span></div><div class="line"><span class="comment">// undefined</span></div><div class="line"><span class="comment">// Uncaught TypeError: null is not a function</span></div></pre></td></tr></table></figure></p>
<p>可以看到异步请求中的异常无法被正常捕获,’error’并没有被打印出来，console.log(‘我会被打印’)仍然执行了，使用async/await可以很容易解决这个问题</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getData</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve,reject</span>)</span>&#123;</div><div class="line">		$.ajax(&#123;</div><div class="line">			<span class="attr">url</span>: <span class="string">'https://api.github.com/'</span>,</div><div class="line">			<span class="attr">type</span>: <span class="string">'get'</span></div><div class="line">		&#125;).done(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">			<span class="comment">// 模拟异常</span></div><div class="line">			reject(<span class="string">'error'</span>)</div><div class="line">		&#125;)</div><div class="line">	&#125;)</div><div class="line">&#125;</div><div class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">asyncFun</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	<span class="keyword">try</span>&#123;</div><div class="line">		<span class="keyword">await</span> getData();</div><div class="line">		<span class="comment">// 这里以下的代码不被执行</span></div><div class="line">		<span class="built_in">console</span>.log(<span class="string">'我不会被打印'</span>);</div><div class="line">	&#125;<span class="keyword">catch</span>(err)&#123;</div><div class="line">		<span class="built_in">console</span>.log(err);</div><div class="line">	&#125;</div><div class="line">&#125;</div><div class="line"><span class="keyword">async</span>();       <span class="comment">// Promise&#123;&#125;</span></div><div class="line">			   <span class="comment">// error</span></div></pre></td></tr></table></figure>
<h1 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h1><ol>
<li>从回调到promise再到async/await解决步式操作需求的问题，可以看到代码易读性明显增强了，async/await不仅去除了嵌套问题，也不用再用链式写法的then了。</li>
<li>async/await只是一套语法糖，其他语言的async/await可能是协程或者多线程编程的语法糖，JS本身是单线程的，async/await与传统的callback或者promise执行起来并无两样</li>
<li>ES7 async/await仍未成标准，最新版谷歌已经支持，但大部分浏览器仍没有实现，要使用它可以用babel编译成浏览器支持的ES5。</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>es6</tag>
      </tags>
  </entry>
  <entry>
    <title>2016.10.3长沙之旅</title>
    <url>/2016/10/03/traveling-of-changsha/</url>
    <content><![CDATA[<p><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-1.jpg" alt="蓝瘦"><br><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-2.jpg" alt="橘子洲夕阳"><br><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-3.jpg" alt="极致美颜"><br><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-4.jpg" alt="万达"><br><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-5.jpg" alt="湖南大学"><br><img src="http://qiniu.huzerui.com/image/2016-10-11-travel-of-changsha-6.jpg" alt="湖南电视台"></p>
]]></content>
      <categories>
        <category>旅行</category>
      </categories>
      <tags>
        <tag>旅行</tag>
      </tags>
  </entry>
  <entry>
    <title>Vuejs2.0 Demo</title>
    <url>/2017/02/04/vuejs-demo/</url>
    <content><![CDATA[<p>前段时间利用闲暇时间学习Vuejs并且做了一个实例，主要使用vue-router实现路由控制，vuex对程序进行状态管理，axios发送http请求。对数据在组件间的传递、webpack打包工具、数据的双向绑定有更深刻的了解和认识。<br><img src="http://qiniu.huzerui.com/image/2017-02-04-vue2.0-demo-poster.gif" alt="预览图"></p>
<p>项目在线地址：<a href="http://www.huzerui.com/vue2.0-demo/" target="_blank" rel="external">http://www.huzerui.com/vue2.0-demo/</a><br>Github地址：<a href="https://github.com/alex1504/vue2.0-demo" target="_blank" rel="external">https://github.com/alex1504/vue2.0-demo</a></p>
<p>扫描二维码预览：<br><img src="/img/post/2017-03-04-vuejs-demo-qrcode.png" alt="二维码地址"></p>
<h1 id="更新记录："><a href="#更新记录：" class="headerlink" title="更新记录："></a>更新记录：</h1><p>2017.1.13 主导航电影、音乐、图书、图片使用router跳转电影模块使用tab菜单切换各个列表模块下拉滚动加载图片模块使用flex布局实现瀑布流效果<br>2017.1.17 增加了电影详情模块，优化路由跳转<br>2017.1.18 增加了登录、登出模块，使用leancloud数据存储功能<br>2017.1.19 增加了图片详情模块，增加了新的生产依赖vue-touch<br>2017.2.6 新增用户注册模块</p>
<h1 id="开发依赖："><a href="#开发依赖：" class="headerlink" title="开发依赖："></a>开发依赖：</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">"babel-core"</span>: <span class="string">"^6.0.0"</span>,</div><div class="line"><span class="string">"babel-loader"</span>: <span class="string">"^6.0.0"</span>,</div><div class="line"><span class="string">"babel-preset-es2015"</span>: <span class="string">"^6.0.0"</span>,</div><div class="line"><span class="string">"cross-env"</span>: <span class="string">"^3.0.0"</span>,</div><div class="line"><span class="string">"css-loader"</span>: <span class="string">"^0.25.0"</span>,</div><div class="line"><span class="string">"file-loader"</span>: <span class="string">"^0.9.0"</span>,</div><div class="line"><span class="string">"mockjs"</span>: <span class="string">"^1.0.1-beta3"</span>,</div><div class="line"><span class="string">"node-sass"</span>: <span class="string">"^4.2.0"</span>,</div><div class="line"><span class="string">"sass"</span>: <span class="string">"^0.5.0"</span>,</div><div class="line"><span class="string">"sass-loader"</span>: <span class="string">"^4.0.0"</span>,</div><div class="line"><span class="string">"style-loader"</span>: <span class="string">"^0.13.1"</span>,</div><div class="line"><span class="string">"vue-loader"</span>: <span class="string">"^10.0.0"</span>,</div><div class="line"><span class="string">"vue-style-loader"</span>: <span class="string">"^1.0.0"</span>,</div><div class="line"><span class="string">"vue-template-compiler"</span>: <span class="string">"^2.1.0"</span>,</div><div class="line"><span class="string">"webpack"</span>: <span class="string">"^2.1.0-beta.25"</span>,</div><div class="line"><span class="string">"webpack-dev-server"</span>: <span class="string">"^2.1.0-beta.9"</span></div></pre></td></tr></table></figure>
<h1 id="生产环境依赖："><a href="#生产环境依赖：" class="headerlink" title="生产环境依赖："></a>生产环境依赖：</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="string">"axios"</span>: <span class="string">"^0.15.3"</span>,</div><div class="line"><span class="string">"vue"</span>: <span class="string">"^2.1.0"</span>,</div><div class="line"><span class="string">"vue-material"</span>: <span class="string">"^0.5.2"</span>,</div><div class="line"><span class="string">"vue-router"</span>: <span class="string">"^2.1.1"</span>,</div><div class="line"><span class="string">"vue-swipe"</span>: <span class="string">"^2.0.2"</span>,</div><div class="line"><span class="string">"vue-touch"</span>: <span class="string">"^2.0.0-beta.3"</span>,</div><div class="line"><span class="string">"vuex"</span>: <span class="string">"^2.1.1"</span></div></pre></td></tr></table></figure>
<h1 id="使用的接口："><a href="#使用的接口：" class="headerlink" title="使用的接口："></a>使用的接口：</h1><p><a href="https://developers.douban.com/wiki/?title=api_v2" target="_blank" rel="external">豆瓣电影图书接口</a>、gankio图片接口、网易云音乐专辑及搜索接口（见<a href="https://github.com/alex1504/vue2.0-demo/tree/gh-pages/src/components" target="_blank" rel="external">components</a>中请求部分）</p>
<h1 id="说明："><a href="#说明：" class="headerlink" title="说明："></a>说明：</h1><ul>
<li>Vue-material中的spinner组件在某些浏览器无法正常显示，在chrome以及微信中体验效果较好。</li>
<li>由于众所周知的原因，Vue-material中的原生icon使用了阿里的<a href="http://www.iconfont.cn/" target="_blank" rel="external">iconfont</a>替代。（已经在许多歌项目使用，表示图标非常丰富，调用方式多样）</li>
<li>由于ithub访问速度原因，初次加载需稍等片刻。</li>
<li>登录及注册使用了Leancloud的后端云服务，关于如何使用你可以点<a href="https://leancloud.cn/docs/demo.html" target="_blank" rel="external">这里</a>查看LeanCloud搭建的各类应用演示。</li>
</ul>
<h1 id="关于使用Leancloud接口"><a href="#关于使用Leancloud接口" class="headerlink" title="关于使用Leancloud接口"></a>关于使用Leancloud接口</h1><p>细心的小伙伴应该会发现，使用LN接口必须使用AV.init对接口进行初始化，接收的参数是你的APPID和APPScret，也就是说即使我们的项目不开源，通过前端审查脚本也可以知道你的应用id及密钥，进而可以轻松调取你的接口做坏坏的事情。<br>为此如果你需要纯前后端分离的进行接口调用，LN给予的安全方案是配置web安全域名，也就是只有安全域名才能访问你的接口。<br><img src="/img/post/2017-02-04-leancloud-web-security.png" alt="安全域名"><br>另外更稳妥的做法是传统开发APP的方式，将APPID和APPScret在后台代码中对接口进行初始化，前端就审查不到了，比如koa就可以单独为静态文件设置公开访问的静态目录，对于敏感数据文件可以设置存放在安全的前端无法访问的目录下面。<br>在有后端对接口实现复杂的逻辑封装的时候，推荐LN官方推出的<a href="https://leancloud.cn/docs/leanengine_overview.html" target="_blank" rel="external">LeanEngine</a>运行方案，有Nodejs、Pythod、PHP、Java四个版本。</p>
<h1 id="感触："><a href="#感触：" class="headerlink" title="感触："></a>感触：</h1><ol>
<li>MVVM三分天下，如果你希望一个轻巧、易上手、文档详尽的框架，选择<a href="https://cn.vuejs.org/" target="_blank" rel="external">Vuejs</a></li>
<li>如果你十分熟练jQuery的用法，在Vuejs中你很容易发现一些遗传着“经典”的足迹</li>
<li>仔细发现，如果你学习过js设计模式，你会觉得Vuejs的设计很像其中一种模式——单例模式</li>
<li>如果你需要做一个小型应用，不妨使用后端云服务（比如<a href="https://leancloud.cn/" target="_blank" rel="external">LeanCloud</a>、<a href="http://www.bmob.cn/" target="_blank" rel="external">Bomb</a>、<a href="https://maxleap.cn/s/web/zh_cn/index.html" target="_blank" rel="external">maxLeap</a>），既减少你的开发时间也减少开发成本；但大型的应用和数据敏感型的应用肯定是不推荐用别人的服务器的。</li>
<li>ES6还没完全支持，ES7就发布了，关于js逐渐融合各种语言优良特性不断发展的前景个人是十分看好的，毕竟js创始人Brendan Eich仅用几天时间就完成了它的设计。在开发过程中也确实因为js的某些缺陷增大了开发的代码量，不得不用一些设计模式去弥补。关于ES6、ES7的新特性，会在今后逐渐学习并记录。</li>
<li>见过一些reactNative制作的应用，体验和原生APP无任何差别，有Vue Native么？不妨给它加层包装，叫做<a href="https://weex.incubator.apache.org/cn/" target="_blank" rel="external">weex</a>，后面也会慢慢地了解和学习。</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>js</tag>
        <tag>vuejs</tag>
      </tags>
  </entry>
  <entry>
    <title>vuejs开发H5页面总结</title>
    <url>/2017/07/03/vuejs-develop-h5-experience/</url>
    <content><![CDATA[<p>最近参与了APP内嵌H5页面的开发，这次使用vuejs替代了jQuery，仅仅把vuejs当做一个库来使用，效率提高之外代码可读性更强，在此分享一下自己的一些开发中总结的经验。</p>
<h1 id="关于布局方案"><a href="#关于布局方案" class="headerlink" title="关于布局方案"></a>关于布局方案</h1><p>当拿到设计师给的UI设计图，前端的首要任务就是布局和样式，相信这对于大部分前端工程师来说已经不是什么难题了。移动端的布局相对PC较为简单，关键在于<strong>对不同设备的适配</strong>。之前介绍了一篇关于移动端<a href="http://www.huzerui.com/blog/2016/06/16/mobile-rem/" target="_blank" rel="external">rem布局方案</a>，这大致是网易H5的适配方案。不过实践中发现淘宝开源的<a href="https://github.com/amfe/lib-flexible" target="_blank" rel="external">可伸缩布局方案</a>效果更好且更容易使用。</p>
<p>网易云的方案总结为：根据<strong>屏幕大小 / 750 = 所求字体 / 基准字体大小</strong>比值相等，动态调节html的font-size大小。</p>
<p>淘宝的方案总结为：根据设备设备像素比设置scale的值，保持视口device-width始终等于设备物理像素，接着根据屏幕大小动态计算根字体大小，具体是将屏幕划分为10等分，每份为a，1rem就等于10a。</p>
<p>通常我们会拿到750宽的设计稿，这是基于iPhone6的物理分辨率。有的设计师也许会偷懒，设计图上面没有任何的标注，如果我们边开发边量尺寸，无疑效率是比较低的。要么让设计师标注上，要么自食其力。如果设计师实在没有时间，推荐使用<a href="http://www.getmarkman.com/" target="_blank" rel="external">markman</a>进行标注，免费版阉割了一些功能（比如无法保存本地）不过基本满足了我们的需求了。</p>
<p>标注完成后开始写我们的样式，使用了淘宝的lib-flexible库之后，我们的根字体基准值就为750/100*10 = 75px。此时我们从图中若某个标注为100px，那么css中就应该设置为100/75 = 1.333333rem。所以为了提高开发效率，可以使用px转化为rem的插件。如果你使用sublimeText，可以用 <a href="https://packagecontrol.io/packages/rem-unit" target="_blank" rel="external">rem-unit</a><br><img src="http://qiniu.huzerui.com/image/2017-07-03-rem-unit.gif" alt="rem-unit"><br>如果你用vscode编辑器，推荐 <a href="https://marketplace.visualstudio.com/items?itemName=cipchk.cssrem" target="_blank" rel="external">cssrem</a><br><img src="http://qiniu.huzerui.com/image/2017-07-03-css-to-rem.gif" alt="pxtorem"></p>
<p>使用rem单位注意以下几点：</p>
<ol>
<li>在所有的单位中，font-size推荐使用px，然后结合媒体查询进行重要节点的控制，这样可以满足突出或者弱化某些字体的需求，而非整体调整。</li>
<li>众向的单位可以全部使用px，横向的使用rem，因为移动设备宽度有限，而高度可以无限向下滑动。但这也有特例，比如对于一些活动注册页面，需要在一屏幕内完全显示，没有下拉，这时候所有众向或者横向都应该使用rem作为单位。如图：<br><img src="http://qiniu.huzerui.com/image/2017-07-03-rem-description.jpg" alt="rem-desc"><br>左图的表单高度单位由于下边空距较大，使用px在不同屏幕显示更加；而右边的活动注册页由于不能出现滚动条，所有的众向高度、margin、padding都应该使用rem。</li>
<li>border、box-shadow、border-radius等一些效果应该使用px作为单位。</li>
</ol>
<h1 id="基于接口返回数据的属性注入"><a href="#基于接口返回数据的属性注入" class="headerlink" title="基于接口返回数据的属性注入"></a>基于接口返回数据的属性注入</h1><p>可能大家不明白什么叫”基于接口返回数据的属性注入”，在此之前，先说一下表单数据的绑定方式，一个重要的点是<strong>有几份表单就分开几个表单对象进行数据绑定</strong>。</p>
<p>已上图公积金查询为例，由于不同城市会有不同的查询要素，可能登陆方式只有一种，也可能有几种。比如上图有三种登陆方式，在使用vue布局时，有两种方案。一是只建立一个表单用于数据绑定，点击按钮触发判断；而是有几种登陆方式建立几个表单，用一个字段标识当前显示的表单。由于使用第三方的接口，一开始也没有先进行接口返回数据结构的查看，采用了第一种错误的方式，错误一是每种登陆方式下面的登陆要素的数量也不同，错误二是数据绑定在同一个表单data下，当用户在用户名登陆方式输入用户名密码后，切换到客户号登陆方式，就会出现数据错乱的情况。</p>
<p>解决完布局问题后，我们需要根据设计图定义一些状态，比如当前登陆方式的切换、同意授权状态的切换、按钮是否可以点击的状态、是否处于请求中的状态。当然还有一些app穿过来的数据，这里就忽略了。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">data: &#123;</div><div class="line">    <span class="attr">tags</span>: &#123;</div><div class="line">        <span class="attr">arr</span>: [<span class="string">''</span>],</div><div class="line">        <span class="attr">activeIndex</span>: <span class="number">0</span></div><div class="line">    &#125;,</div><div class="line">    <span class="attr">isAgreeProxy</span>: <span class="literal">true</span>,</div><div class="line">    <span class="attr">isLoading</span>: <span class="literal">false</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>接着审查一下接口返回的数据，推荐使用chrome插件postman，比如呼和浩特的登陆要素如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="string">"code"</span>: <span class="number">2005</span>,</div><div class="line">    <span class="string">"data"</span>: [</div><div class="line">        &#123;</div><div class="line">            <span class="string">"name"</span>: <span class="string">"login_type"</span>,</div><div class="line">            <span class="string">"label"</span>: <span class="string">"身份证号"</span>,</div><div class="line">            <span class="string">"fields"</span>: [</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_name"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"身份证号"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"text"</span></div><div class="line">                &#125;,</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_pass"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"密码"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"password"</span></div><div class="line">                &#125;</div><div class="line">            ],</div><div class="line">            <span class="string">"value"</span>: <span class="string">"1"</span></div><div class="line">        &#125;,</div><div class="line">        &#123;</div><div class="line">            <span class="string">"name"</span>: <span class="string">" login_type"</span>,</div><div class="line">            <span class="string">"label"</span>: <span class="string">"公积金账号"</span>,</div><div class="line">            <span class="string">"fields"</span>: [</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_name"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"公积金账号"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"text"</span></div><div class="line">                &#125;,</div><div class="line">                &#123;</div><div class="line">                    <span class="string">"name"</span>: <span class="string">"user_pass"</span>,</div><div class="line">                    <span class="string">"label"</span>: <span class="string">"密码"</span>,</div><div class="line">                    <span class="string">"type"</span>: <span class="string">"password"</span></div><div class="line">                &#125;</div><div class="line">            ],</div><div class="line">            <span class="string">"value"</span>: <span class="string">"0"</span></div><div class="line">        &#125;</div><div class="line">    ],</div><div class="line">    <span class="string">"message"</span>: <span class="string">"登录要素请求成功"</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>可以看到呼和浩特有两种授权登陆方式，我们在data中定义了一个loginWays，初始为空数组，接着methods中定义一个请求接口的函数，里面就是基于返回数据的基础上为上面fields对象注入一个input字段用于绑定，这就是所谓的基于接口返回数据的属性注入。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">methods: &#123;</div><div class="line">    <span class="attr">queryloginWays</span>: <span class="function"><span class="keyword">function</span>(<span class="params">channel_type, channel_code</span>) </span>&#123;</div><div class="line">        <span class="keyword">var</span> params = <span class="keyword">new</span> URLSearchParams();</div><div class="line">        params.append(<span class="string">'channel_type'</span>, channel_type);</div><div class="line">        params.append(<span class="string">'channel_code'</span>, channel_code);</div><div class="line">        axios.post(<span class="keyword">this</span>.loginParamsProxy, params)</div><div class="line">            .then(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</div><div class="line">                <span class="built_in">console</span>.log(res);</div><div class="line">                <span class="keyword">var</span> code = res.code || res.data.code;</div><div class="line">                <span class="keyword">var</span> msg = res.message || res.data.message;</div><div class="line">                <span class="keyword">var</span> loginWays = res.data.data ? res.data.data : res.data;</div><div class="line">                <span class="comment">// 查询失败</span></div><div class="line">                <span class="keyword">if</span> (code != <span class="number">2005</span>) &#123;</div><div class="line">                    alert(msg);</div><div class="line">                    <span class="keyword">return</span>;</div><div class="line">                &#125;</div><div class="line">                <span class="comment">// 添加input字段用于v-model绑定</span></div><div class="line">                loginWays.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">loginWay</span>) </span>&#123;</div><div class="line">                    loginWay.fields.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">field</span>) </span>&#123;</div><div class="line">                        field.input = <span class="string">''</span>;</div><div class="line">                    &#125;)</div><div class="line">                &#125;)</div><div class="line">                <span class="keyword">this</span>.loginWays = loginWays;</div><div class="line">                <span class="keyword">this</span>.tags.arr = loginWays.map(<span class="function"><span class="keyword">function</span>(<span class="params">loginWay</span>) </span>&#123;</div><div class="line">                    <span class="keyword">return</span> loginWay.label;</div><div class="line">                &#125;)</div><div class="line">            &#125;.bind(<span class="keyword">this</span>))</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>即使返回的数据有我们不需要的数据也没有关系，这样保证我们不会遗失进行下一步登陆所需要的数据。</p>
<p>这样多个表单绑定数据问题解决了，那么怎么进行页面间数据传递？如果是app传过来，那么通常使用URL拼接的方式，使用window.location.search获得queryString后再进行截取；如果通过页面套入javaWeb中，那么直接使用”${字段名}”就能获取，注意要js中获取java字段需要加双引号。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">computed: &#123;</div><div class="line">        <span class="comment">// 真实姓名</span></div><div class="line">        realName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.getQueryVariable(<span class="string">'name'</span>) || <span class="string">''</span></div><div class="line">        &#125;,</div><div class="line">        <span class="comment">// 身份证</span></div><div class="line">        identity: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> <span class="built_in">parseInt</span>(<span class="keyword">this</span>.getQueryVariable(<span class="string">'identity'</span>)) || <span class="string">''</span></div><div class="line">        &#125;,</div><div class="line">        <span class="comment">/*If javaWeb</span></div><div class="line">        realName: function() &#123;</div><div class="line">            return this.getQueryVariable('name') || ''</div><div class="line">        &#125;,</div><div class="line">        identity: function() &#123;</div><div class="line">            return parseInt(this.getQueryVariable('identity')) || ''</div><div class="line">        &#125;*/</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">methods</span>: &#123;</div><div class="line">        <span class="attr">getQueryVariable</span>: <span class="function"><span class="keyword">function</span>(<span class="params">variable</span>) </span>&#123;</div><div class="line">            <span class="keyword">var</span> query = <span class="built_in">window</span>.location.search.substring(<span class="number">1</span>);</div><div class="line">            <span class="keyword">var</span> vars = query.split(<span class="string">'&amp;'</span>);</div><div class="line">            <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; vars.length; i++) &#123;</div><div class="line">                <span class="keyword">var</span> pair = vars[i].split(<span class="string">'='</span>);</div><div class="line">                <span class="keyword">if</span> (<span class="built_in">decodeURIComponent</span>(pair[<span class="number">0</span>]) == variable) &#123;</div><div class="line">                    <span class="keyword">return</span> <span class="built_in">decodeURIComponent</span>(pair[<span class="number">1</span>]);</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">            <span class="built_in">console</span>.log(<span class="string">'Query variable %s not found'</span>, variable);</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure></p>
<h1 id="关于前端跨域调试"><a href="#关于前端跨域调试" class="headerlink" title="关于前端跨域调试"></a>关于前端跨域调试</h1><p>在进行接口请求时，我们的页面通常是在sublime的本地服务器或者vscode本地服务器预览，所以请求接口会遇到跨域的问题。<br>在项目构建的时候通常我们源代码会放在src文件夹下，然后使用gulp进行代码的压缩、合并、图片的优化（根据需要）等等，我们会使用gulp。这里解决跨域的问题可以用<a href="https://www.npmjs.com/package/gulp-connect" target="_blank" rel="external">gulp-connect</a>结合<a href="https://www.npmjs.com/package/http-proxy-middleware" target="_blank" rel="external">http-proxy-middleware</a>，此时我们在gulp-connect中的本地服务器进行预览调试。<br>gulpfile.js如下： 开发过程使用gulp server命令，监听文件改动并使用livereload刷新；使用gulp命令进行打包。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> concat = <span class="built_in">require</span>(<span class="string">'gulp-concat'</span>);</div><div class="line"><span class="keyword">var</span> uglify = <span class="built_in">require</span>(<span class="string">'gulp-uglify'</span>);</div><div class="line"><span class="keyword">var</span> autoprefixer = <span class="built_in">require</span>(<span class="string">'gulp-autoprefixer'</span>);</div><div class="line"><span class="keyword">var</span> useref = <span class="built_in">require</span>(<span class="string">'gulp-useref'</span>);</div><div class="line"><span class="keyword">var</span> connect = <span class="built_in">require</span>(<span class="string">'gulp-connect'</span>);</div><div class="line"><span class="keyword">var</span> proxyMiddleware = <span class="built_in">require</span>(<span class="string">'http-proxy-middleware'</span>);</div><div class="line"></div><div class="line"><span class="comment">// 定义环境变量，若为 dev，则代理src目录； 若为prod，则代理dist目录</span></div><div class="line"><span class="keyword">var</span> env = <span class="string">'prod'</span></div><div class="line"></div><div class="line"><span class="comment">// 跨域代理  将localhost:8088/api 映射到 https://api.shujumohe.com/</span></div><div class="line">gulp.task(<span class="string">'server'</span>, [<span class="string">'listen'</span>], <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> middleware = proxyMiddleware([<span class="string">'/api'</span>], &#123;</div><div class="line">        <span class="attr">target</span>: <span class="string">'https://api.shujumohe.com/'</span>,</div><div class="line">        <span class="attr">changeOrigin</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">pathRewrite</span>: &#123;</div><div class="line">            <span class="string">'^/api'</span>: <span class="string">'/'</span></div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">    connect.server(&#123;</div><div class="line">        <span class="attr">root</span>: env == <span class="string">'dev'</span> ? <span class="string">'./src'</span> : <span class="string">'./dist'</span>,</div><div class="line">        <span class="attr">port</span>: <span class="number">8088</span>,</div><div class="line">        <span class="attr">livereload</span>: <span class="literal">true</span>,</div><div class="line">        <span class="attr">middleware</span>: <span class="function"><span class="keyword">function</span>(<span class="params">connect, opt</span>) </span>&#123;</div><div class="line">            <span class="keyword">return</span> [middleware]</div><div class="line">        &#125;</div><div class="line"></div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'html'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/*.html'</span>)</div><div class="line">        .pipe(useref())</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/css/main.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'main.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/css/'</span>));</div><div class="line"></div><div class="line">    gulp.src(<span class="string">'src/css/share.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'share.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/css/'</span>));</div><div class="line"></div><div class="line">    gulp.src(<span class="string">'src/vendors/css/*.css'</span>)</div><div class="line">        .pipe(concat(<span class="string">'vendors.min.css'</span>))</div><div class="line">        .pipe(autoprefixer(&#123;</div><div class="line">            <span class="attr">browsers</span>: [<span class="string">'last 2 versions'</span>],</div><div class="line">            <span class="attr">cascade</span>: <span class="literal">false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/vendors/css'</span>));</div><div class="line">    <span class="keyword">return</span> gulp</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'js'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'src/vendors/js/*.js'</span>)</div><div class="line">        .pipe(concat(<span class="string">'vendors.min.js'</span>))</div><div class="line">        .pipe(uglify())</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/vendors/js'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'img'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(<span class="string">'src/imgs/*'</span>)</div><div class="line">        .pipe(gulp.dest(<span class="string">'dist/imgs'</span>));</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'listen'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.watch(<span class="string">'./src/css/*.css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/css/*.css'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">    gulp.watch(<span class="string">'./src/js/*.js'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/js/*.js'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">    gulp.watch(<span class="string">'./src/*.html'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        gulp.src([<span class="string">'./src/*.html'</span>])</div><div class="line">            .pipe(connect.reload());</div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line">gulp.task(<span class="string">'default'</span>, [<span class="string">'html'</span>, <span class="string">'css'</span>, <span class="string">'js'</span>, <span class="string">'img'</span>]);</div></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>vuejs</tag>
      </tags>
  </entry>
  <entry>
    <title>基于bmob后端云微信小程序开发</title>
    <url>/2017/10/14/wechat-guita-tabs/</url>
    <content><![CDATA[<p>人的一生90%的时间都在做着无聊的事情，社会的发展使得我们的闲暇时间越来越多，我们把除了工作的其他时间放在各种娱乐活动上    。</p>
<p>程序员有点特殊，他们把敲代码看成娱乐活动的一部分，以此打发时间的不占少数。这不最近无聊搞了一个口袋吉他小程序，使用<a href="http://www.bmob.cn/" target="_blank" rel="external">bmob后端云</a>提供数据存储服务，除吉他谱图片，其他图片存储在<a href="https://www.qiniu.com/" target="_blank" rel="external">七牛</a>。</p>
<p>关于bmob小程序开发文档请戳<a href="https://docs.bmob.cn/data/wechatApp/a_faststart/doc/index.html" target="_blank" rel="external">这里</a>，文档详细简练，主要是缩短了开发周期，不过对于复杂的项目，还是推荐使用自己服务器提供数据服务。</p>
<h1 id="使用微信扫描二维码预览"><a href="#使用微信扫描二维码预览" class="headerlink" title="使用微信扫描二维码预览"></a>使用微信扫描二维码预览</h1><p><img src="http://qiniu.huzerui.com/image/2017-10-14-guita-qrcode.jpg" alt="qrcode"><br>源码地址：<a href="https://github.com/alex1504/wx-guita_tab" target="_blank" rel="external">https://github.com/alex1504/wx-guita_tab</a></p>
<p>下面分点分享下小程序的开发过程中的关键点及感受，说明：</p>
<ol>
<li>小程序标签统称组件，Html标签统称元素。</li>
<li>部分内容会与vuejs及jQuery作对比</li>
</ol>
<h1 id="使用iconfont字体图标"><a href="#使用iconfont字体图标" class="headerlink" title="使用iconfont字体图标"></a>使用iconfont字体图标</h1><p>新建项目并添加图标<br><img src="http://qiniu.huzerui.com/image/2017-10-14-iconfont.jpg" alt="iconfont"><br>在app.wxss中以unicode方式引入<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">@font-face &#123;</div><div class="line">  font-family: &apos;iconfont&apos;;  /* project id 431644 */</div><div class="line">  src: url(&apos;//at.alicdn.com/t/font_431644_aahynh26y6lp7gb9.eot&apos;);</div><div class="line">  src: url(&apos;//at.alicdn.com/t/font_431644_aahynh26y6lp7gb9.eot?#iefix&apos;) format(&apos;embedded-opentype&apos;),</div><div class="line">  url(&apos;//at.alicdn.com/t/font_431644_aahynh26y6lp7gb9.woff&apos;) format(&apos;woff&apos;),</div><div class="line">  url(&apos;//at.alicdn.com/t/font_431644_aahynh26y6lp7gb9.ttf&apos;) format(&apos;truetype&apos;),</div><div class="line">  url(&apos;//at.alicdn.com/t/font_431644_aahynh26y6lp7gb9.svg#iconfont&apos;) format(&apos;svg&apos;);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>定义通用icon样式，定义伪元素<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.icon</span>&#123;</div><div class="line">  <span class="attribute">display</span>: inline-block;</div><div class="line">  <span class="attribute">font-family</span>: <span class="string">'iconfont'</span>;</div><div class="line">&#125;</div><div class="line"><span class="selector-class">.icon-home</span><span class="selector-pseudo">::before</span>&#123;</div><div class="line">  <span class="attribute">content</span>: <span class="string">"\e600"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">&lt;view class=&quot;icon icon-home&quot;&lt;/view&gt;</div></pre></td></tr></table></figure></p>
<h1 id="小程序事件绑定及处理器"><a href="#小程序事件绑定及处理器" class="headerlink" title="小程序事件绑定及处理器"></a>小程序事件绑定及处理器</h1><p>小程序并没有类似vuejs的v-model进行双向绑定，使用bindinput类似jQuery监听input事件在事件处理器中更新数据，通过event对象e.data.value即可获得input的值。<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line">// bindconfirm监听键盘回车事件，focus属性聚焦渲染组件时会自动弹出手机软键盘</div><div class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">'text'</span> <span class="attr">placeholder</span>=<span class="string">'歌曲名 / 歌手'</span> <span class="attr">bindinput</span>=<span class="string">'bindSearchInput'</span> <span class="attr">bindconfirm</span>=<span class="string">'onSearch'</span> <span class="attr">focus</span>&gt;</span><span class="tag">&lt;/<span class="name">input</span>&gt;</span></div></pre></td></tr></table></figure></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">bindSearchInput(e) &#123;</div><div class="line">  <span class="keyword">this</span>.setData(&#123;</div><div class="line">    <span class="attr">searchTxt</span>: e.detail.value</div><div class="line">  &#125;)</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>小程序中的事件处理器并不能像vue一样传入参数，因为事件处理器只有一个默认的参数event对象，在for循环的组件中如果要想获取元素绑定的id，可以通过和jQuery相同的方式绑定data属性。<br><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">&lt;!-- 轮播图 --&gt;</div><div class="line">&lt;swiper indicator-dots=&quot;&#123;&#123;indicatorDots&#125;&#125;&quot; autoplay=&quot;&#123;&#123;autoplay&#125;&#125;&quot; interval=&quot;&#123;&#123;interval&#125;&#125;&quot; duration=&quot;&#123;&#123;duration&#125;&#125;&quot;&gt;</div><div class="line">  &lt;block wx:for=&quot;&#123;&#123;banner_list&#125;&#125;&quot; wx:key=&quot;&#123;&#123;index&#125;&#125;&quot;&gt;</div><div class="line">    &lt;swiper-item bindtap=&quot;navigateToDetail&quot;  data-id=&quot;&#123;&#123;item.href&#125;&#125;&quot;&gt;</div><div class="line">      &lt;image src=&quot;&#123;&#123;item.image&#125;&#125;&quot; class=&quot;slide-image&quot; mode=&quot;widthFix&quot;&gt;&lt;/image&gt;</div><div class="line">    &lt;/swiper-item&gt;</div><div class="line">  &lt;/block&gt;</div><div class="line">&lt;/swiper&gt;</div></pre></td></tr></table></figure></p>
<p>获取id：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//事件处理函数</span></div><div class="line">navigateToDetail: <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</div><div class="line">  <span class="keyword">const</span> id = e.currentTarget.dataset.id;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h1 id="阻止事件冒泡"><a href="#阻止事件冒泡" class="headerlink" title="阻止事件冒泡"></a>阻止事件冒泡</h1><figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">bindtap、bindlongtap、bindtouchstart、bindtouchmove、bindtouchend、bindtouchcancle</div></pre></td></tr></table></figure>
<p>对应阻止冒泡事件将bind用catch替代</p>
<h1 id="setData"><a href="#setData" class="headerlink" title="setData"></a>setData</h1><p>小程序的视图更新需要调用setData修改绑定数据，直接对数据进行修改是不会触发视图层更新的。setData接受一个对象，为需要添加或修改的属性。属性名有点特殊，[]中的值会被识别为变量，因此如果要对对象数组中的某个属性进行修改，只能预先拼接好属性名。<br>错误做法：<br><figure class="highlight"><table><tr><td class="code"><pre><div class="line">// 视图不更新</div><div class="line">this.data.searchSongs[index].love_flag': 2</div><div class="line">// SyntaxError: unknown: Unexpected token</div><div class="line">this.setData(&#123;</div><div class="line">  'searchSongs[' + index + '].love_flag': 2</div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<p>正确做法：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">setSongFlag(e) &#123;</div><div class="line"><span class="comment">// 注意setData属性名[]中的非整数值会被识别为变量</span></div><div class="line"><span class="keyword">let</span> key = <span class="string">'searchSongs['</span> + index + <span class="string">'].love_flag'</span></div><div class="line"><span class="keyword">this</span>.setData(&#123;</div><div class="line">  [key]: <span class="number">2</span></div><div class="line">&#125;)</div></pre></td></tr></table></figure></p>
<h1 id="关于image组件"><a href="#关于image组件" class="headerlink" title="关于image组件"></a>关于image组件</h1><p>小程序wxss的background-image及image组件都不支持本地url<br>在H5的开发中，通常我们会将页面一些不需要根据容器大小来选择显示方式的图片使用img标签，需要一些特殊显示方式的使用background。但小程序只需要image组件便可。它提供的mode属性和背景定义图片及img元素控制图片显示方式对比</p>
<table>
<thead>
<tr>
<th>mode属性</th>
<th style="text-align:right">background-size</th>
<th style="text-align:right">html img元素</th>
</tr>
</thead>
<tbody>
<tr>
<td>scaleToFill</td>
<td style="text-align:right">100%,100%(默认)</td>
<td style="text-align:right">width:100%;height:100%</td>
</tr>
<tr>
<td>aspectFit</td>
<td style="text-align:right">contain</td>
<td style="text-align:right">js实现</td>
</tr>
<tr>
<td>aspectFill</td>
<td style="text-align:right">cover</td>
<td style="text-align:right">js实现</td>
</tr>
<tr>
<td>widthFix</td>
<td style="text-align:right">100%, auto</td>
<td style="text-align:right">width: 100%;</td>
</tr>
</tbody>
</table>
<p>其他的top、bottom、right、left等不缩放图片调整位置的属性与background-position作用相同，img元素则只能通过定位控制。</p>
<h1 id="小程序API异步方案"><a href="#小程序API异步方案" class="headerlink" title="小程序API异步方案"></a>小程序API异步方案</h1><p>如果没有强迫症，小程序API使用默认回调的方式即可；另外由于小程序只支持es6，不支持async及await，也可以将API封装成promise的方式，参考funnycoder的<a href="http://wangyaxing.top/2017/09/01/wxapp/" target="_blank" rel="external">这篇文章</a>。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">promiseify</span>(<span class="params">func</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="function">(<span class="params">args = &#123;&#125;</span>) =&gt;</span> &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</div><div class="line">            func.call(wx, <span class="built_in">Object</span>.assign(args, &#123;</div><div class="line">                <span class="attr">success</span>: resolve,</div><div class="line">                <span class="attr">fail</span>: reject,</div><div class="line">            &#125;));</div><div class="line">        &#125;)</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line"><span class="keyword">for</span> (<span class="keyword">const</span> key <span class="keyword">in</span> wx) &#123;</div><div class="line">    <span class="keyword">if</span> (<span class="built_in">Object</span>.prototype.hasOwnProperty.call(wx, key) &amp;&amp; <span class="keyword">typeof</span> wx[key] === <span class="string">'function'</span>) &#123;</div><div class="line">        wx[<span class="string">`_<span class="subst">$&#123;key&#125;</span>`</span>] = promiseify(wx[key]);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h1 id="小程序问题"><a href="#小程序问题" class="headerlink" title="小程序问题"></a>小程序问题</h1><ul>
<li>调试器没有css快捷提示功能和颜色面板，影响布局及颜色调整效率（随性派）</li>
<li>无法引入第三方js库</li>
<li>内置组件单调，没有考虑字体数量比较多时的自适应情况</li>
<li>不支持跳转外部链接</li>
<li>背景图片或者image组件不能用本地图片</li>
</ul>
<h1 id="关于小程序审发布或更新"><a href="#关于小程序审发布或更新" class="headerlink" title="关于小程序审发布或更新"></a>关于小程序审发布或更新</h1><p>小程序上线需要经过审核、发布两个过程。<br>审核通过后有全量更新、或者分阶段发布，小程序才会更新，首次发布没有选项。</p>
<p>全量发布：即时向全量微信用户发布新版小程序。<br>分阶段发布：新版小程序将在15天内以开发者自定义比例，向微信用户发布更新<br>详情见知乎：<a href="https://www.zhihu.com/question/65215216/answer/228639943" target="_blank" rel="external">发布小程序时选择全量发布和分阶段发布是什么意思？</a></p>
<p>不得不说小程序审核速度是非常快的，即便是个人申请（相比以企业账号申请会有应用服务类型限制），通常小程序没有涉及政策不允许的内容或者超过小程序允许的应用服务类型，都是可以顺利通过，初次体验，即便在国庆期间，也是有工作团队进行审核，审核时间通常在几小时内。</p>
<h1 id="未完待续"><a href="#未完待续" class="headerlink" title="未完待续"></a>未完待续</h1><p>一直关注着小程序，一直处于不愠不火的状态，但微信团队一直坚持在更新。从小程序的<a href="https://mp.weixin.qq.com/debug/wxadoc/dev/devtools/uplog.html" target="_blank" rel="external">历史更新日志</a>可以看到，无论是开发工具、基础库、与原生硬件交互API都在不断的更新或者修复异常bug，有时间希望做些与硬件交互更有趣的小程序和大家分享。</p>
<p>这个简易小程序将加入评论功能，用户系统功能、曲谱本地收藏、分享、改善图片加载、滑动位置保存等功能及问题，借此熟悉小程序开发以便做出更有趣的东西出来，因此本篇文章随开发过程持续更新。<br>希望这篇分享对你有所帮助，更希望能与同样热爱前端的你交流心得体会抑或工作经历、困扰等，欢迎知乎私信或邮件交流。</p>
<p>知乎：<a href="https://www.zhihu.com/people/huzerui" target="_blank" rel="external">https://www.zhihu.com/people/huzerui</a><br>邮箱：me@huzerui.com</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>微信小程序</tag>
      </tags>
  </entry>
</search>
