language: node_js # 设置语言
node_js: stable # 设置相应版本
branches:
  only:
  - master

install: npm install

before_script:
- git config --global user.name "alex1504"
- git config --global user.email "alex1504@163.com"
- sed -i "s/__GITHUB_TOKEN__/${__GITHUB_TOKEN__}/" _config.yml

script:
    - hexo clean # 清除
    - hexo g # 生成
    - hexo d # 部署
